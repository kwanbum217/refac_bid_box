# 용역 모델 손실함수 탐색: quantile(0.5) 채택 후보

> **작성일**: 2026-08-07
> **버전**: v1.0.0
> **상태**: 홀드아웃 우세 확인. **운영 경로 쌍대 검정 전이므로 승격하지 않음**
> **대상**: Servc (용역) 낙찰률 예측 모델 `servc_institution_v1`
> **측정 스크립트**: [`scripts/eval_servc_huber_alpha.py`](../../scripts/eval_servc_huber_alpha.py)

---

## 1. 왜 이 축을 봤는가

[`servc_hyperparam_search_20260804.md`](servc_hyperparam_search_20260804.md) 의
좌표 하강 17회는 `num_leaves`, `min_child_samples`, `learning_rate`,
`n_estimators`, `colsample_bytree`, `subsample`, `reg_lambda` 를 다뤘습니다.
**손실함수는 탐색 축에 없었습니다.** `trainer._train_lightgbm` 은
`objective="huber", alpha=1.0` 으로 고정돼 있었고 한 번도 재본 적이 없습니다.

이 축이 중요한 이유가 있습니다. Huber 는 잔차가 `alpha` 보다 작으면 L2(평균
겨냥), 크면 L1(중앙값 겨냥)으로 동작합니다. 현행 `alpha` 는 1.0 인데 운영
실측에서 오차가 0.5%p 이내인 건이 64%, 1%p 이내가 75% 입니다. **핵심 지표가
걸린 구간 전체가 평균 겨냥으로 학습되고 있었습니다.**

2026-08-06~07 진단에서 세 번 만난 "모델이 조건부 중앙값을 겨냥해 평균 편향이
남는다" 는 현상도 이 손잡이가 정하는 동작입니다. 현상을 세 번 관찰하고도
손잡이 자체는 돌려보지 않았습니다.

---

## 2. 설계

| 항목 | 설정 |
| --- | --- |
| 학습 구간 | 2025년까지 861,291행 |
| 평가 구간 | 2026년 56,338행 out-of-sample |
| 기준선 | `huber alpha=1.0` (현행 서빙 설정) |
| Huber 후보 | alpha 0.2 / 0.5 / 2.0 / 5.0 |
| 대조 | `quantile alpha=0.5` (MAE 직접 최적화) |
| 고정 | 손실함수 외 하이퍼파라미터와 학습 상한 전부 동일 |
| 판정 | 같은 공고의 절대오차 쌍대 차이 + 최소 감지 차이 |

쌍대 비교는 7개 모집단에서 냅니다. `alpha` 를 낮추면 큰 오차에 관대해지므로
산포가 큰 셀에서 손해, 작은 셀에서 이득이 나는 **재분배**가 예상됐기 때문입니다.
전체만 보면 상쇄돼 판별 불가로 끝나고 이유를 모르게 됩니다.

---

## 3. 결과

### 3.1 요약

| 후보 | MAE | RMSE | 편향 | 0.5%p 적중 | 1%p 적중 | 3%p 적중 | 학습 초 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| huber a=0.2 | 1.7971 | 3.1088 | -0.6532 | 51.75% | 60.59% | 74.51% | 42.8 |
| huber a=0.5 | 1.3477 | 2.7539 | -0.0524 | 59.48% | 70.73% | 85.93% | 41.4 |
| **huber a=1 (기준선)** | 1.3554 | 2.7454 | +0.0200 | 58.53% | 70.74% | 86.12% | 35.5 |
| huber a=2 | 1.3900 | 2.7330 | +0.0791 | 55.23% | 68.84% | 85.98% | 22.1 |
| huber a=5 | 1.4644 | 2.7127 | +0.1766 | 49.65% | 65.28% | 85.10% | 13.3 |
| **quantile a=0.5** | **1.3312** | 2.7834 | -0.0360 | **59.65%** | **71.20%** | 86.06% | 21.3 |

`alpha` 를 키울수록 MAE 와 적중률이 단조롭게 나빠지고 RMSE 는 좋아집니다.
평균 겨냥으로 갈수록 제곱오차에 유리해지는 정상적인 맞바꿈입니다.

### 3.2 쌍대 검정 (후보 - 기준선, 음수면 후보가 나음)

**quantile a=0.5**

| 모집단 | 건수 | 평균 차이 | 표준오차 | t | 최소 감지 차이 | 판정 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 전체 | 56,338 | **-0.02417** | 0.00161 | -14.98 | 0.00323 | 후보 우세 |
| 하한율 보유 | 37,581 | -0.03346 | 0.00159 | -21.03 | 0.00318 | 후보 우세 |
| 하한율 결측 | 18,757 | -0.00556 | 0.00365 | -1.52 | 0.00730 | 판별 불가 |
| 일반용역 | 35,915 | -0.02187 | 0.00229 | -9.55 | 0.00458 | 후보 우세 |
| 기술용역 | 20,423 | -0.02822 | 0.00190 | -14.88 | 0.00379 | 후보 우세 |
| 기관 이력 얕음(<50) | 12,339 | -0.01447 | 0.00441 | -3.28 | 0.00881 | 후보 우세 |
| 기관 이력 두꺼움(>=50) | 43,999 | -0.02689 | 0.00166 | -16.24 | 0.00331 | 후보 우세 |

**6개 우세, 1개 판별 불가, 악화 0개.** 관측 차이가 최소 감지 차이의 7.5배입니다.

**huber a=0.5**

| 모집단 | 건수 | 평균 차이 | t | 판정 |
| --- | ---: | ---: | ---: | --- |
| 전체 | 56,338 | -0.00766 | -6.53 | 후보 우세 |
| 하한율 결측 | 18,757 | -0.00201 | -0.82 | 판별 불가 |
| **기관 이력 얕음(<50)** | 12,339 | **+0.00973** | **+2.69** | **기준선 우세** |
| 기관 이력 두꺼움(>=50) | 43,999 | -0.01254 | -11.32 | 후보 우세 |

전체로는 우세하지만 **기관 이력이 얕은 12,339건에서 유의하게 악화**합니다.
재분배 요소가 있어 quantile 보다 열등합니다.

**huber a=0.2 / a=2 / a=5** 는 7개 모집단 전부에서 기준선 우세입니다.

---

## 4. 판정

**`quantile(alpha=0.5)` 를 채택 후보로 올립니다.**

| 기준 | 결과 |
| --- | --- |
| 홀드아웃 개선 | MAE 1.3554 -> 1.3312 (1.8%), 0.5%p 적중 +1.12%p |
| 최소 감지 차이 초과 | 예. 관측 -0.02417 대 한계 0.00323 (7.5배) |
| 악화 집단 | 없음 (6 우세 / 1 판별 불가 / 0 악화) |
| 재분배 여부 | 순증. 어제 최근 가중과 결정적으로 다름 |
| 학습 비용 | 21.3초로 기준선 35.5초보다 **빠름** |

**아직 승격하지 않았습니다.** 스킬 워크플로우의 H 단계(운영 경로 쌍대 검정)를
거치지 않았습니다. 홀드아웃만 보고 내린 승격 판단이 이 프로젝트에서 네 번
뒤집혔습니다.

### 4.1 함께 기록할 대가

**RMSE 가 나빠집니다** (2.7454 -> 2.7834, +1.4%). L1 최적화가 큰 오차를 덜
벌하므로 당연한 결과입니다. 운영 지표가 MAE 와 0.5%p 적중이므로 수용 가능하지만,
제곱오차를 보는 곳이 있다면 이 맞바꿈을 알고 있어야 합니다.

3%p 이내 적중도 86.12% 에서 86.06% 로 미세하게 낮아집니다.

---

## 5. Huber 와 quantile 은 이어지지 않습니다

`alpha` 를 0 으로 줄이면 Huber 가 L1(=quantile 0.5)에 수렴할 것처럼 보이지만
**실측은 반대입니다.**

| 후보 | MAE |
| --- | ---: |
| huber a=0.5 | 1.3477 |
| huber a=0.2 | **1.7971** |
| quantile a=0.5 | 1.3312 |

`alpha` 0.2 에서 MAE 가 급격히 나빠집니다(기준선 대비 32% 악화). LightGBM 의
Huber 구현에서 잔차가 `alpha` 를 넘으면 gradient 가 `±alpha` 로 고정되는데,
`alpha` 가 작으면 대부분의 표본에서 gradient 크기가 같아져 **학습 신호가
평평해집니다.** quantile 목적함수는 L1 을 직접 최적화하므로 이 문제가 없습니다.

**두 목적함수는 연속적으로 이어지지 않습니다.** L1 을 원하면 `alpha` 를 줄이지
말고 objective 를 바꾸십시오.

---

## 6. 승격 전 남은 절차

1. **운영 경로 쌍대 검정.** 후보 모델을 서빙 루트에 임시 ID 로 올리고
   `compare_servc_models_paired.py` 로 champion 과 겨룹니다. 홀드아웃 우세가
   운영에서 뒤집힌 전례가 네 번 있습니다.
2. **예측 구간 확인.** `_conformal_scale` 은 분위 모델의 자체 중심
   `(lo+hi)/2` 를 쓰므로 점 추정 목적함수 변경이 구간 보정에 **영향을 주지
   않습니다.** 코드로 확인했으나 승격 후 실측으로 재확인하십시오.
3. **모델 선택 경로 확인.** `train_and_register` 는 LightGBM/CatBoost/Ridge 중
   MAPE 가 가장 낮은 것을 고릅니다. 목적함수를 바꾸면 이 선택이 달라질 수
   있으므로 후보 선택 결과를 함께 봐야 합니다.
4. 승격은 예행 후 명시적 `--apply`, 사후 기본 서빙 경로 재측정.

구현 시 `trainer._train_lightgbm` 의 `params` 를 바꾸게 되는데, 이 함수는
**물품(Thng)도 함께 씁니다.** 용역에서만 검증된 변경이므로
`CATEGORY_HYPERPARAMS` 처럼 카테고리별로 분기해야 합니다. 물품에 그대로
적용하면 검증되지 않은 변경이 됩니다.

---

## 7. 교훈

**세 번 관찰한 현상의 원인을 손잡이로 갖고 있으면서 돌려보지 않았습니다.**

2026-08-06~07 진단에서 "모델이 조건부 중앙값을 겨냥해 평균 편향이 남는다" 를
하한율 축, 낙찰방법 축, 소분류 축에서 각각 확인했습니다. 세 번 모두 편향
보정을 기각하는 근거로만 썼고, **그 동작을 정하는 파라미터가 탐색된 적 없다는
사실은 확인하지 않았습니다.**

기존 탐색 문서를 읽을 때는 무엇을 찾았는지뿐 아니라 **무엇을 축에 넣지
않았는지**를 함께 보십시오.
