# 인수인계: 20260911 Wave AX·AY 세션 종료

> **작성일**: 2026-09-11
> **Run**: `run_fd19c3f39c9a` (Wave AX)
> **기준 커밋**: `c4fb2a0` -> `21cf1d2`
> **인수 대상**: 다음 코디네이터
> **선행 문서**: [`handoff_20260911_wave_aw_session_close.md`](handoff_20260911_wave_aw_session_close.md)

---

## 0. 다음 세션 첫 작업

**환경을 먼저 올리십시오.** 이전 세션이 컴퓨터 종료를 위해 스택을 내렸습니다.

```sh
docker compose up -d db redis meilisearch app
until curl -sf http://localhost:8000/api/v1/health/ready >/dev/null; do sleep 5; done
```

헬스 경로는 `/api/v1/health/ready` 입니다. `/health` 는 404 입니다.

그다음이 **R-29 입니다.** compare-stats 종단 3,951ms 를 줄이는 일이며, 출발점은
**종단 1.6초 개선이 어느 쿼리에서 왔는지 귀속하는 것**입니다. 통계 갱신 뒤
매칭 쿼리는 그대로였으므로 기관별 상위 10 과 월별 두 건 중 어딘가입니다.

**이제 판정 도구가 있습니다.** 잡음 바닥은 890ms 이고
`scripts/compare_rag_segments.py` 가 RAG 경로의 비교를 판정합니다.
절차는 `docs/analysis/ax1_measurement_design_20260911.md` 를 따르십시오.

---

## 1. 이 세션이 한 일

| 커밋 | 내용 |
| --- | --- |
| `0a5ca95` | 콜드 SQL 측정 설계 개선과 비교 판정기 (AX1) |
| `448644b` | 매칭 쿼리 실행계획 뒤집힘 조사 (AX2) |
| `66537fb` | AX 리뷰 산출물과 산포 초과 분기 테스트 |
| `887f91e` | compare-stats 종단 기준선과 통계 갱신 실험 (AY, 코디네이터 직접) |

Task 4건 전부 `completed`, 자원 잔류 0. 전량 4,385 passed, 규칙 21/21.

---

## 2. R-28 완료: 잡음을 개선으로 못 읽게 만들었습니다

**시간 분기에는 확정과 개선이라는 판정이 존재하지 않습니다.** 가능한 문구는
판정 불가, 구별 불가, 산포 초과(참고) 셋뿐입니다. 임계값을 고른 것이 아니라
문자열 자체가 없어서 구조적으로 막혀 있습니다.

| 판정 | 조건 |
| --- | --- |
| 판정 불가 | 조건별 표본이 `min_samples` 미만 |
| 구별 불가 | 범위가 겹치거나, 평균차가 산포 합 이내 |
| 산포 초과(참고) | 둘 다 아님. **그래도 확정 아님** |

리뷰어가 소표본에서 우연히 시간 확정이 나올 확률을 추적해 **0** 으로 판정했습니다.

**옛 결과 JSON 은 소급 비교가 안 됩니다.** 2026-09-10 이전 측정에는
`structured_sql_traces` 가 없어 비교기가 판정 불가를 냅니다. 이것은 정상 동작이며
결함이 아닙니다. 다시 재야 합니다.

비교기는 읽기 전용이며 `UV_RUN_ALLOWED_SCRIPTS` 에 등록돼 사람 승인 없이
실행됩니다. **그 목록에 무언가 추가할 때는 쓰기 경로가 없는지 코드로 확인하십시오.**

---

## 3. R-27 완료: 원인 미확정, 정황은 확보

세 가설을 각각 기각했습니다.

| 가설 | 기각 근거 |
| --- | --- |
| 영속 통계 변화 | 마지막 갱신 2026-09-01. 두 측정(09-10) 사이에 안 바뀜 |
| 수집 데이터 증가 | `MAX(collected_at)` 2026-09-10 02:04, 두 측정 모두 그 뒤 |
| 인덱스 현황 변화 | 후보 인덱스 집합 불변 |

**계획 전환 경계가 실재합니다.** `EXPLAIN` 추정 기준 2025-08-15(9.5%)와
2025-09-01(9.2%) 사이에 전환점이 있고, 운영 대표값 2025-09-10 은 9.0% 로
경계 바로 옆입니다. 뒤집힘은 이상 현상이 아니라 벼랑 끝 구조였습니다.

남은 후보는 메모리 통계, index dive, 동시 테스트 쓰기입니다.

---

## 4. R-26 완료와 통계 갱신 (코디네이터 직접 측정)

### 4.1 가열 구간과 정상상태를 구분하십시오

| 국면 | 회차 | 범위 | 평균 | 산포 |
| --- | ---: | --- | ---: | ---: |
| 버퍼풀 가열 | 5 | 6,466~17,298ms | 11,208ms | 10,832ms |
| **정상상태** | 6 | 5,038~5,928ms | **5,526ms** | **890ms** |

가열 구간은 단조 감소하며 첫 호출과 정상상태가 **2.7배** 차이 납니다.
과거 기록의 "31.97초", "6.5초", "147.7초" 가 서로 달랐던 이유가 이것입니다.

**정상상태 산포 890ms 가 이 경로의 판정 바닥입니다.**

### 4.2 잔여 2.33초는 조건 차이였습니다

같은 조건에서 재니 종단 5,526ms 에서 SQL 합계 4.17초를 뺀 **약 1.4초**이며,
HTTP·FastAPI·SQLAlchemy·조립·Redis 쓰기 구간입니다. 숨은 SQL 이 아닙니다.
AV1 리뷰어의 경고가 옳았습니다.

### 4.3 `ANALYZE TABLE` 은 실행됐고 되돌릴 수 없습니다

| 테이블 | 갱신 전 | 갱신 후 | 실제 |
| --- | ---: | ---: | ---: |
| `bid_results` | 3,118,641 | 3,388,123 | 3,431,580 |
| `bid_announcements` | 2,295,025 | 6,377,745 | 5,504,119 |

**이 문서 이전의 모든 실행계획 관찰은 옛 통계 기준입니다.** 다음 측정자는
2026-09-11 갱신 상태를 기준선으로 삼으십시오.

동일 절차 재측정은 3,889~4,030ms 평균 3,951ms 산포 141ms 입니다.
범위가 안 겹치고 평균차 1,575ms 가 산포 합 1,031ms 를 넘지만 판정은
**산포 초과(참고)이며 확정이 아닙니다.** 적용 전 통계를 복원할 수 없어
표본을 늘릴 수 없기 때문입니다.

**확정할 수 있는 것은 계획 안정화입니다.** 매칭 쿼리가 `EXPLAIN` 3회 전부
`ALL` 로 고정됐고 산포가 890ms 에서 141ms 로 6.3배 조여졌습니다. 다만
**전체 스캔 쪽으로** 고정됐으며, 매칭 쿼리 단독은 1,265~1,584ms 로 이전과
같은 계열입니다. **따라서 종단 개선은 매칭 쿼리에서 온 것이 아닙니다.**

---

## 5. 코디네이터가 겪은 것

### 5.1 리뷰어 커밋 금지가 효과가 있었습니다

Capsule 의 `context` 에 "커밋하지 말라. git add, git commit, git push 를
실행하지 말라. 커밋은 코디네이터가 한다" 를 넣었더니, grok 이 두 번 다
지켰고 **Level 1 게이트가 처음으로 실패 없이 통과했습니다.** 두 웨이브 연속
겪던 문제가 사라졌습니다.

**리뷰 문서는 코디네이터가 별도 커밋으로 넣습니다.** 워크트리를 제거하기 전에
문서와 `review_done.json` 을 주 저장소로 복사하십시오. 워크트리 제거는
미커밋 파일 때문에 `--force` 가 필요하며, **제거 전에 무엇이 미커밋인지 반드시
확인하십시오.**

### 5.2 Capsule 쓰기 범위 누락을 또 냈습니다

AX1 에서 "비교기를 새로 만들라" 고 하면서 파일명을 `allowed_write_files` 에
넣지 않았습니다. 워커가 정당하게 물었습니다. **새 파일 생성을 요구하는
Capsule 은 그 파일명을 쓰기 범위에 반드시 넣으십시오.** 지난 웨이브에서도
같은 실수를 했습니다.

갱신 대상은 네 곳입니다. 주 저장소와 워크트리 각각의 정본과 자리표시자
Capsule 이며, `allowed_write_files`, `required_write_files`,
`allowed_read_files`, `required_change`, `search_scope` 를 함께 고쳐야 합니다.

### 5.3 감시기 차단 표시는 또 오탐이었습니다

기동 직후 두 워커가 `[차단:실패 정체]` 로 떴습니다. 사유는
`worker_done 보고 파일이 존재하지 않음` 이었고, 방금 시작한 워커에게는 당연한
상태입니다. **터미널을 직접 읽어 확인하십시오.**

### 5.4 리뷰어의 비차단 지적을 이번엔 놓치지 않았습니다

AX1 리뷰어가 `산포 초과(참고)` 분기에 테스트가 없다고 적었습니다.
`blocking_issues` 는 비어 있었고 `remaining_risks` 도 `null` 이었으며
**본문에만 있었습니다.** 지난 웨이브에서 같은 위치의 지적을 놓쳤기에 이번엔
문서를 읽어 찾아 테스트를 추가했습니다.

**리뷰 보고는 checklist 와 blocking 만 보지 말고 본문을 읽으십시오.**

### 5.6 감시기 종료 패턴이 틀려 6대가 쌓였습니다

세션 종료 정리에서 `orca_worker_watch` 프로세스 6개가 살아 있는 것을 발견했습니다.
웨이브마다 하나씩 남아 누적된 것입니다.

원인은 코디네이터가 쓴 종료 패턴입니다. 실제 명령줄은 다음 형태인데,

    orca_worker_watch.py --repo <워크트리> --watch --respawn

`pkill -f "orca_worker_watch.py --watch"` 로 찾았습니다. `--repo <경로>` 가 사이에
끼어 있어 그 문자열은 연속으로 나타나지 않습니다. **한 번도 안 잡혔고 매번
"감시기 정리" 라고 출력만 했습니다.**

올바른 형태입니다.

```sh
pgrep -fl orca_worker_watch        # 먼저 확인
pkill -f orca_worker_watch.py      # 스크립트 이름만으로 찾는다
```

**정리 명령을 쓸 때는 종료 코드나 `pgrep` 으로 실제로 사라졌는지 확인하십시오.**
`pkill` 은 대상이 없어도 조용히 끝납니다. 같은 이유로 워크트리가 이미 제거된
뒤에도 감시기는 없는 경로를 계속 감시하고 있었습니다.

---

### 5.5 병렬 판단 근거

AX1(코드)과 AX2(DB 읽기 조사)를 병렬로 돌렸습니다. 안전했던 이유는 AX2 의
판정 근거를 **실행시간이 아니라 실행계획 선택**으로 못박았기 때문입니다.
그 지표는 옆에서 테스트가 돌아도 흔들리지 않습니다. Capsule 에 그 이유까지
적어 워커가 시간값으로 결론 내지 않게 했습니다.

**측정이 조용한 DB 를 요구하면 병렬로 돌리지 마십시오.** R-26 은 직렬로 했습니다.

---

## 6. 자원 정리 상태

**전부 회수했습니다.**

- 워크트리: 주 저장소 하나. `orca-ax1`, `orca-ax2` 제거
- 브랜치: 병합 확인 후 삭제, `-D` 미사용
- 터미널: 코디네이터 창만 남음
- `orca_settled_session_audit.py` 잔류 없음
- 리뷰 산출물은 `.orca/capsules/<task_id>/review_done.json` 과
  `docs/analysis/*_review_20260911.md` 로 보존
- **Docker 스택을 내렸습니다.** 사용자가 컴퓨터를 끄기 위해 세션 종료 시 정리했습니다.
  다음 세션은 `docker compose up -d db redis meilisearch app` 으로 다시 올리십시오.
  `LATENCY_SEGMENT_LOGGING` 은 `false` 로 원복된 상태입니다
- **배경 감시기를 전부 종료했습니다.** 5.6 절을 보십시오

### 6.1 Task 종결 상태 (`run_fd19c3f39c9a`)

| Task | 상태 | 비고 |
| --- | --- | --- |
| `task_300707949c54` | completed | AX1 빌더 (Muse Spark 1.3) |
| `task_1966202f2fc0` | completed | AX1 리뷰 (grok-4.6, pass) |
| `task_98306a4a5007` | completed | AX2 조사 (Muse Spark 1.3) |
| `task_c0925e1c6071` | completed | AX2 리뷰 (grok-4.6, pass) |

R-26 과 통계 갱신은 Task 로 등록하지 않았습니다. 코디네이터가 직접 수행하는
측정이며 스킬 3.1 이 긴 측정 실행을 위임 대상에서 제외하기 때문입니다.

---

## 7. 남은 항목

| ID | 내용 | 선행 조건 |
| --- | --- | --- |
| **R-29** | compare-stats 잔여 3,951ms 최적화 | 없음. 귀속부터 시작 |
| **R-30** | 통계 갱신 주기 정책 | 없음. 이번에 140% 어긋난 이력 |
| **R-18** | `.env` 비밀값 회전 | 사용자만 가능 |
| **R-12** | G2 Windows Docker Desktop 실기 | Windows 장비 |
| **R-15** | Orca setup 스크립트를 `npm ci` 로 영속 변경 | Orca 앱 UI |
| **R-17** | 협상 가격점수 | **차단**. `k` 와 `T` 원천 부재 |
| **R-21** | SSR E2E Phase 2~4 | 사용자 합의 |
| **R-22** | 관측성 2단계 Prometheus | 메트릭 계측 선행 |
| **R-23** | RPO/RTO 분기 restore drill | 없음 |

**R-19, R-20, R-24, R-25, R-26, R-27, R-28 은 종결했습니다.**

### 7.1 하지 말아야 할 것

- 종단 1,575ms 차이를 개선 효과로 인용하지 마십시오. 산포 초과(참고)입니다.
- 가열 구간과 정상상태를 섞어 재지 마십시오. 2.7배 차이 납니다.
- 2026-09-11 이전의 실행계획 관찰을 현재 기준으로 인용하지 마십시오. 통계가 갱신됐습니다.
- 옛 결과 JSON 으로 비교기를 돌려 놓고 결함이라 하지 마십시오. 판정 불가가 정상입니다.
- 리뷰어에게 빌더 브랜치 커밋을 시키지 마십시오.
- 새 파일 생성을 요구하는 Capsule 에 그 파일명을 빠뜨리지 마십시오.
- `.env` 를 `diff` 로 비교하지 마십시오.

---

## 8. 원격 반영

`21cf1d2` 까지 푸시했습니다. `git status -sb` 로 확인하십시오.

## 9. 모델 배정

| 역할 | 모델 | 결과 |
| --- | --- | --- |
| 빌더·조사 | `opencode/muse-spark-1.3-contributor-free` | 계약 위반 0. **Capsule 결함을 정확히 지적** |
| 리뷰어 | `grok-4.6` (`--always-approve`, 읽기 전용) | pass 2건. **커밋 금지 지시를 지킴. 형식 이탈 없음** |

Codex 는 Wave AV 에서 용량 소진 후 계속 쓰지 않았습니다. Muse Spark 가 세
웨이브 연속 좋았습니다. grok 은 Capsule 에 금지 사항을 명시하면 지킵니다.
**이전 이탈은 지시 부재였지 모델 문제가 아니었습니다.**
