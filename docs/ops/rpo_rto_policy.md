# RPO·RTO 복구 목표 정책 (RPO/RTO Policy)

> **작성일**: 2026-09-06
> **버전**: 1.2
> **수정일**: 2026-09-23 (RTO 단계별 예산 배분)
> **상태**: 확정 (R-07, 사용자 확정 2026-09-06)
> **관련 문서**: [`docs/ops/backup_recovery_runbook.md`](backup_recovery_runbook.md), [`docs/context/CURRENT_STATE.md`](../context/CURRENT_STATE.md), [`docs/context/current_state_facts.yaml`](../context/current_state_facts.yaml)

---

## 1. 확정 목표

| 지표 | 확정값 | 의미 |
| --- | --- | --- |
| RPO (Recovery Point Objective) | 24시간 | 장애 시 최대 24시간치 데이터 유실을 허용합니다. 마지막 유효 스냅샷 이후의 변경은 복원되지 않을 수 있습니다. |
| RTO (Recovery Time Objective) | 4시간 | 장애 발생 시점부터 서비스 정상화까지 최대 4시간을 허용합니다. |

본 값은 사용자가 2026-09-06에 확정한 정본이며 사용자 승인 없이 변경하지 않습니다.

---

## 2. RPO 24시간 충족 근거

현행 백업 주기인 일 1회 스냅샷이 정상 동작하면 스냅샷 간격이 24시간을 넘지 않으므로 RPO 24시간을 충족합니다. 스냅샷별 실제 시점 차이는 매니페스트의 `consistency_window`으로 관측합니다.

선행 조건으로 백업 스케줄이 활성화되어 있어야 합니다. [`docs/ops/backup_recovery_runbook.md`](backup_recovery_runbook.md)에 따르면 `BACKUP_SCHEDULE_ENABLED=false`가 기본값이며 활성화된 백업 주기가 RPO에 영향을 주므로, 비활성화 상태에서는 본 RPO 전제가 성립하지 않습니다. 스냅샷 생성 실패나 `partial_backup: true` 기록은 RPO 위반 위험이므로 5절 조치 순서를 따릅니다.

---

## 3. RTO 4시간 단계별 시간 예산

| 복구 단계 | 4시간 내 예산 | 실측 (2026-09-25) | 측정 방법 |
| --- | --- | ---: | --- |
| 스냅샷 검증 | 1분 | 2.1초 | restore drill 보고서의 단계별 소요 시간 |
| 아카이브 해제 | 1분 | 16.6초 | restore drill 보고서의 단계별 소요 시간 |
| DB import | 70분 | 1,429.2초 | restore drill 보고서의 단계별 소요 시간 |
| G1 무손실 검증 (파일 20.8초 + DB 3.6초) | 1분 | 24.4초 | restore drill 보고서의 단계별 소요 시간 |
| 정리 | 1분 | 5.0초 | restore drill 보고서의 단계별 소요 시간 |
| 서비스 정상화 (app·meilisearch 재생성부터 첫 200 응답) | 1분 | 15.1초 (2026-09-22 실측, 3회 21.7·15.1·15.1초의 중앙값. 2026-09-25 에는 재지 않았다) | `docker compose up -d --force-recreate --no-deps meilisearch app` 후 health healthy 와 `/accounts/login/` 200 까지 |
| Meilisearch 읽기 모델 전체 재색인 | 42분 | 951.2초 (클라이언트 적재 687.9초 + 서버 색인 완료 대기 263.2초) | 빈 임시 Meilisearch(v1.14)에 `scripts/sync_search_index.py` 인자 없이 전체 동기화한 뒤 `/tasks` 의 enqueued·processing 이 0 이 될 때까지 |
| **합계 (재색인 제외)** | - | **약 1,493초 (약 25분)** | drill 보고서 `total_duration_seconds` 1,477.4초 + 서비스 정상화 15.1초 |
| **단계 예산 합계** | 117분 | - | 위 단계 예산의 합 |
| **예비** | 123분 | - | 4시간 - 단계 예산 합계. 장애 진단, 재시도, 사람 판단 대기에 쓴다 |
| **합계 (재색인 포함)** | 4시간 | **약 2,444초 (약 41분)** | 위 합계 + 재색인 951.2초. 단계를 직렬로 더한 값 |

모든 단계가 자기 예산 안입니다. 직전 실측(2026-09-22)은 DB import 1,391.6초, G1 검증 11.1초(파일 3.2초 + DB 7.9초), 재색인 839.3초, 재색인 포함 합계 약 2,278초였습니다.

실측 근거는 `data/backups/restore_drill_report_20260925.json`(스냅샷 `snapshot_20260921_061612`, `main` `3e3bf9be`, G1 PASS)입니다. 2026-09-22 drill 과 **같은 스냅샷**이며, DB import 는 1,391.6초에서 1,429.2초로 2.7% 차이였습니다. G1 파일 검증은 3.2초에서 20.8초로 늘었으나 원인은 확인하지 않았고, 측정 시간대에 같은 호스트에서 다른 프로젝트 작업이 돌고 있었습니다.

**2026-09-11 대비 DB import 1.7배(823.9초에서 1,391.6초)의 판정.** 같은 스냅샷에서 실행 간 차이가 2.7% 이므로 1.7배는 실행 변동이 아니라 스냅샷 차이에서 옵니다. 2026-09-25 drill 이 처음 기록한 테이블별 적재 시간(`database_import_breakdown`, 74구간)에서 `bid_announcements` 데이터 구간이 1,163.7초로 import 의 81.7% 이고(해제 기준 25.0GB, 전체 27.2GB 의 92%), 다음은 `bid_results` 218.2초, `servc_inst_verify` 29.5초이며 나머지 71구간은 합해 약 13초입니다. 2026-09-11 이후 추가된 커버링 인덱스 중 `ix_bid_ann_inst_cat_ntce`(762MB)가 이 테이블에 있어 적재 중 인덱스 유지 비용 가설과 맞습니다. 다만 덤프는 테이블 생성 시 인덱스를 함께 만들고 데이터를 넣으므로 인덱스 비용이 데이터 구간 안에 섞여 있어, 이 측정만으로 가설을 확정하지는 않습니다. 확정하려면 2026-09-11 스냅샷으로 drill 을 돌려 같은 구간을 비교해야 합니다. 과거 경위: 2026-09-22 첫 drill 은 G1 스키마 서명 기준선이 2026-09-14 테이블 추가를 반영하지 않아 `g1_db_verification` FAIL 이었고(총 1,616.6초), 기준선을 `00222bd0` 에서 갱신한 뒤 재실행해 통과했습니다(`data/backups/restore_drill_report_20260922.json`).

재색인 실측(2026-09-25, `main` `3e3bf9be`)은 공고 4,879,588건과 낙찰 3,439,172건, 문서 8,318,760건을 실패 작업 0건으로 적재했고 색인 크기는 23.3GB, 클라이언트 최대 RSS 는 약 188MB 였습니다. 2026-09-22 의 839.3초(색인 21.8GB)보다 13% 늘었고, 늘어난 몫은 클라이언트 적재(581.9초에서 687.9초)에 있습니다. 그 사이 공고 문서마다 적격심사 분석 가능 여부(`qualification_analyzable`)를 판정해 넣도록 바뀌었습니다. 운영 색인을 건드리지 않도록 별도 포트(127.0.0.1:7701)의 임시 컨테이너와 빈 전용 볼륨, 일회용 마스터 키를 썼고 측정 뒤 삭제했습니다. 이 수치는 `MEILI_TIMEOUT_SECONDS=30` 에서 잰 값입니다. 기본값 5초로 전체 재색인이 배치 실패 없이 끝나는지는 확인하지 않았으므로, 실제 복구에서는 같은 값을 주고 실행합니다.

단계별 예산은 2026-09-23 사용자 결정에 따라 **각 단계 실측의 3배를 분 단위로 올림**해 배분했고 합계는 117분입니다. 나머지 123분은 특정 단계에 묶지 않은 예비입니다. 3배 여유는 DB import 가 09-11 823.9초에서 09-22 1,391.6초로 1.7배 흔들린 이력을 흡수하기 위한 것입니다. drill 에서 한 단계가 자기 예산을 넘으면 합계가 4시간 안이라도 그 단계를 5절 1번의 병목 조사 대상으로 삼고, 예산 초과를 예비로 메웠다는 사실을 drill 보고에 적습니다. 실측 합이 4시간을 초과하면 목표 미충족으로 5절을 따릅니다. 실측하지 않은 수치를 실측인 것처럼 기록하지 않습니다.

---

## 4. Restore Drill 주기

분기 1회 실시합니다. drill은 [`docs/ops/backup_recovery_runbook.md`](backup_recovery_runbook.md) 4.4절의 격리 리허설 절차를 따르며 본 정책은 절차를 중복 서술하지 않습니다. drill 보고서의 단계별 소요 시간과 RPO 관측값이 3절 예산 배분의 근거가 됩니다.

---

## 5. 목표 미충족 시 조치 순서

1. drill 보고서와 스냅샷 매니페스트에서 위반 지표를 특정합니다. RPO 위반은 스냅샷 간격과 실패 기록으로, RTO 초과는 단계별 소요 시간의 병목으로 판정합니다.
2. RPO 위반 시 백업 스케줄 주기와 실패 알림을 점검하고 스냅샷 실패 원인을 해소한 뒤 다음 스냅샷에서 충족을 확인합니다.
3. RTO 초과 시 병목 단계를 개선하고 다음 분기 drill에서 재측정합니다.
4. 반복 미충족 시 목표 개정안을 사용자에게 상신합니다. 사용자 승인 없이 목표값을 변경하지 않습니다.
5. 부분 백업(`partial_backup: true`)은 복원 성공 대상으로 기록하지 않습니다.
