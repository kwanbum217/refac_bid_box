# Phase 7 컷오버 선언 (2026-09-01)

> **선언일**: 2026-09-01
> **동결 HEAD**: `f2310c3`
> **선언 주체**: Orca Coordinator (사용자 승인 후)
> **판정 범위**: 3대 리팩토링 목표 G1 / G2 / G3
> **성격**: **조건부 컷오버.** Windows Docker Desktop 실기 검증을 후속 과업으로 분리합니다

---

## 1. 선언

**refac_bid_box 를 컷오버합니다.** G1 과 G3 은 무조건 통과이며, G2 는 **실기 검증을
제외한 조건부 통과**입니다.

| 목표 | 정의 | 판정 | 근거 |
| --- | --- | :---: | --- |
| **G1** | 데이터 무손실 | **통과** | 2절 |
| **G2** | 크로스 플랫폼 | **조건부 통과** | 3절. Windows 실기만 후속 분리 |
| **G3** | 스택 최적화 | **통과** | 4절 |

---

## 2. G1 데이터 무손실 (통과)

2026-09-01 운영 FULLTEXT 인덱스 생성과 제거를 거친 뒤 실측했습니다. 이 조작은
`bid_announcements` 27GB 테이블을 재구축했으므로 무손실 확인이 특히 중요합니다.

| 항목 | 값 | 판정 |
| --- | ---: | :---: |
| `bid_announcements` 행 수 | **5,490,072** | 불변 |
| `bid_results` 행 수 | **3,423,008** | 불변 |
| `procurement` 테이블 수 | 31 | 불변 |
| 잔여 FULLTEXT 인덱스 | 0 | 원상복구 |
| 잔여 `FTS_*` 숨은 컬럼 | 0 | 원상복구 |
| ML 모델 가중치 (`model.bin`) | 5개 | 존재 |
| ChromaDB `bidding_kb` | 존재 | 무결 |
| `tests/test_data_preservation.py` | 3 passed | 통과 |

기존 테이블명·컬럼명·타입 변경은 없습니다.

---

## 3. G2 크로스 플랫폼 (조건부 통과)

### 3.1 통과 근거

2026-08-26 CI run `32930156938`(`5f8174a`)에서 **5개 job 전부 green** 입니다.

| Job | 결과 |
| --- | :---: |
| Docker build | green |
| lint | green |
| Ubuntu | green |
| macOS | green |
| **Windows** | **green** |

Docker + Makefile 기반 실행 경로가 세 플랫폼에서 동일하게 동작함이 CI 로 확인됐습니다.

### 3.2 제외 항목과 그 사유

**Windows Docker Desktop 실기 구동 확인은 하지 못했습니다.** 장비가 없기 때문이며
코드나 설정 결함이 아닙니다. CI Windows job 은 러너에서 테스트를 실행하지만
Docker Desktop 위에서 compose 스택을 통째로 띄우는 검증은 아닙니다.

**이 항목을 컷오버 차단 사유에서 제외합니다.** 근거는 다음과 같습니다.

1. 세 플랫폼 CI 가 green 이고 Windows 도 포함됩니다.
2. 컨테이너 정의는 플랫폼 독립적이며 Windows 전용 분기가 없습니다.
3. 장비 확보 시점이 정해지지 않아 무기한 보류가 됩니다.

### 3.3 후속 과업으로 분리

| 항목 | 내용 |
| --- | --- |
| 과업명 | Windows Docker Desktop 실기 구동 검증 |
| 선행 조건 | Windows 장비 확보 |
| 검증 범위 | `docker compose up` 전 서비스 healthy, 예측 API 응답, 마이그레이션 |
| 실패 시 조치 | G2 를 미통과로 되돌리고 컷오버 선언을 철회합니다 |

**이 과업이 실패하면 컷오버는 철회 대상입니다.** 조건부 통과라는 표현은 그 뜻입니다.

---

## 4. G3 스택 최적화 (통과)

동결 HEAD `cee7463`, 규약 준수 조건에서 재측정한 결과입니다. 상세는
[`../analysis/g3_cutover_verdict_20260901.md`](../analysis/g3_cutover_verdict_20260901.md).

| 게이트 | 3회 최악 P95 | 판정선 | 판정 |
| --- | ---: | ---: | :---: |
| 예측 c1 | 15.03ms | 17.02ms | 통과 |
| 예측 c2 | **19.66ms** | 21.77ms | **통과** |
| 예측 c4 | 32.77ms | 36.81ms | 통과 |
| 예측 c10 | 48.14ms | 100ms | 통과 |
| SSE 첫 토큰 | 2,664.19ms | 3,000ms | 통과 |
| SSE 전체 완료 | 3,353.98ms | 20,000ms | 통과 |

100ms 초과 **0건 / 7,200 요청** 입니다.

**종전 유일 미달이던 c2 는 오염된 측정이었습니다.** 당시 호스트 부하가 42.45% 로
규약 임계(중앙 30%)를 넘겼고, 규약 안에서 재측정하니 통과했습니다.

### 4.1 게이트에서 제외한 지표

| 지표 | 처리 | 근거 |
| --- | --- | --- |
| 콜드 SQL 총량 | 관찰 지표로 강등 | 동일 조건 재측정이 333.19초와 432.99초로 30% 흔들려 재현되지 않습니다 |
| ngram 선행필터 | 기각 | 기대한 19배 개선이 나타나지 않았습니다 |

두 항목의 상세는 [`../analysis/coldsql_metric_demotion_20260901.md`](../analysis/coldsql_metric_demotion_20260901.md)
와 [`../analysis/ngram_prefilter_paired_verdict_20260901.md`](../analysis/ngram_prefilter_paired_verdict_20260901.md)
입니다.

---

## 5. 컷오버 이후에도 유지되는 원칙

컷오버는 최적화의 종료가 아닙니다. `AGENTS.md` 는 **G3 을 상시 과제**로 규정합니다.

| 원칙 | 내용 |
| --- | --- |
| 데이터 무손실 | 테이블명·컬럼명·타입 변경 금지 |
| Train/Serve 단일화 | 특징 생성은 `src/ml/features.py` 단일 함수 |
| 게이트 규약 | 미준수 수치는 양방향 무효 |
| 성능 상시 개선 | 기능 동작이 완료가 아니며 실측으로 확인해야 완료 |

---

## 6. 이 선언이 근거로 삼지 않는 것

| 항목 | 이유 |
| --- | --- |
| 콜드 SQL 총량 수치 | 재현되지 않아 관찰 지표로 강등했습니다 |
| Wave F3 의 ngram 19배 예측 | 운영 실측에서 재현되지 않아 기각했습니다 |
| Windows Docker Desktop 실기 동작 | 검증하지 못했습니다. 3.3 절 후속 과업 |
| 2026-08-22 c2 미달 판정 | 부하 임계 초과 측정이라 판정 근거가 아닙니다 |
