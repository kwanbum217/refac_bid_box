# Task s1: src/ml/trainer.py 모듈 분할 보고서

> **작성일**: 2026-08-17
> **작성자**: Orca Worker (s1/builder)
> **대상 모듈**: `src/ml/trainer.py` (838줄) -> 4개 모듈로 기계적 분할

---

## 1. 개요 및 목적

`src/ml/trainer.py` (838줄)의 복잡도를 완화하고 모듈별 단일 책임을 부여하기 위해 사양에 정의된 고정 경계에 따라 세 개의 모듈(`splitters.py`, `training_config.py`, `conformal.py`)로 기계적 분할을 수행했습니다.
기존 외부 모듈 및 테스트에서의 하위 호환성을 완벽히 유지하기 위해, 이동된 모든 심볼을 `src/ml/trainer.py`에서 재수출했습니다.

---

## 2. 분할 경계 및 심볼 매핑

| 모듈 경로 | 라인 수 | 이동 심볼 목록 | 역할 |
| --- | --- | --- | --- |
| `src/ml/splitters.py` | 94줄 | `TIME_SORT_COLUMN`, `DEFAULT_VALIDATION_SPLIT`, `DEFAULT_N_FOLDS`, `MIN_FOLD_SAMPLES`, `has_time_column`, `_sorted_positions`, `_time_based_split`, `_time_based_kfold_splits` | 시계열 기준 교차 검증 및 홀드아웃 분할 |
| `src/ml/training_config.py` | 211줄 | `DEFAULT_MODEL_NAME`, `CATEGORY_MODEL_NAMES`, `LGB_BASE_PARAMS`, `CATEGORY_HYPERPARAMS`, `NUMERIC_FEATURES`, `TRAINING_FEATURES`, `SERVC_EXTRA_FEATURES`, `model_name_for_category`, `training_features_for_category`, `hyperparams_for_category` | 모델 기본 파라미터, 특징 정의 및 카테고리별 설정 |
| `src/ml/conformal.py` | 132줄 | `INTERVAL_QUANTILES`, `INTERVAL_TARGET_COVERAGE`, `CALIBRATION_SPLIT`, `QUANTILE_PARAM_OVERRIDES`, `QUANTILE_HYPERPARAM_KEY`, `_conformal_scale`, `_train_quantile_models` | 등각예측(Conformal Prediction) 및 분위 회귀 학습 |
| `src/ml/trainer.py` | 502줄 | `ModelTrainer`, `trainer`, `_best_iteration_of`, `_refit_on_full`, `_present_categoricals`, `_to_numeric_matrix`, `_train_ridge_cv`, `_RidgeFrameAdapter`, `_train_lightgbm`, `_train_catboost`, `_CatBoostFrameAdapter`, `_cross_validate_model` + 분할 심볼 전량 재수출 | 모델 학습 오케스트레이션 및 아티팩트 레지스트리 버저닝 |

---

## 3. 수용 조건(Acceptance Criteria) 검증 결과

1. **라인 수 제한 (< 600줄)**:
   - `src/ml/trainer.py`: 502줄 (충족)
   - `src/ml/splitters.py`: 94줄 (충족)
   - `src/ml/training_config.py`: 211줄 (충족)
   - `src/ml/conformal.py`: 132줄 (충족)

2. **함수 및 메서드 본문 불변성**:
   - 모든 함수와 메서드의 본문을 한 글자도 바꾸지 않고 그대로 이동했습니다.
   - AST 구문 분석 검증(`test_ast_function_definitions`) 통과.

3. **심볼 재수출 및 하위 호환성**:
   - `src/ml/trainer.py`의 `__all__`에 이동된 모든 심볼을 등록 및 재수출하여 기존 import 경로(`tests/test_mlops_pipeline.py`, `src/tasks/retrain_task.py` 등)가 그대로 정상 작동합니다.

4. **전체 테스트 통과**:
   - `uv run pytest tests/ -q -m 'not data_assets'`: 1367 passed, 0 failed.

5. **린터 통과**:
   - `uv run ruff check .`: 저장소 전체 범위에서 통과 (All checks passed).

6. **전용 검증 테스트 추가**:
   - `tests/test_trainer_split.py`를 추가하여 재수출 누락 시 즉시 실패하도록 구성하고, 분할된 모듈별 기능 및 라인 수 검증을 완료했습니다.

---

## 4. 결론

사양에 정의된 기계적 분할 요구사항을 100% 충족하였으며, 어떠한 기능적 결함이나 하위 호환성 깨짐 없이 분할을 완료했습니다.
