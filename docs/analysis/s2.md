# S2 작업 분석 및 완료 보고서: planner.py 모듈 분리

> **작업 식별자**: s2 (Dispatch: ctx_c2499577618a)
> **대상 모듈**: `src/app/services/planner.py` -> `src/app/services/planner_interpreter.py`
> **작성일**: 2026-08-17

---

## 1. 작업 개요 및 목적

기존 `src/app/services/planner.py`(644줄)에서 1단계 질의 해석(interpretation) 책임 묶음을 분리하여 `src/app/services/planner_interpreter.py`로 이식하였습니다. 이를 통해 두 파일 모두 600줄 미만으로 모듈 크기를 최적화하고 단방향 의존성(`planner.py` -> `planner_interpreter.py`)을 확립하였습니다.

---

## 2. 심볼 분리 및 이전 내역

### 2.1 이전된 심볼 (planner_interpreter.py)
1. `_load_last_plan(context_state)`
2. `_load_last_tool_results(context_state)`
3. `interpret_request(message, context_state)`

> 옮긴 세 심볼의 함수 본문은 변경 전과 AST 수준에서 100% 동일하게 보존되었습니다.

### 2.2 유지된 심볼 (planner.py)
1. `_step(...)`
2. `_compute_requires_confirmation(...)`
3. `_default_answer_plan(...)`
4. `_build_advisory_plan()`
5. `_attempt_llm_plan_draft(...)`
6. `compile_plan(execution_plan)`
7. `plan_chat_request(message, context_state)`

`planner.py`는 `planner_interpreter`로부터 `interpret_request`, `_load_last_plan`, `_load_last_tool_results`를 import하여 `__all__`을 통해 완벽히 재수출(re-export)합니다.

---

## 3. 핵심 안전성 및 정합성 검증

### 3.1 LLM Draft Mock Patch 안전성
- `tests/test_chatbot_planner.py`에서 `src.app.services.planner._request_llm_plan_draft`를 patch하고, `_attempt_llm_plan_draft`가 해당 모듈 전역을 참조합니다.
- 본 작업에서 `_attempt_llm_plan_draft`와 `_request_llm_plan_draft` import는 `planner.py`에 그대로 유지되어 테스트 patch가 무력화되지 않고 정상 동작함을 확인하였습니다.

### 3.2 단방향 의존성 (Unidirectional Dependency)
- `planner_interpreter.py`는 `planner.py`를 전혀 참조하지 않으며, AST 수준 검증을 통해 역참조가 없음을 확인하였습니다.

### 3.3 라인 수 제한 준수
| 모듈 | 분리 전 라인 수 | 분리 후 라인 수 | 상한 기준 | 충족 여부 |
| --- | --- | --- | --- | --- |
| `src/app/services/planner.py` | 644 | 387 | 600줄 미만 | 충족 |
| `src/app/services/planner_interpreter.py` | - | 307 | 600줄 미만 | 충족 |

---

## 4. 검증 결과 요약

1. **AST 동등성 검증**: `_load_last_plan`, `_load_last_tool_results`, `interpret_request` 3개 심볼 AST 일치 (PASS)
2. **단위 및 연계 테스트**:
   - `tests/test_planner_split.py` (5 passed)
   - `tests/test_chatbot_planner.py` (20 passed)
   - `tests/test_chatbot_advanced.py` (15 passed)
   - `uv run pytest tests/ -q -m 'not data_assets'` 실행 완료 (TestFreePoolOptIn 2건 외 전량 통과)
3. **린터 검증**: `uv run ruff check .` 저장소 전체 통과 (PASS)
4. **에이전트 규칙 검증**: `python3 scripts/validate_agent_rules.py --quiet` 12/12 건 통과 (PASS)
