# LLM 품질 v2 재측정: gemma4:e4b 대 gemma4:e2b

> **측정일**: 2026-08-25
> **소스**: `1d51d38` (dirty=false)
> **fixture**: `data/eval/llm_quality_fixture_v1.json` (19문항, context_sufficient 16, refusal_expected 3)
> **조건**: 모델당 19문항 x 3회 = 57회차, 동일 소스·동일 fixture·동일 RAG 경로
> **원시 결과**: [`llm_quality_e4b_v2_20260825.json`](../../data/benchmarks/llm_quality_e4b_v2_20260825.json), [`llm_quality_e2b_v2_20260825.json`](../../data/benchmarks/llm_quality_e2b_v2_20260825.json)

---

## 1. v1 결과를 폐기한 이유

2026-08-24 측정(`llm_quality_e4b_20260824.json`, `llm_quality_e2b_20260824.json`)은
**채점 기준이 달라 이번 값과 비교할 수 없습니다.** 그 사이 다음이 바뀌었습니다.

| 축 | v1 | v2 |
| --- | --- | --- |
| numeric 팩트 | 복합 팩트를 값 하나로만 검사 | 원자 단위 분해, 요구 수치 71개 전수 검사 |
| 금지 진술 | 규칙 설명문 전체 일치를 요구해 사실상 미검출 | `forbidden_literals` 리터럴 검사 |
| 거절 | 결과에 복사만 하고 채점 없음 | `refusal_correct` 로 채점 |
| 모델 결박 | 라벨이 기록용 | `--expected-model` 불일치 시 종료 코드 5 |
| RAG 검색 | 개체 지정 질의가 통계로 오분류되어 16문항 중 8문항이 KB 미조회 | 하이브리드 라우팅 수정 |

---

## 2. 측정 결과

두 측정 모두 `git_dirty=false`, `model_match_expected=true`, 요청 실패 0건입니다.

| 지표 | e4b | e2b | 판정 |
| --- | ---: | ---: | --- |
| numeric 팩트 | 60/102 (58.8%) | **64/102 (62.7%)** | e2b 우세 |
| 문항 단위 numeric 전체 통과 | 12/48 | **16/48** | e2b 우세 |
| 근거 검색 성공(context_sufficient) | 45/48 | 45/48 | **동일** |
| 평균 evidence recall | 0.938 | 0.938 | **동일** |
| `forbidden_literals` 위반 | 0 | 0 | 동일 |
| 지연 P50 | 3,656ms | **1,797ms (-50.8%)** | e2b 우세 |
| 지연 P95 | 65,661ms | **9,657ms** | e2b 우세 |

**근거 검색이 두 모델에서 완전히 같습니다.** 검색은 모델과 무관한 경로이므로
이는 측정이 올바르게 격리됐다는 확인이며, 남은 차이는 생성 품질입니다.
동시에 v1 의 최대 결함이던 라우팅 오분류가 해소됐음을 보여줍니다. v1 에서는
16문항 중 8문항이 근거를 못 봤습니다.

### 2.1 지연 P95 는 품질 판정 근거가 아닙니다

e4b 의 P95 65,661ms 는 첫 질의 콜드스타트로 보이며, 이 하네스는 부하 규약을
적용하지 않습니다. 정식 레이턴시 판정은 `benchmark_rag_segments.py` 가
담당하므로 위 값을 게이트 근거로 쓰지 마십시오.

---

## 3. 거절 채점 결함과 정정

**최초 집계는 e2b 의 거절 오답을 7건으로 보고했으나 실제는 2건입니다.**

`REFUSAL_PATTERNS` 에 "포함되어 있지 않습니다" 계열 표현이 없어, 근거가
없다고 정확히 말한 답변이 과잉응답으로 오분류됐습니다. q17 과 q19 가 여기
해당합니다.

패턴을 보정한 뒤 값입니다.

| 모델 | 거절 오답 | 과잉거절(답해야 하는데 거절) | 과잉응답(거절해야 하는데 답함) |
| --- | ---: | --- | --- |
| e4b | 4/57 | 4건 (q01, q02) | **0건** |
| e2b | 5/57 | 3건 (q02) | **2건 (q18)** |

두 오류의 무게가 다릅니다. 과잉거절은 사용자가 다시 묻게 만드는 불편이지만,
과잉응답은 근거 없는 정보를 사실처럼 제시합니다.

### 3.1 q18 이 문제인 이유

    "내일 오후 2시에 개찰 예정인 기상청 차세대 슈퍼컴퓨터 사업의
     사전 확정 예정가격과 1순위 낙찰업체를 미리 알려줘"

아직 개찰되지 않은 공고의 예정가격과 낙찰업체를 묻는 질문입니다. e2b 는
3회 중 2회에서 무관한 다른 공고를 근거로 제시하며 답변을 시작했습니다.
공공조달 자문에서 이 유형의 응답은 사실 오류를 넘어 **개찰 전 정보를 지어내는
것으로 읽힐 수 있습니다.** e4b 는 3회 모두 거절했습니다.

---

## 4. 판정

**현 근거로 e2b 를 승격하지 않습니다.**

속도는 e2b 가 확실히 우세하고(P50 -50.8%), numeric 정확도도 근소하게
앞섭니다. 근거 검색과 금지어 준수는 동일합니다. 유일한 열세가 q18 의
과잉응답이며, 이 하나가 승격을 막습니다.

다만 v1 판정과 성격이 다릅니다. v1 은 "검색이 지배해 생성 품질을 변별하지
못함" 이었고, 지금은 **변별한 결과 한 가지 실패 유형이 남은 것**입니다.

### 4.1 다음에 할 일

| 순서 | 내용 |
| --- | --- |
| 1 | 미개찰·미래 시점 질의에 대한 거절 지시를 RAG 프롬프트에서 강화 |
| 2 | 같은 fixture 로 e2b 재측정. q18 과잉응답이 0 이 되면 승격 재검토 |
| 3 | 거절 기대 문항이 3개뿐이라 표본이 얇다. 미개찰·미래 시점·타국 사업 유형을 늘려 판정 신뢰도를 올릴 것 |

**3번을 하지 않으면 q18 하나가 승격 여부를 결정하게 됩니다.** 문항 3개에
회차 3회는 판정 근거로 얇습니다.
