# task_63f8823e4969 분석 보고서: 공급망 액션 및 도구 불변 참조 고정 (S-02)

> **작성일**: 2026-09-05
> **태스크 ID**: `task_63f8823e4969`
> **상태**: 완료 (succeeded)
> **대상 작업**: GitHub Actions 및 스캔 도구 버전 불변 참조 고정 (외부 진단 S-02 조치)

---

## 1. 개요 및 배경

2026-09-04 외부 진단 보고서 S-02에서 지적된 바와 같이, CI 공급망 보안 게이트가 아무리 fail-closed로 강화되어도 워크플로에서 참조하는 액션과 도구가 이동 가능한 태그(`v5`, `v4` 등)나 동적 최신 버전(`latest`)을 가리키고 있으면 상류 저장소의 코드 변조나 태그 리포인팅에 의해 무단 변경된 코드가 무검증 실행될 수 있습니다.

본 태스크에서는 워크플로(`.github/workflows/ci.yml`, `.github/workflows/release.yml`) 내의 7종 액션을 전부 40자 커밋 SHA로 고정하고, `pip-audit` 및 `actionlint` 컨테이너 도구를 불변 참조로 못 박았습니다.

---

## 2. 액션 커밋 SHA 실측 및 해소 결과

`gh api`를 통해 각 액션의 태그 참조를 실측하였습니다. Git의 annotated tag 객체(`tag`)인 경우 실제 타깃 커밋(`commit`) SHA를 1회 더 풀어내어 액션 런타임이 정확한 커밋을 체크아웃할 수 있도록 조치하였습니다.

| 액션 | 원본 태그 | 태그 객체 SHA / 형태 | 최종 타깃 커밋 SHA (40자) | 비고 |
|---|---|---|---|---|
| `actions/checkout` | `v5` | direct commit | `fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09` | Lightweight tag |
| `actions/setup-python` | `v5` | direct commit | `a26af69be951a213d495a4c3e4e4022e16d87065` | Lightweight tag |
| `actions/setup-node` | `v4` | direct commit | `49933ea5288caeca8642d1e84afbd3f7d6820020` | Lightweight tag |
| `actions/upload-artifact` | `v4` | direct commit | `ea165f8d65b6e75b540449e92b4886f43607fa02` | Lightweight tag |
| `astral-sh/setup-uv` | `v3` | `8d55fbecc275b1c35dbe060458839f8d30439ccf` (tag) | `caf0cab7a618c569241d31dcd442f54681755d39` | Annotated tag 해소 |
| `aquasecurity/trivy-action` | `v0.36.0` | `a9c7b0f06e461e9d4b4d1711f154ee024b8d7ab8` (tag) | `ed142fd0673e97e23eac54620cfb913e5ce36c25` | Annotated tag 해소 |
| `anchore/sbom-action` | `v0.24.2` | `006b7ce8314066bdf1765b4500370d40fa6917a3` (tag) | `3ad7283483fc7af8ff2b4ea19663c2d5ca935e26` | Annotated tag 해소 |

모든 워크플로 파일에서 `<action>@<40자 SHA> # <태그>` 형태로 원래 태그명을 주석에 남겨 사람의 가독성과 추적성을 확보하였습니다.

---

## 3. 스캔 도구 및 컨테이너 불변 참조 고정

1. **`pip-audit`**:
   - 기존: `uvx --from pip-audit pip-audit ...` (매 실행마다 최신 버전 자동 수신)
   - 변경: `uvx --from 'pip-audit==2.10.1' pip-audit ...` (2.10.1 버전 명시적 고정)
2. **`rhysd/actionlint` 도커 이미지 다이제스트**:
   - 로컬 및 CI 검증 컨테이너 다이제스트 실측:
   - `rhysd/actionlint@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667`
   - 문서([`docs/ops/supply_chain_policy.md`](../ops/supply_chain_policy.md), [`docs/ops/ci_supply_chain.md`](../ops/ci_supply_chain.md))에 다이제스트 고정 명령 형태를 명시하여 재현성 확보.

---

## 4. 게이트 강도 및 불변조건 보존 검증

- **차단 임계 보존**: CRITICAL, HIGH 즉시 차단 원칙 유지.
- **Fail-Closed 유지**: 결과 파일 부재 시 즉시 실패, allowlist 외 취약점 잔존 시 즉시 실패 유지.
- **`continue-on-error` 부재**: 실패를 삼키는 우회 플래그 일절 없음.
- **1인 작업 체계 정합성**: PR 승인 절차를 전제하지 않고, 1인 개발자가 커밋 전 실측 및 기계적 린트를 거쳐 작업 브랜치 커밋 메시지로 추적성을 확보하도록 운영 정책 확립.

---

## 5. 검증 명령어 실행 결과

| 검증 항목 | 실행 명령 | 종료 코드 | 결과 요약 |
|---|---|---|---|
| 워크플로 린트 | `uv run actionlint` | 0 | 구문 오류 0건 정상 통과 |
| 도커 shellcheck 린트 | `docker run --rm -v "/Users/kwanbum/orca/workspaces/refac_bid_box/wave-v-v3-pin:/repo" -w /repo rhysd/actionlint:latest` | 0 | 셸 스크립트 및 워크플로 0건 정상 통과 |
| 다이제스트 도커 린트 | `docker run --rm -v "/Users/kwanbum/orca/workspaces/refac_bid_box/wave-v-v3-pin:/repo" -w /repo rhysd/actionlint@sha256:b1934ee5f1c509618f2508e6eb47ee0d3520686341fec936f3b79331f9315667` | 0 | 불변 다이제스트 검증 0건 통과 |
| 에이전트 규칙 검증 | `python3 scripts/validate_agent_rules.py --quiet` | 0 | 검증 통과: 20/20 건 |
| 타입 체크 (mypy) | `uv run mypy src` | 0 | `Success: no issues found in 47 source files` |
| 문서 링크 검증 | `python3 scripts/validate_doc_links.py --quiet` | 0 | 문서 링크 정합성 통과 |
