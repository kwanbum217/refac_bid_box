# Orca 정본 스킬 영수증 2층 방어 게이트 구축 보고서 (Task task_4d362235e897)

> **작성일**: 2026-09-02
> **작업 식별자**: `task_4d362235e897`
> **역할**: `builder`
> **상태**: 완료
> **상위 규약**: [`AGENTS.md`](../../AGENTS.md), [`docs/ops/orca_control_plane_tools.md`](../ops/orca_control_plane_tools.md)

---

## 1. 개요 및 목적

코디네이터가 Orca 정본 스킬(`orca skills get orchestration`)을 확인하지 않고 워커를 띄우거나 낡은 지침을 기준으로 저수준 경로를 수동 조립하는 실패를 기계적으로 차단하기 위해 **2층 방어 체계(2-Layer Defense Gate)**를 구축했습니다.

- **1층 (자동 주입 및 발급)**: 코디네이터 세션 시작 시 `.claude/settings.json` 의 `SessionStart` 훅을 통해 `python3 scripts/orca_skill_receipt.py issue || true` 를 자동 실행합니다. 런타임 미가동 시에도 세션 진입을 방해하지 않는 fail-open 정책을 적용합니다.
- **2층 (엄격 검증 게이트)**: `scripts/orca_taskctl.py` 의 `create` 및 `dispatch` 서브커맨드 실행 시, 발급된 정본 영수증의 유효성(`sha256`, `appVersion`, 코디네이터 세션 핸들)을 실시간 런타임 상태와 대조하여 불일치 시 종료 코드 `4`로 즉시 거부하는 fail-closed 정책을 적용합니다.

---

## 2. 세부 구현 내역

### 2.1 1층: 세션 시작 자동 주입 및 발급 결합 (`.claude/settings.json`)

기존 `enabledPlugins` 구성을 완전하게 보존하면서 `SessionStart` 훅을 추가했습니다.

- **설정 파일**: [`.claude/settings.json`](../../.claude/settings.json)
- **훅 명령**: `python3 scripts/orca_skill_receipt.py issue || true`
- **주입 및 발급 결합 아키텍처**:
  - `issue` 명령은 단순히 영수증만 생성하지 않고, **정본 스킬(`orca skills get orchestration`) 본문 전체(~400줄)를 `sys.stdout`으로 직접 출력**합니다.
  - 이를 통해 Claude 세션 진입 시 훅 출력이 에이전트의 문맥에 온전히 주입되어 정본 스킬을 자연스럽게 읽게 됩니다.
  - **본문 전체 주입 근거**: 정본 문서는 약 400줄 수준으로 컴팩트하여 세션 문맥 예산 내에서 무리 없이 수용 가능하며, 요약에 따른 지침 왜곡이나 누락 위험을 원천 차단하기 위해 원문 그대로 주입합니다.
  - **실패 시 격리**: 정본 조회가 실패하거나 출력이 불가능한 경우 영수증을 발급하지 않으며, 기존 잔류 영수증이 있다면 즉시 삭제하여 불완전한 상태에서의 2층 게이트 통과를 방지합니다. 훅의 `|| true`는 세션 기동 중단을 방지합니다.

### 2.2 2층: 정본 영수증 발급 및 검증기 (`scripts/orca_skill_receipt.py`)

- **스크립트 경로**: [`scripts/orca_skill_receipt.py`](../../scripts/orca_skill_receipt.py)
- **영수증 파일 위치**: `.orca/skill_receipt.json` (Git 미추적)
- **영수증 스키마 (`ORCA_SKILL_RECEIPT_V1`)**:
  - `canonical_command`: `orca skills get orchestration`
  - `sha256`: 발급 시점의 정본 스킬 본문 SHA-256 해시
  - `app_version`: 발급 시점의 Orca 런타임 버전 (`result.runtime.appVersion`)
  - `coordinator_handle`: 발급 코디네이터 터미널 핸들 (`ORCA_TERMINAL_HANDLE` 또는 `run-current`)
  - `issued_at`, `issued_at_iso`, `content_bytes`, `content_chars`
- **검증 규칙 (fail-closed)**:
  - 영수증 부재 또는 JSON 파싱 오류 시 거부.
  - 실시간 `orca status --json` 의 `appVersion` 과 영수증의 `app_version` 불일치 시 거부.
  - 실시간 `orca skills get orchestration` 의 SHA-256 해시와 영수증의 `sha256` 불일치 시 거부.
  - 영수증의 `coordinator_handle` 과 현재 세션 핸들 불일치 시 타 세션 영수증 재사용으로 간주하여 거부 (핸들 조회 불가 환경에서는 해당 검사만 건너뜀).
- **단일 해소 명령**: 모든 거부 상황에서 `python3 scripts/orca_skill_receipt.py issue` 를 출력.

### 2.3 제어 평면 게이트 통합 (`scripts/orca_taskctl.py`)

- **적용 서브커맨드**: `create`, `dispatch` (워커 기동과 관련된 두 명령에만 엄격 적용, `expand`, `finalize`, `status` 등은 제외).
- **종료 코드 규약**:
  - `0`: 성공
  - `1`: 워커 기동 실패, 동시 쓰기 상한 초과, 잔류 세션
  - `2`: 인자/계약 오류
  - `3`: 지시 도달 미확인
  - **`4`**: 정본 스킬 영수증 부재/불일치/만료로 거부
- **단일 우회 플래그**: `--skip-skill-receipt` (사용 시 stderr 경고 및 Dispatch 기록 보존, 환경변수 우회 없음).

### 2.4 규칙 검증기 포인터 정합성 검사 (`scripts/validate_agent_rules.py`)

- **추가 검사**: `check_canonical_skill_pointer`
- **검증 내용**: `.agents/skills/orca-section-coordination/SKILL.md` 0장에 정본 명령 `orca skills get orchestration` 이 정확히 명시되어 있는지 기계적으로 검증.
- **총 검사 수**: 17건에서 18건으로 확장 (`검증 통과: 18/18 건`).

### 2.5 테스트 격리 및 재작업 해소 (`tests/test_orca_taskctl.py`)

- **반려 원인 분석**: `tests/test_orca_taskctl.py` 내의 기존 dispatch/create 테스트들은 영수증 게이트 도입 이전에 작성되어, 게이트가 실제 디렉터리의 `.orca/skill_receipt.json`을 읽게 되어 영수증 부재 또는 세션 핸들 불일치 시 exit code 4로 46건이 실패하는 현상이 발생했습니다.
- **해소 구현**: 기존 `mock_settled_session_audit_default`와 동일한 아키텍처로 `mock_skill_receipt_default` (autouse=True) fixture를 추가하여 테스트 환경을 저장소 상태로부터 완전 격리했습니다.
- **게이트 동작 고정**: 게이트의 실제 fail-closed 거부 및 `--skip-skill-receipt` 우회 동작을 검증하는 3개 테스트(`test_cmd_dispatch_refuses_when_skill_receipt_invalid`, `test_cmd_dispatch_bypasses_skill_receipt_with_flag`, `test_cmd_create_refuses_when_skill_receipt_invalid`)를 `tests/test_orca_taskctl.py`에 추가하여 회귀를 방지했습니다.

---

## 3. 검증 결과

### 3.1 규칙 정합성 검증 (`scripts/validate_agent_rules.py`)

- **실행 명령**: `python3 scripts/validate_agent_rules.py --quiet`
- **종료 코드**: `0`
- **출력 결과 요약**:
  ```text
  ============================================================
  다중 에이전트 규칙 정합성 검증 (pre-commit / v2)
  ============================================================
  [PASS] CLAUDE.md thin pointer
  [PASS] .antigravity/rules.md
  [PASS] .cursor core rule AGENTS.md 참조
  [PASS] opencode.json instructions v2 단일 주입
  [PASS] 스킬 미러 정합성
  [PASS] AGENTS.md 단일 진실 원천 (@SKILLS.md 미참조)
  [PASS] Task Capsule v2 규약 문서
  [PASS] Task Capsule v2 템플릿 정합성
  [PASS] orca-section-coordination v2 스킬
  [PASS] Orca 정본 스킬 포인터 정합성
  [PASS] CURRENT_STATE 정본 존재
  [PASS] CURRENT_STATE 필수 필드
  [WARN] 컨텍스트 예산
  [PASS] 워커 모델 배정표 정합성 (TIER_POLICY vs 문서)
  [PASS] AGENTS.md 워커 모델 배정표 부재
  [PASS] CURRENT_STATE 기계 상태 원장 정합성
  [PASS] CURRENT_STATE 6.1 상태 모순 검사
  [PASS] 분석 문서 수치 정합성 (METRICS 마커)
  ------------------------------------------------------------
  검증 통과: 18/18 건.
  ```

### 3.2 단위 테스트 검증 (`tests/test_orca_skill_receipt.py`)

- **실행 명령**: `uv run pytest tests/test_orca_skill_receipt.py -v`
- **종료 코드**: `0`
- **결과**: `24 passed, 1 warning in 0.49s` (정본 본문 stdout 주입 검증, 실패 시 영수증 삭제 검증, CLI 테스트 등 포함).

### 3.3 제어 평면 단위 테스트 (`tests/test_orca_taskctl.py`)

- **실행 명령**: `uv run pytest tests/test_orca_taskctl.py -q` (.orca/skill_receipt.json 미존재 상태)
- **종료 코드**: `0`
- **결과**: `192 passed, 1 warning in 1.48s`.

### 3.4 규칙 검증기 단위 테스트 (`tests/test_validate_agent_rules.py`)

- **실행 명령**: `uv run pytest tests/test_validate_agent_rules.py -v`
- **종료 코드**: `0`
- **결과**: `39 passed, 1 warning in 0.70s`.

### 3.5 전체 테스트 스위트 회귀 검증 (`pytest`)

- **실행 명령**: `rm -f .orca/skill_receipt.json && uv run pytest tests/ -q -m 'not data_assets'`
- **종료 코드**: `0`
- **결과 요약**: `3237 passed, 35 skipped, 3 deselected, 311 warnings in 65.53s`
- **격리 워크트리 특이사항**: 격리 작업 트리 환경 특성상 `data/model_files` 및 `chroma_db` 미존재로 인한 `tests/test_data_preservation.py` 내 `test_model_bin_files_exist`, `test_chroma_db_exists` 2건은 사전 정의된 환경 차이로 정상 통과 대상에서 제외되었습니다.

---

## 4. 변경 파일 목록

| 파일 경로 | 변경 유형 | 핵심 변경 내용 |
| --- | --- | --- |
| [`.claude/settings.json`](../../.claude/settings.json) | 수정 | `SessionStart` 훅 추가 (`orca_skill_receipt.py issue \|\| true`) |
| [`scripts/orca_skill_receipt.py`](../../scripts/orca_skill_receipt.py) | 생성 | 정본 스킬 본문 stdout 주입과 영수증 발급 원자적 결합, 실시간 대조 검증(`verify`) |
| [`scripts/orca_taskctl.py`](../../scripts/orca_taskctl.py) | 수정 | `create` / `dispatch` 게이트 부착, 종료 코드 4, `--skip-skill-receipt` 추가 |
| [`scripts/validate_agent_rules.py`](../../scripts/validate_agent_rules.py) | 수정 | `check_canonical_skill_pointer` 검사 추가 (18건) |
| [`tests/test_validate_agent_rules.py`](../../tests/test_validate_agent_rules.py) | 수정 | 18건 단정 갱신 및 `test_check_canonical_skill_pointer` 테스트 추가 |
| [`tests/test_orca_skill_receipt.py`](../../tests/test_orca_skill_receipt.py) | 생성 | 정본 영수증 발급·본문 방출·실패 삭제·게이트 차단 단위 테스트 24건 |
| [`tests/test_orca_taskctl.py`](../../tests/test_orca_taskctl.py) | 수정 | 테스트 격리 fixture 추가 및 영수증 거부/우회 테스트 3건 추가 (192건 통과) |
| [`docs/ops/orca_control_plane_tools.md`](../ops/orca_control_plane_tools.md) | 수정 | 영수증 도구 인터페이스, 2층 방어 체계, 종료 코드 4 규약 문서화 |
| [`docs/analysis/task_4d362235e897.md`](task_4d362235e897.md) | 생성 | 본 분석 및 이행 완료 보고서 |
