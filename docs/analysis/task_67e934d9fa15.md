# CSRF 토큰 검증 도입 보고

## 적용 범위

SSR 브라우저 폼인 `/accounts/login/`, `/accounts/signup/`, `/accounts/logout/` 화면에 서명 토큰을 발급하고, 세 상태 변경 POST를 모두 서버에서 검증합니다. 검증 플래그가 켜져 있을 때 토큰 쿠키가 없거나 폼 토큰이 없으면 거부하는 fail-closed 정책으로, 이후 세션 쿠키 정책이 완화되어도 CSRF 방어가 유지됩니다.

브라우저 폼이 없는 순수 JSON API(`/api/v1/accounts/*`, 챗봇, 예측, 자동화, 입찰 수집)는 기존 API 소비자를 깨뜨리지 않도록 CSRF 강제 대상에서 제외했습니다. 워커가 `X-BIDBOX-CALLBACK-TOKEN`으로 인증하는 `/api/v1/automation/job/{job_id}/callback`도 기계 간 인증 경로이므로 명시적으로 제외했습니다.

## 구현

토큰은 `settings.SECRET_KEY`와 전용 salt를 결합한 SHA-256 HMAC 및 타임스탬프 형식으로 발급하고, 쿠키와 폼 값을 상수 시간 비교 후 서명·만료를 검증합니다. `CSRF_PROTECTION_ENABLED`는 기본값 `True`이며 비활성화 시 SSR 검증을 건너뜁니다. 세션 쿠키 속성과 CORS 설정은 변경하지 않았습니다.

토큰 쿠키 없이 직접 POST하는 기존 소비자는 fail-closed 계약에 따라 403을 받습니다. 브라우저 사용 흐름은 먼저 GET 폼 화면을 방문해 쿠키와 hidden token을 발급받으므로 영향을 받지 않으며, 토큰을 발급받지 않는 소비자는 명시적으로 토큰 발급·제출을 추가해야 합니다.

## 검증

- `uv run pytest tests/test_csrf.py tests/test_ui_ssr.py tests/test_ssr_session_store.py tests/test_ssr_auth_offload.py tests/test_signup_form_parity.py tests/test_auth_hardening.py -q`: 100 passed
- `tests/test_ui_ssr.py`, `tests/test_ssr_session_store.py`, `tests/test_ssr_auth_offload.py`, `tests/test_signup_form_parity.py`, `tests/test_auth_hardening.py`는 실제 브라우저 흐름처럼 GET으로 토큰을 발급받은 뒤 POST하도록 공통 헬퍼를 사용해 보정했습니다. 기존 업무 단언은 유지했습니다.
- `uv run mypy src/`: Success
- `uv run pytest tests/ -q -m 'not data_assets'`: 3193 passed, 30 skipped, 3 deselected
