# LLM 품질 실측: gemma4:e4b 대 gemma4:e2b (2026-08-24)

> **작성일**: 2026-08-24 (Asia/Seoul)
> **observed_commit**: `228b4b3`
> **status**: current
> **fixture**: `data/eval/llm_quality_fixture_v1.json` (19문항, 컨텍스트 충족 16, KB 근거 17/17 실재)
> **하네스**: `scripts/measure_llm_quality.py` (운영 RAG 경로 `/api/v1/chatbot/query`)
> **판정**: **승격 보류.** 속도 이득은 크고 실재하나, 이번 지표가 생성 품질을 변별하지 못함

---

## 1. 측정 조건

| 항목 | 값 |
| --- | --- |
| 문항 수 | 19 (컨텍스트 충족 16, 거절 기대 3) |
| 반복 | 모델당 문항별 3회 (총 57회) |
| 요청 실패 | 두 모델 모두 0건 |
| 서빙 모델 결박 | 측정 시작·종료 양쪽에서 컨테이너 `OLLAMA_MODEL` 확인, 두 측정 모두 `consistent: true` |
| 임베딩 | `bge-m3` (두 측정 동일) |

---

## 2. 결과

| 지표 | e4b | e2b | 차이 |
| --- | ---: | ---: | ---: |
| 지연 P50 | 2,794ms | **1,568ms** | **-43.9%** |
| 지연 P95 | 7,825ms | **3,312ms** | **-57.7%** |
| numeric 팩트 일치 | 24/45 | 24/45 | 동일 |
| 근거 recall 평균 | 0.500 | 0.500 | 동일 |
| recall = 1.0 | 24/48 | 24/48 | 동일 |
| `must_not_claim` 위반 | 0 | 0 | 동일 |
| 인용 표기 | 46/48 | 47/48 | +1 |

---

## 3. 이번 지표는 생성 품질을 변별하지 못한다

품질 지표가 **소수점까지 동일**하다. 우연이 아니라 구조적인 이유가 있다.

- **근거 recall 은 임베딩이 결정한다.** 검색은 `bge-m3` 로 수행되고 생성 모델과 무관하다.
  문항별 recall 을 대조한 결과 **두 모델에서 다른 문항이 하나도 없다**.
- **numeric 팩트 일치는 recall 을 따라간다.** 근거 문서가 검색되면 두 모델 모두 그 안의
  수치를 그대로 인용하고, 검색되지 않으면 둘 다 답하지 못한다.

즉 이번 자동 채점 축(numeric, recall, citation)은 **검색 성능의 함수**이며 생성 모델의
능력을 거의 반영하지 않는다. `must_not_claim` 위반 0건도 두 모델이 검색된 문맥에
충실했다는 뜻일 뿐, 어느 쪽이 더 나은 답을 쓰는지는 말해주지 않는다.

**생성 품질을 실제로 가르는 축은 proposition 팩트인데, 문자열 일치로 채점할 수 없어
자동 채점에서 제외했다.** 응답 전문은 raw 에 보존되어 있으므로 별도 판정자가 rubric 으로
채점해야 한다.

---

## 4. 함께 드러난 검색 결함

**컨텍스트 충족으로 설계한 문항의 절반이 기대 근거를 검색하지 못한다.**

recall < 1.0 문항: `q02`, `q03`, `q04`, `q07`, `q08`, `q11`, `q13`, `q14` (8/16)

fixture 는 KB 에 실재하는 문서로 근거를 결박했으므로 문서는 분명히 있다. 그런데 질문을
그대로 임베딩해 검색하면 상위 5건 안에 들어오지 않는다. 이것은 모델 승격과 별개의
**RAG 검색 품질 문제**이며, 승격 판정보다 우선순위가 높을 수 있다.

---

## 5. 판정

**승격하지 않는다.** 근거는 다음과 같다.

1. 속도 이득(P50 -43.9%, P95 -57.7%)은 크고 재현 가능하며 실재한다.
2. 그러나 "품질 저하 없음" 은 **측정하지 못한 축에 대한 진술이 아니다.** 이번 지표는
   검색이 지배하므로 두 모델이 같게 나오는 것이 당연하고, 생성 품질 비교로 읽으면 안 된다.
3. 검색 recall 이 0.5 인 상태에서 생성 모델을 바꾸면, 승격 효과와 검색 결함이 뒤섞여
   이후 회귀 판정이 어려워진다.

**다음 단계는 검색 결함 해소가 먼저다.** recall 을 올린 뒤 proposition 채점을 붙여
재측정해야 생성 품질 비교가 성립한다.

---

## 6. 산출물

| 파일 | 내용 |
| --- | --- |
| `data/benchmarks/llm_quality_e4b_20260824.json` | e4b 57회 raw |
| `data/benchmarks/llm_quality_e2b_20260824.json` | e2b 57회 raw |

두 raw 모두 응답 전문을 포함하므로 proposition 채점을 나중에 소급 적용할 수 있다.
