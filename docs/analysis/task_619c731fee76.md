# CURRENT_STATE 정규화 작업 기록

> Task: `task_619c731fee76`
> 기준일: 2026-09-03

## 변경 결과

`CURRENT_STATE.md`를 부팅용 요약과 기계 검증 사실 원장 중심으로 정규화했습니다. 판정에 쓰이는 게이트, CI, 버전, 활성 플래그, 성능 판정, 잔여 위험을 `current_state_facts.yaml`의 33개 사실로 확장했고, 각 사실의 주장·앵커·증거 경로를 검증합니다. 기존 상세 측정 로그와 과거 경위는 `current_state_history.md`에 전체 보존했습니다.

| 항목 | 정규화 전 | 정규화 후 |
| --- | ---: | ---: |
| `docs/context/CURRENT_STATE.md` 바이트 | 50,692 | 7,930 |
| `docs/context/CURRENT_STATE.md` 문자 | 측정 전 원본 기준 | 5,013 |
| 기계 원장 최상위 사실 | 4개 | 33개 |

현재 문서는 12,000바이트·8,000자 목표 안에 있으며, 판정 문서는 원장과 서술이 다르면 실패합니다. `source_commit` 메타데이터 헤더의 구조와 4행 위치는 유지했습니다.

## 검증

| 명령 | exit_code | 실제 요약 |
| --- | ---: | --- |
| `uv run pytest tests/test_validate_agent_rules.py -q` | 0 | `43 passed in 0.70s` |
| `uv run pytest tests/ -q -m 'not data_assets'` | 0 | `3392 passed, 40 skipped, 3 deselected in 94.94s (0:01:34)` |
| `python3 scripts/validate_agent_rules.py --quiet` | 0 | `검증 통과: 20/20 건.` |

기존 19개 검사에 판정 사실 원장 검사를 추가해 총 20개가 되었습니다. 원장 검사는 claim·document_anchor·evidence를 확인하며, 기존 상태 표지·6.1절 모순 검사는 유지했습니다.

## 범위 밖 알려진 자산 상태

격리 워크트리에는 `data/model_files`와 `chroma_db`가 없습니다. 따라서 `tests/test_data_preservation.py`의 `test_model_bin_files_exist`와 `test_chroma_db_exists`는 자산 부재로 실패할 수 있으며, 본 문서 정규화 결함이 아닙니다. 지정 검증 명령은 `data_assets`를 제외해 이 환경의 알려진 자산 부재를 분리했습니다.

## 보존 및 후속

- `docs/context/current_state_history.md`는 정규화 전 `CURRENT_STATE.md` 전체 본문을 보존합니다.
- `docs/handoff/`와 기존 `docs/analysis/` 문서는 수정하지 않았습니다. 이 작업 기록만 새로 추가했습니다.
- 코디네이터는 병합 시 `source_commit`을 현재 정본 커밋으로 갱신하고, 병합 후 위 검증을 다시 실행해야 합니다.
