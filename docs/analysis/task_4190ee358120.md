# task_4190ee358120 작업 분석

> **task_id**: `task_4190ee358120`
> **run_id**: `run_0e78bf666e8c`
> **role**: investigator
> **mode**: worker
> **대상**: 9 개 미병합 워커 베이크오프 브랜치 ( b2- 5 + bakeoff- 4 )
> **산출물**: [`docs/ops/bakeoff_branches_verdict_20260826.md`](../ops/bakeoff_branches_verdict_20260826.md)

## 1. 작업 정의

코디네이터는 같은 과제를 여러 모델에 태운 베이크오프 산출물 9 개 ( `b2-` 5 + `bakeoff-` 4 ) 가 미병합 상태로 남아 있음을 확인하고, 회수할 구현이 있는지 혹은 전량 폐기해도 되는지를 판정하는 보고서를 요청했습니다. 사양은 단일 정본인 capsule.yaml 에 한정되어 있습니다.

## 2. 데이터 수집 절차

| 단계 | 명령 | 결과 |
| --- | --- | --- |
| 1 | `git branch -a \| grep -E "(b2-\|bakeoff-)"` | 9 개 브랜치 모두 로컬에 존재 확인 |
| 2 | `git log --oneline main..<branch>` ( 각 9 회 ) | 모든 브랜치의 마지막 커밋이 2026-08-20 으로 일치 ( capsule 사양과 일치 ) |
| 3 | `git diff main..<branch> --stat` ( 각 9 회 ) | 9 개 모두 광범위한 파일 변경 포함 ( 베이스가 다른 과거 시점임을 시사 ) |
| 4 | `git diff main..<branch> -- scripts/audit_model_inventory.py` ( 각 9 회 ) | 핵심 과제 파일 diff 추출 |
| 5 | `git diff main..<branch> --name-only` 필터링 | `scripts/audit_model_inventory.py` 외 다른 변경은 부수적 ( 공통 베이스 회귀 ) |
| 6 | `grep` ( main 기준 보유 기능 확인 ) | main 의 `--state` / `--reset-state` / `counter` 추적 존재 확인 |

## 3. 핵심 발견

### 3.1 main 의 현재 상태 ( `scripts/audit_model_inventory.py` )

- `source_commit = 918881e`
- `--state`, `--reset-state`, `--with-agy` 옵션 보유
- `_update_history()` 가 `counter` 키로 연속 absent 를 누적
- 종료 코드 0/1/2 의미 정의 ( 0 확인/의심, 1 소멸, 2 도구 오류 )
- **`--json` 옵션 없음** ( b2- 5 개가 추가하려는 대상 )
- **`absent_streak` / `consecutive_absent` 같은 명시적 키 없음** ( bakeoff- 4 개가 추가하려는 대상 )
- **`observations` 이력 누적 없음** ( 일부 bakeoff- 가 추가하려는 대상 )

### 3.2 b2- 계열 ( `--json` 출력 옵션 )

5 개 모두 `audit_model_inventory.py` 에 `--json` 옵션을 추가하는 같은 과제입니다. 차이는 구현 분리도와 시그니처 변경 여부에 국한됩니다.

- **회수 후보**: `b2-or_nemoultra` ( `_audit_with_json()` helper + `audit()` 시그니처 유지, main 호환성 최고 )
- **폐기 권고**: 나머지 4 개 ( b2-deepseek, b2-laguna_xs, b2-mimo, b2-oc_nemo3ultra )

### 3.3 bakeoff- 계열 ( 연속 미관측 기반 관측 이력 판정 )

4 개 모두 main 의 `_update_history` ( `counter` 누적 ) 를 더 명시적인 키·분리 함수로 재구성하는 과제입니다.

- **회수 후보**: `bakeoff-mimo` ( `audit_with_state()` 명시적 분리, `audit()` 시그니처 main 과 동일 )
- **폐기 권고**: 나머지 3 개 ( bakeoff-deepseek, bakeoff-nemotron_ultra, bakeoff-oc_nemotron_ultra )

## 4. 판정 매트릭스 ( 요약 )

| 브랜치 | 과제 카테고리 | main 호환 | 추가 가치 | 판정 |
| --- | --- | :---: | --- | :---: |
| kwanbum217/b2-deepseek | `--json` | 호환 | 낮음 | 폐기 |
| kwanbum217/b2-laguna_xs | `--json` | 비호환 | 낮음 | 폐기 |
| kwanbum217/b2-mimo | `--json` | 호환 | 낮음 | 폐기 |
| kwanbum217/b2-oc_nemo3ultra | `--json` | 비호환 | 낮음 | 폐기 |
| kwanbum217/b2-or_nemoultra | `--json` | 호환 | 중간 | **회수** |
| kwanbum217/bakeoff-deepseek | 연속 미관측 | 비호환 | 낮음 | 폐기 |
| kwanbum217/bakeoff-mimo | 연속 미관측 | 호환 | 중간 | **회수** |
| kwanbum217/bakeoff-nemotron_ultra | 연속 미관측 | 비호환 | 낮음 | 폐기 |
| kwanbum217/bakeoff-oc_nemotron_ultra | 연속 미관측 | 비호환 | 낮음 | 폐기 |

## 5. 회수 시 유의 사항 ( 다음 Task 참고 )

1. 두 회수 후보는 **서로 다른 파일을 건드리지 않으며**, 같은 `scripts/audit_model_inventory.py` 를 다룬다. 동시에 회수하지 말고 별도 브랜치/작업 트리에서 한 번에 하나씩 진행한다.
2. `b2-or_nemoultra` 회수 시 `pools` 키의 `streak` 이 main 의 `counter` 와 의미가 일치하는지 회귀 테스트로 확인한다.
3. `bakeoff-mimo` 회수 시 `--state` 미지정 경로의 동작이 main 과 의미적으로 동일한지 ( 카운터 추적 OFF ) 합의한다. main 의 기본은 `STATUS_PATH` 에 기록되므로, 두 동작이 같은지·다른지 명시적 정책이 필요하다.
4. 두 회수 모두 `tests/test_audit_model_inventory.py` 가 존재하면 새 동작에 대한 회귀 테스트가 함께 들어가야 한다.
5. 회수·폐기 모두 본 Task 범위가 아니다. 본 보고서는 판정만 한다. 브랜치 삭제·병합은 코디네이터가 별도 Task 로 분리한다.

## 6. 메타데이터

| 항목 | 값 |
| --- | --- |
| 작성 시각 | 2026-08-26 (KST) |
| source_commit ( main ) | `918881e` |
| 베이크오프 브랜치 base 추정 | `~70a4ec4` (CURRENT_STATE.md 가 인용한 source_commit, 베이크오프 마지막 커밋 2026-08-20 과 정합 ) |
| 보고서 경로 | `docs/ops/bakeoff_branches_verdict_20260826.md` |
| 본 분석 경로 | `docs/analysis/task_4190ee358120.md` |
| INSTR.txt 처리 | 커밋하지 않음 ( 정본 사양 준수 ) |
