# Task task_9afe32007e75 분석 및 구현 보고서

> **작성일**: 2026-08-30
> **Task ID**: `task_9afe32007e75` (Dispatch: `ctx_53e40a7ddc66`)
> **역할**: `builder`
> **목적**: 정확 공고명 재순위 포함 관계 매칭 적용 및 Lexical 채널 조건부 벡터 바이패스 분리

---

## 1. 개요 및 배경

2026-08-30 LLM 품질 정본 측정에서 fixture v2 32문항 중 `q21`("2026년 조림지 풀베기사업 2차(동부지구)")만 numeric 0/2, evidence recall 0.0을 기록했습니다.
원인 조사(`docs/analysis/q21_retrieval_miss_20260830.md`) 결과:
- 기대 문서 `bid_10169448` 은 DB, ChromaDB, Meilisearch 모두에 정상 색인되어 있었음.
- ChromaDB 30건 후보 풀 내 9위(거리 0.3161)에 위치하고 있었으나, `_rerank_by_exact_title`이 자연어 전체 질의문과 공고명 간의 엄격 동치(`==`)를 비교하여 재순위 승격에 실패하고 `top_k=5` 절단선 밖으로 밀려 누락됨.
- 또한 Lexical(Meilisearch) 채널에서도 동일한 단순 동치 비교로 인해 정확 일치 채택이 작동하지 않았음.

본 Task에서는 자연어 질의에서도 정확 공고명이 매칭될 수 있도록 포함 관계(`title_key in query_key`) 재순위를 적용하고, 벡터 바이패스 안전 경계를 엄격 동치로 격리 분리했습니다.

---

## 2. 코드 수정 내용

### 2.1 `src/rag/vector_store.py`

1. **최소 제목 길이 하한 상수 선언**:
   - `RERANK_MIN_TITLE_LENGTH = 5`
   - 단어 수준의 짧은 문자열(예: '공사', '풀베기' 등)이 자연어 질의에 우연히 포함되어 발생하는 오탐 승격을 방지.
2. **`_rerank_by_exact_title` 포함 관계 매칭 적용**:
   - `title_key and len(title_key) >= RERANK_MIN_TITLE_LENGTH and title_key in query_key`
   - 복수 승격 시 기존 `_extract_doc_sort_key`(공고일시 최신순 등) 결정적 정렬 유지.
   - 미매칭 문서는 기존 distance 순서를 100% 보존.

### 2.2 `src/rag/engine.py`

1. **Lexical 채널 판정 분리 (Bypass vs Promotion)**:
   - **엄격 동치(`title_key == query_key`)**: `exact_lexical_matches`에 추가되어 ChromaDB 벡터 검색 자체를 생략(conditional vector bypass).
   - **포함 매칭(`title_key in query_key`)**: `lexical_containment_matches`에 추가되어 벡터 검색을 생략하지 않고 수행한 뒤, 반환된 벡터 결과의 상위로 결정적 승격 병합.
   - **분리 사유 주석 명시**: 대상 질의 급증 및 회귀 방지를 위해 벡터 검색 생략은 엄격 동치로만 제한.

---

## 3. 단위 테스트 검증 (`tests/test_rag_vector_store.py`)

총 6건의 단위 테스트를 작성하여 동작을 고정했습니다:

| 테스트 함수명 | 검증 내용 | 결과 |
| --- | --- | :---: |
| `test_rerank_q21_natural_language_query_promotes_target_doc_to_first` | q21 실제 질의에서 9위 정답 문서가 1위로 정상 승격 | PASS |
| `test_rerank_ignores_short_title_under_min_length_guard` | 길이 5 미만 짧은 제목('풀베기')은 승격 배제 (거리 순 유지) | PASS |
| `test_rerank_preserves_original_distance_order_when_no_match` | 미매칭 시 기존 후보 풀 distance 정렬 순서 보존 | PASS |
| `test_rerank_multiple_matches_sorted_deterministically` | 복수 포함 매칭 시 최신 공고일시 우선 결정적 정렬 | PASS |
| `test_engine_containment_match_does_not_bypass_vector_search` | Lexical 포함 매칭 시 ChromaDB 벡터 검색 미생략(호출 확인) | PASS |
| `test_engine_strict_equality_match_bypasses_vector_search` | Lexical 엄격 동치 시 ChromaDB 벡터 검색 정상 바이패스 | PASS |

---

## 4. 규약 및 비기능 요구사항 준수

- **G1 데이터 무손실**: DB 스키마 및 원본 데이터 변경 없음.
- **특징 단일화**: `src/ml/features.py` 단일 공급원 원칙 준수.
- **의존성 무변경**: `pyproject.toml` 변경 없음, 외부 패키지 추가 없음.
- **이모지 금지**: 코드, 주석, 커밋 메시지, 문서 내 이모지 일체 미사용.
- **측정 비실행**: fixture 32문항 측정 및 벤치마크 미실행 (코디네이터 직렬 측정 구역 존중).
