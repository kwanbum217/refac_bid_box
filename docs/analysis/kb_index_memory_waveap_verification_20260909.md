# Wave AP 재측정: KB 색인 메모리 폭주가 해소되었다

> **작성일**: 2026-09-09
> **작성자**: 코디네이터 (직접 실측)
> **대상 커밋**: `4e40bd1` (AP1·AP2·AP3·AP5 전량 병합본)
> **판정**: **통과**. AP4 판정 기준 3항 충족
> **선행 문서**: [`kb_index_memory_verification_20260909.md`](kb_index_memory_verification_20260909.md)

---

## 1. 결론

단일 `update_kb_task` 1회가 **959초에 정상 완료**했고 worker 컨테이너 피크는
**4,323MB** 입니다. 2026-09-09 오전의 10,762MB 소멸 곡선이 재현되지 않았습니다.

색인은 처음으로 끝까지 도달했습니다. `knowledge_base_status` 가 `failed` 에서
`ready` 로 바뀌었고 증분 판정이 실데이터에서 동작함이 확인됐습니다.

---

## 2. 판정 기준 대조

Capsule `task_0e45a3cedaad` 가 정한 기준 셋입니다.

| 기준 | 요구 | 실측 | 판정 |
| --- | --- | --- | :---: |
| 1 | 색인 중 worker 컨테이너가 소멸하지 않는다 | 표본 1,206건 전량 `UP`. 재시작 0회 | **통과** |
| 2 | 피크가 10,509MB 곡선을 재현하지 않는다 | 4,323MB. 오전 대비 **60% 감소** | **통과** |
| 3 | `source_bid_count` 와 임베딩 시각이 갱신된다 | `ready`, 500,000건, `2026-09-09 04:41:50` | **통과** |

---

## 3. 실측 곡선

측정은 `docker exec <컨테이너> cat /sys/fs/cgroup/memory.current` 1초 간격이며
원본은 [`kb_index_worker_mem_waveap_20260909.tsv`](../../data/benchmarks/kb_index_worker_mem_waveap_20260909.tsv)
입니다. 60초 대표값입니다.

| 시각 | worker | 구간 |
| --- | ---: | --- |
| 13:25:44 | 304.2MB | 유휴 |
| 13:25:50 | 305.5MB | **작업 투입** |
| 13:28:21 | 248.4MB | 준비 단계. 상승 없음 |
| 13:30:01 | 1,491.5MB | 임베딩 시작 |
| 13:32:09 | 2,626.3MB | **고원 진입** |
| 13:34:53 | 2,689.9MB | 평탄 유지 |
| 13:37:24 | 3,301.7MB | |
| **13:40:48** | **4,323.0MB** | **피크** |
| 13:41:50 | 3,627.9MB | **색인 완료 (959.63초)** |
| 13:45:16 | 3,630.5MB | 상주 안정 |

**오전 곡선과의 차이가 결정적입니다.** 오전에는 투입 2분 09초 만에 10,762MB 에
도달하고 컨테이너가 소멸했습니다. 이번에는 같은 2분 시점이 248MB 이고, 이후에도
2.6~2.7GB 고원을 12분간 유지하다 마지막에 4.3GB 로 한 번 올랐습니다. 상주량이
문서 수에 비례해 무한히 커지는 형태가 아니라 청크 단위로 유계인 형태입니다.

---

## 4. 색인 결과

```
최근 1년 데이터 기준 500000건 인덱싱 완료
(증분: 갱신 1751건 / 유지 498249건 / 삭제 40322건)
(source=announcements_by_notice_date)
```

증분 판정과 `unseen_ids.discard` 삭제 대상 계산이 **실데이터에서 처음으로
검증됐습니다.** 지금까지 색인이 끝난 적이 없어 이 경로는 단위 테스트로만
확인된 상태였습니다. 폴백도 공고일 기준(`announcements_by_notice_date`)으로
정상 동작했습니다.

### 4.1 문서 수 감소에 대한 판단

`source_bid_count` 가 종전 기록 535,702 에서 500,000 으로 줄고 40,322건이
삭제됐습니다. **이는 상한 정책의 결과이며 데이터 손실이 아닙니다.**

- `KB_MAX_DOCUMENTS` 기본값이 500,000 이고 환경변수 미설정입니다.
- 최근 1년 공고가 상한을 넘었고, 공고일 내림차순 정렬이라 오래된 40,322건이 제외됐습니다.
- 삭제 비율 7.5% 로 `MAX_REMOVAL_RATIO` 0.5 이내입니다.
- 컬렉션 표본 10만 건의 id 접두는 전량 `bid_` 이며 종류가 뒤섞이지 않았습니다.
- 종전 기록 535,702 는 `status=failed` 인 색인이 남긴 값이라 기준선으로 삼기에 부적절합니다.

원본 DB 와 모델 가중치는 건드리지 않으므로 G1 대상이 아닙니다. KB 는 파생물입니다.
커버리지를 늘리려면 `KB_MAX_DOCUMENTS` 를 올리는 별도 판단이 필요합니다.

---

## 5. 측정 조건

| 항목 | 값 |
| --- | --- |
| Docker Desktop 메모리 | 16,384MiB (오전 측정과 동일) |
| 투입 방식 | 단일 `update_kb_task`, job_id `kb-waveap-verify-20260909` |
| 사전 정리 | arq 큐 비움. 재시도 잔재 없음 |
| 겹친 작업 | `run_schedule_catchup_task` 가 재시작 직후 발화해 13:40:51 에 완료(966초) |

**catchup 이 겹친 상태에서도 통과했습니다.** 큐에서 제거했으나 이미 실행 중이었습니다.
무거운 작업 둘이 겹친 조건에서 피크 4,323MB 이므로, 단독 조건은 이보다 낮을 것으로
보입니다. 다만 그 수치는 측정하지 않았습니다.

`run_schedule_catchup_task` 는 `HEAVY_TASK_NAMES` 에 없어 AP5 의 단일 실행 잠금이
걸리지 않습니다. AP5 리뷰가 지적한 `nightly_schedule_task` 누락과 같은 계열이며
후속 과제로 남습니다.

---

## 6. 남은 것

| 항목 | 내용 |
| --- | --- |
| Docker Desktop 메모리 원복 | 16,384 -> 14,336 으로 되돌린 뒤 재확인 |
| `HEAVY_TASK_NAMES` 보강 | `run_schedule_catchup_task`, `nightly_schedule_task` 포함 검토 |
| 단독 조건 측정 | catchup 이 겹치지 않은 순수 단일 색인 피크 |
| KB 커버리지 | 상한 500,000 유지 여부 판단 |
