# mypy mixed override 해소 (task_0806b3051833)

> **작성일**: 2026-08-26
> **작업 ID**: `task_0806b3051833`
> **후속 remediation**: `task_3d7bb40a0d2e` (Any/cast 정리)
> **범위**: assignment, arg-type+assignment, operator+return-value, benchmark_arq_container, benchmark_provenance override 5블록 9모듈

---

## 1. override 제거·존치 결과 (remediation 후)

| 모듈 | override 제거 | 비고 |
| --- | --- | --- |
| `src.ml.model_registry` | 예 | `BaseModelWrapper`·`Protocol` 런타임 주입 |
| `src.app.services.kb_builder` | 예 | `KbSyncStats` TypedDict |
| `src.tasks.automation_tasks` | 예 | `Callable[..., object]`, `dict[str, object]` |
| `scripts.audit_model_inventory` | 예 | `prev_counter` int 정규화 |
| `scripts.benchmark_arq_container` | 예 | `_await_maybe`, `isinstance` 좁히기 |
| `scripts.benchmark_provenance` | 예 | `isinstance` 좁히기, MAD None 분기 |
| `scripts.eval_servc_prediction_interval` | 아니오 | LightGBM `**params` 스텁 한계 |
| `src.ml.trainer` | 아니오 | LightGBM `**params` 스텁 한계 |
| `src.rag.llm` | 아니오 | google-genai `contents` list/union 스텁 한계 |

**제거된 override 블록**: 4개 (model_registry, arg-type+assignment 묶음의 3모듈 제외, audit, benchmark_arq, benchmark_provenance)

**복원된 override 블록**: 1개 (`eval_servc_prediction_interval`, `trainer`, `llm` — `arg-type`, `assignment`)

---

## 2. 스텁 근거로 override 유지

| 모듈 | 오류 | 근거 |
| --- | --- | --- |
| `scripts.eval_servc_prediction_interval` | `LGBMRegressor(**dict)` arg-type | lightgbm 스텁이 `**dict[str, object]` 확장을 거부 |
| `src.ml.trainer` | 동일 | 동일 |
| `src.rag.llm` | `generate_content(contents=list[Content])` arg-type | google-genai 스텁이 `list[Content]`와 union 인자 불일치 |

구체화 시도: `Sequence[Content]`(llm), `dict[str, int|float|str]`(trainer) — mypy 통과 불가.

---

## 3. remediation에서 제거한 Any/cast

| 파일 | 제거·대체 |
| --- | --- |
| `model_registry.py` | `cast(Any, _wrappers)` → `Protocol` + `cast(_WrappersRuntimeBinding, ...)` |
| `benchmark_arq_container.py` | `cast(Awaitable[Any], ...)` → `_await_maybe`; load_sample `isinstance` |
| `benchmark_provenance.py` | `cast(float, ...)` → `isinstance` |
| `trainer.py` | `dict[str, Any]` annotation → `dict[str, object]` 또는 제거 |
| `automation_tasks.py` | `cast(Callable[..., Any], ...)` → `STEP_RUNNERS` 타입; `kwargs: dict[str, object]` |
| `eval_servc_prediction_interval.py` | `dict[str, Any]` annotation 제거 |

---

## 4. G1 / train-serve 영향

DB·`features.py`·런타임 동작 변경 없음. KB delta `ValueError`는 `delta_mode`에서 `collected_since`가 None일 때만(논리상 불가) 발생.

---

## 5. 검증

| 명령 | 결과 |
| --- | --- |
| `uv run mypy src/ scripts/` | 통과 |
| `python3 -m compileall -q src scripts tests` | 통과 |
| 관련 pytest (kb_builder, trainer, model_registry, benchmark, automation, audit) | 134 passed |
