# mypy mixed override 해소 (task_0806b3051833)

> **작성일**: 2026-08-26
> **작업 ID**: `task_0806b3051833`
> **범위**: assignment, arg-type+assignment, operator+return-value, benchmark_arq_container, benchmark_provenance override 5블록 9모듈

---

## 1. 해소 결과

| 모듈 | 이전 override | 조치 | override 제거 |
| --- | --- | --- | --- |
| `src.ml.model_registry` | assignment | `BaseModelWrapper` 좁은 할당, `setattr` 런타임 주입 | 예 |
| `scripts.eval_servc_prediction_interval` | arg-type, assignment | `QUANTILE_PARAMS: dict[str, Any]`, `np.asarray(predict)` | 예 |
| `src.app.services.kb_builder` | arg-type, assignment | delta `collected_since` None 검사, `_sync` 반환 `dict[str, Any]` | 예 |
| `src.ml.trainer` | arg-type, assignment | `np.asarray` 반환, `params: dict[str, Any]`, `aggregated: dict[str, Any]` | 예 |
| `src.rag.llm` | arg-type, assignment | Gemini `contents` cast, `build_backend` 분기 변수 분리 | 예 |
| `src.tasks.automation_tasks` | arg-type, assignment | `Callable` cast, `kwargs: dict[str, Any]`, `to_thread` 명시 키워드 | 예 |
| `scripts.audit_model_inventory` | operator, return-value | `prev_counter` int 정규화 | 예 |
| `scripts.benchmark_arq_container` | arg-type, call-overload, misc, unused-ignore | `blpop([key])`, `Awaitable` cast, `load_sample` cast | 예 |
| `scripts.benchmark_provenance` | arg-type, dict-item, operator | `cast(float, ...)`, `dict[str, float | None]`, MAD None 분기 | 예 |

5개 override 블록 전부 삭제. `pyproject.toml`의 다른 override 블록은 변경하지 않았습니다.

---

## 2. G1 / train-serve 영향

| 항목 | 판정 |
| --- | --- |
| DB 스키마·행 수 | 변경 없음 |
| `src/ml/features.py` | 미수정 |
| 모델 레지스트리 런타임 동작 | wrapper 타입 주석·`setattr` 주입만; 로드·예측 경로 동일 |
| ML 학습 파라미터 | `LGB_BASE_PARAMS` 값 불변; 타입 어노테이션만 |
| KB delta 모드 | `collected_since is None` 시 기존과 동일하게 `delta_mode=False`; None이면 `ValueError`로 조기 실패(이전에는 타입만 모호) |

---

## 3. 검증

| 명령 | 결과 |
| --- | --- |
| `uv run mypy src/ scripts/` | 통과 (198 files) |
| `uv run pytest tests/ -q -m 'not data_assets'` | 2197 passed; `test_validate_agent_rules` 2건 실패 (CURRENT_STATE `source_commit` 지연, 본 Task 범위 밖) |
| `python3 scripts/validate_agent_rules.py --quiet` | 11/12 통과; CURRENT_STATE `source_commit` 6커밋 지연 (허용 5) |

CURRENT_STATE 갱신은 `allowed_write_files` 밖이므로 코디네이터 후속 조치가 필요합니다.
