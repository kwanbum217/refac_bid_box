# Task task_801947f162b6 분석 및 이행 보고서: SuperGrok 로컬 grok CLI 등재 및 리뷰어 경로 연동

> **Task ID**: `task_801947f162b6`
> **Run ID**: `run_971584ddb4a0`
> **역할**: `builder`
> **작성일**: 2026-09-04
> **정본 사양**: `.orca/capsules/task_grok_pool_registration/capsule.yaml`

---

## 1. 개요 및 목적

SuperGrok 구독의 로컬 Grok CLI(`/opt/homebrew/bin/grok`)를 Orca 모델 풀(`scripts/orca_model_router.py`), 독립 리뷰어 실행 경로(`scripts/orca_run_reviewer.py`), 그리고 워커 제어 도구(`scripts/orca_taskctl.py`)에 등재하여 워커 및 독립 리뷰어로 명시 배정할 수 있도록 제어 평면을 갱신했습니다.

2026-09-04 기준 리뷰어 공백(기존 자동 라우팅 리뷰어의 주간 토큰 소진, Antigravity CLI 인쇄 모드 오류 등) 상황에서, 빌더인 Gemini와 패밀리 계열이 완전히 분리되는 Grok을 독립 리뷰어 및 수동 보조 워커로 가동할 수 있는 안정적인 실행 경로를 확보하는 것이 이번 작업의 핵심입니다.

---

## 2. Grok CLI 환경 및 실측 정본

- **실행 파일**: `/opt/homebrew/bin/grok` (grok 1.0.13 (5e9a58528b76) [stable], Homebrew 설치)
- **계정 상태**: SuperGrok 구독, grok.com 계정 로그인 완료
- **사용 가능 모델**: `grok-4.6` (기본값) 및 `grok-4.5` (2종만 유효)
- **단발 실행 형식**: `grok -p "<프롬프트>" --model grok-4.6 --output-format plain`
- **추론 수준**: `--effort` (별칭 `--reasoning-effort`, 값: `low`, `medium`, `high`)
- **등급 정책**:
  - `high`: 코디네이터 전용 등급 (워커 자동 배정 제외)
  - `medium`, `low`: 워커 및 리뷰어 허용 등급
  - Gemini `TIER_POLICY` 자동 배정은 불변 유지, Grok은 `WORKER_MODEL_NOTICE` 후 명시 배정으로만 운용
- **워커 기동 경로**:
  - `orca terminal create --worktree path:<워크트리> --command grok` 으로 TUI 대화형 세션을 기동한 후
  - `orca_taskctl dispatch --terminal <handle>` 로 부착

---

## 3. 구현 변경 내역

### 3.1 모델 라우터 갱신 (`scripts/orca_model_router.py`)

1. **`PROBE_CONFIG["grok"]` 추가**:
   - `probe_cmd`: `["grok", "-p", "ping", "--model", "{model}", "--output-format", "plain"]`
   - `timeout`: 60초
2. **`MODEL_POOL` 모델 등록**:
   - `grok-4.6`: id `grok-4.6`, provider `grok`, probe_provider `grok`, tier `secondary`, auto_selectable `False`, max_tokens `1,000,000`, worker_efforts `["medium", "low"]`, coordinator_efforts `["high"]`
   - `grok-4.5`: id `grok-4.5`, provider `grok`, probe_provider `grok`, tier `secondary`, auto_selectable `False`, max_tokens `1,000,000`, worker_efforts `["medium", "low"]`, coordinator_efforts `["high"]`
3. **`MODEL_PROVIDER_PREFIXES` 갱신**:
   - `("grok", "grok")` 매핑 추가로 미등록 임의 grok ID도 `grok` 패밀리로 정확히 판정
4. **`probe_model` provider 분기 추가**:
   - `elif "grok" in model_id.lower(): provider = "grok"; probe_key = "grok"` 추가

### 3.2 독립 리뷰어 실행 경로 연동 (`scripts/orca_run_reviewer.py`)

1. **`SUPPORTED_REVIEWER_PROVIDERS` 확장**:
   - `frozenset({"gemini", "claude", "cerebras", "qwen", "grok"})`로 `grok` 추가
2. **`build_model_command` grok 분기 구현**:
   - `provider == "grok"`일 때 `["grok", "-p", prompt, "--model", model, "--output-format", "plain"]` 인자 배열 반환

### 3.3 워커 제어 및 CLI 종류 인식 (`scripts/orca_taskctl.py`)

1. **`prepare-worker --cli-type grok` 지원**:
   - CLI 옵션 help 및 메타데이터 기록에 `grok` 지원
2. **`classify_file_edit_auto_approve_support` 및 화면 판정**:
   - grok 메타데이터 및 화면 문자열을 grok CLI 로 판정하고 파일 편집 모드 전환(shift+tab)을 전송하지 않도록 안전 차단
3. **`dispatch --terminal` 경로에서 grok TUI 자동 식별**:
   - `detected_cli`: `--agent`, 기존 메타데이터 `cli_type`, `--model`, 또는 `orca terminal show` 명령 결과에서 grok TUI 터미널을 자동 판정하여 메타데이터에 기록

### 3.4 운영 문서 갱신

1. `docs/ops/orca_worker_model_pool.md`:
   - 1.1절 등록 모델 풀 표에 `grok-4.6` 및 `grok-4.5` 등록
   - 4절로 Grok CLI v1.0.13 가용성 실측 및 운용 정책(effort high 코디네이터 보존, 워커 effort medium/low 등) 신설
   - 6.1절 Grok 워커 기동 경로(`orca terminal create --command grok` -> `dispatch --terminal`) 추가
   - 이후 절 번호 순차 갱신 (5~9절)
2. `docs/ops/orca_control_plane_tools.md`:
   - 3.5절 Grok 워커 기동 및 터미널 부착 절차 신설
   - 4.3절 `MODEL_POOL` 표에 `grok-4.6` 및 `grok-4.5` 행 추가

### 3.5 단위 테스트 보강

1. `tests/test_orca_model_router.py`:
   - `test_grok_pool_registration`: grok-4.6, grok-4.5 등록, provider, probe_provider, auto_selectable False, effort 등급 검증
   - `test_provider_for_model_grok`: grok 계열 제공자 판정 검증
   - `test_probe_grok_uses_local_grok_cli`: mock을 통한 grok CLI 호출 인자 형태(`-p`, `--model`, `--output-format plain`) 검증
   - `test_probe_grok_4_5_uses_local_grok_cli`: grok-4.5 호출 인자 검증
   - `test_grok_not_in_tier_policy`: TIER_POLICY 자동 배정/fallback 배제 불변식 검증
   - `test_auto_selectable_pools_distinction`: non_auto_pools에 grok 모델 추가
2. `tests/test_orca_run_reviewer.py`:
   - `test_build_model_command_supported_independent_reviewer_models`: grok 지원 검증
   - `test_build_model_command_grok_generates_grok_dash_p`: grok 명령어 생성 세부 인자 검증
3. `tests/test_orca_taskctl.py`:
   - `test_classify_file_edit_auto_approve_support_grok`: grok 메타데이터 기록 판정 검증
   - `test_classify_screen_text_grok`: grok 화면 문자열 판정 검증
   - `test_prepare_worker_cli_type_grok`: `prepare-worker --cli-type grok` 실행 및 메타데이터 기록 검증

---

## 4. 검증 결과 요약

1. **단위 테스트**:
   - `uv run pytest tests/test_orca_model_router.py tests/test_orca_run_reviewer.py tests/test_orca_taskctl.py -q`: 405 passed
2. **다중 에이전트 규칙 정합성 검증**:
   - `python3 scripts/validate_agent_rules.py --quiet`: 20/20 통과
3. **코드 린터**:
   - `uv run ruff check`: 대상 파일 전량 통과
4. **회귀 검증**:
   - `TIER_POLICY` 및 `route` 결과 기존과 완전 동일 확인
   - Antigravity 및 Claude Code 등 기존 프로바이더 호출 경로 회귀 없음 확인
