# Task 89a6734a0df5: 모델 인벤토리 감사 도구 회수 변경 이식 보고서

> **작성일**: 2026-08-26
> **작업 ID**: `task_89a6734a0df5`
> **수행 역할**: Builder

---

## 1. 작업 개요

본 작업은 2026-08-26 베이크오프 브랜치 판정에서 회수 후보로 확정된 두 변경 사항을 `main` 브랜치의 `scripts/audit_model_inventory.py`에 이식하고 회귀 테스트를 구축하는 작업입니다.

| 대상 브랜치 | 회수 대상 변경 | 적용 방식 |
| --- | --- | --- |
| `kwanbum217/b2-or_nemoultra` | `--json` 기계 판독 출력 옵션 | `scripts/audit_model_inventory.py`에 `--json` 옵션 및 구조화된 JSON 생성 로직 이식 |
| `kwanbum217/bakeoff-mimo` | `audit_with_state` 함수 분리 | 상태 파일 경로(`state_path`) 주입 및 관측 이력 기반 감사를 담당하는 함수 분리 |

---

## 2. 세부 구현 내역

### 2.1 scripts/audit_model_inventory.py 변경

1. **`audit_with_state` 함수 분리 및 JSON 구조 생성**:
   - `audit_with_state(with_agy: bool = False, state_path: Path | None = None) -> tuple[int, list[str], dict[str, dict[str, str | int]]]` 형태로 분리.
   - 상태 파일 경로(`state_path`)를 인자로 받아 임의 경로의 이력 파일을 주입할 수 있도록 개선.
   - 각 모델 풀별 상태(`skipped`, `unknown`, `present`, `absent`)와 연속 absent 스트릭(`streak`) 정보를 `pools_json` 딕셔너리로 취합.
2. **`audit` 기존 호환 인터페이스 유지**:
   - `audit(with_agy: bool = False, state_path: Path | None = None) -> tuple[int, list[str]]` 유지.
   - 내부적으로 `audit_with_state`를 호출하여 기존 시그니처와 반환값 호환성을 100% 보장.
3. **`--json` CLI 옵션 추가**:
   - `parser.add_argument("--json", action="store_true", help="기계 판독용 JSON 만 출력합니다 (--quiet 무시)")` 추가.
   - 정상 실행 시 `{"extinct": missing, "pools": pools_json}` JSON 출력 후 종료 코드(0: 정상, 1: 소멸 발견) 반환.
   - 도구 오류 발생 시 `{"extinct": 0, "pools": {}}` JSON 출력 후 종료 코드 2 반환.
   - `--reset-state --json` 실행 시 안내 메시지 억제.

### 2.2 tests/test_audit_model_inventory.py 회귀 테스트 추가

기존 11개 테스트에 더해 5개의 신규 회귀 테스트를 추가하여 총 16개 테스트 케이스를 구축하였습니다.

| 테스트 함수명 | 검증 내용 |
| --- | --- |
| `test_audit_with_state_returns_pools_json` | `audit_with_state`가 3개 튜플(`missing`, `lines`, `pools_json`)을 반환하고 각 풀의 상태 및 스트릭이 올바른지 검증 |
| `test_json_flag_output_format` | `--json` 실행 시 유효한 JSON 포맷 출력 및 정상 종료 코드 0 검증 |
| `test_json_flag_missing_model` | 3회 연속 absent 발생 시 `--json`에서 `extinct: 1`, `streak: 3` 및 종료 코드 1 검증 |
| `test_json_flag_error_handling` | 도구 오류 발생 시 `--json`에서 `extinct: 0`, 빈 `pools` 및 종료 코드 2 검증 |
| `test_json_flag_reset_state` | `--reset-state --json` 동시 사용 시 stdout 안내문구 억제 및 정상 종료 검증 |

---

## 3. 검증 결과

1. **단위 테스트 (`tests/test_audit_model_inventory.py`)**:
   - 16 passed (100% 통과)
2. **정적 검사 및 린트**:
   - `uv run ruff check src scripts tests`: 통과 (All checks passed)
   - `uv run mypy src/ scripts/`: 통과 (Success: no issues found in 198 source files)
3. **CLI 수동 검증**:
   - `--json` 없이 실행 시: 기존 사람이 읽는 텍스트 정상 출력 확인
   - `--json` 실행 시: 기계 판독용 JSON 한 줄 출력 확인

---

## 4. 특이사항 및 범위 외 사안

- **`CURRENT_STATE.md` source_commit 지연 건**:
  - `python3 scripts/validate_agent_rules.py` 실행 시 `CURRENT_STATE.md`의 `source_commit`(`ad0267a`)이 HEAD 대비 6 커밋 뒤처져 규칙 검증(허용 5 커밋)에 걸리는 현상이 확인되었습니다.
  - 이는 코디네이터의 이전 선행 커밋들로 인한 현상이며, 워커의 `allowed_write_files` 범위 밖이므로 본 작업에서는 정본 원칙에 따라 `docs/context/CURRENT_STATE.md`를 직접 수정하지 않고 코디네이터 인수인계 사항으로 보고합니다.
