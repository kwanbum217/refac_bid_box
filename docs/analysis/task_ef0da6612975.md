# Task task_ef0da6612975 분석 기록

> 작성일: 2026-09-06
> Task: backup_schedule_task 디스크 경보 순서 시정 및 테스트 보강

## 1. 변경 요약

- `src/tasks/scheduled_tasks.py`의 `backup_schedule_task`에서 사전 측정은 로그로만 남기고, `notify` 경보는 `prune_snapshots` 이후 재측정한 값으로만 판정하도록 순서를 바로잡았습니다.
- 반환 dict의 `disk_free_gb`에는 정리 이후 측정값을 담고, 정리 이전 값은 `disk_free_gb_before_prune` 키로 함께 반환합니다.
- 실패 경로(`notify_task_failure`, `status: failed`)와 prune errors 경고 로그는 그대로 유지했습니다.

## 2. 기존 테스트 조정

- 기존 테스트의 판정을 약화시키지 않았습니다.
- `test_backup_task_notifies_low_disk_space`는 단일 값 mock으로 사전/사후가 동일하므로 새 계약에서도 그대로 경보 1회를 기대하며 통과합니다. 기대값 변경이 필요 없었습니다.

## 3. 추가 테스트

| 테스트 | 검증 내용 |
| --- | --- |
| threshold above/equal/below 3건 | `<` 비교이며 동일 값은 `is_low=False` |
| measurement failure 1건 | `disk_usage` 예외 시 `(0.0, True)` |
| symlink stale 1건 | stale 심볼릭 링크 시 errors 기록, 삭제 없음 |
| prune 회복 1건 | 사전 부족, 사후 회복 시 `notify` 미호출, 두 측정값 반환 확인 |
| prune 후에도 부족 1건 | 사후 부족 시 `notify` 1회, 두 측정값 반환 확인 |

## 4. 검증 결과

- `uv run pytest tests/test_backup_schedule.py -q`: 18건 통과
- `uv run pytest tests/ -q -m 'not data_assets'`: 3722건 통과, 41건 건너뜀, 3건 제외
- `uv run mypy src`: 93개 파일 이상 없음
- `python3 scripts/validate_agent_rules.py --quiet`: 20/20 통과
