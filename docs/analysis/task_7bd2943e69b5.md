# task_7bd2943e69b5: orca_worker_watch 정체 신호 확장

> **작성일**: 2026-08-28
> **상태**: 완료

## 배경

2026-08-28 세션에서 워커가 네트워크 오류로 턴이 종료되어 유휴 상태였으나, `orca_worker_watch.py`는 CLI 설문만 탐지했습니다. 승인 대화창 신호만으로는 키 입력으로 풀리지 않는 실패 정체를 표현할 수 없었습니다.

## 변경 요약

| 항목 | 내용 |
| --- | --- |
| 신호 분류 | `prompt`(승인 대기) / `failure`(실패 정체) |
| 실패 신호 | network error, connection error, fetch failed, rate limit, 429, quota exceeded, authentication failed, unauthorized, token expired, model not found, upstream error, 502 |
| 우선순위 | 같은 화면에 prompt 와 failure 가 함께 있으면 failure 우선 |
| 출력 | `blocked_kind` 필드, 사람용 출력에 분류 라벨 |
| note | failure 는 지시 재전송 안내, prompt 는 기존 터미널 직접 확인 안내 |

## 검증

- `uv run pytest tests/test_orca_worker_watch.py -q`: 28 passed
- `uv run pytest tests/ -q -m 'not data_assets'`: 2540 passed
- `uv run ruff check src/ scripts/ tests/`: passed
- `python3 scripts/validate_agent_rules.py --quiet`: passed
