# Task: 운영 쌍대검정 기계 게이트 및 원자적 교체

> **task_id**: task_15b11c894c87
> **작성일**: 2026-09-02
> **상태**: 완료

---

## 1. 배경

`v_20260807_110637_435` 는 운영 쌍대검정에서 기각된 버전이다 (docs/changelogs/work_log.md 2026-08-07 절).
2025년 8,995건에서 MAE 1.4050 -> 1.4188 로 악화, t=5.14 로 base 우세.
그러나 `promote_model.py status` 는 이 버전을 "승격 조건 통과" 로 표시했다.
구조적 조건(홀드아웃, 시계열 분할, R2 누수, 특징 목록)만 검사하고 운영 쌍대검정 판정은 보지 않았기 때문이다.

## 2. 변경 내용

### 2.1 쌍대검정 판정 파일 (paired_verdict.json)

`ml_registry/<model>/<version>/paired_verdict.json` 을 기계 게이트로 도입.

| 필드 | 값 (v_20260807_110637_435) |
| --- | --- |
| verdict | rejected |
| champion_version | v_20260807_043210_535 |
| challenger_version | v_20260807_110637_435 |
| champion_checksum | "" (워크트리에 모델 파일 부재) |
| challenger_checksum | "" (동일) |
| sample_hash | "" (동일) |
| code_commit | "" |
| decided_at | 2026-08-07 |
| evidence | work_log.md 경로 + t=5.14, MAE 수치 |

체크섬과 sample_hash 는 워크트리에 모델 파일이 없어 계산 불가. 빈 문자열로 기록.

### 2.2 승격 게이트 계약

| 판정 상태 | force 없이 | force 있음 |
| --- | --- | --- |
| approved | 통과 | 통과 |
| rejected | 거부 | **거부** (뚫림 없음) |
| 파일 부재 (미판정) | 거부 | 통과 |
| 기타 값 | 거부 | 통과 |

### 2.3 원자적 교체

기존: serving 이동 -> 복사 (사이 부재/부분 상태 가능)
변경: staging 에 복사+검증 -> 검증 완료 후 swap

- staging 실패 시 serving 디렉터리 손대지지 않음
- swap 실패 시 backup 에서 복원

## 3. 변경 파일

| 파일 | 변경 |
| --- | --- |
| src/ml/promotion.py | read_paired_verdict, _is_paired_rejected 추가. check_promotion_criteria 에 registry_dir 인자. promote() 원자적 교체 |
| scripts/promote_model.py | status 에서 쌍대 기각 사유 + force 우회 불가 안내 출력. registry_dir 전달 |
| tests/test_promotion_gate.py | 신규: 게이트 계약 6건, 원자성 2건, CLI 표시 1건 |
| tests/test_promotion_cli.py | 픽스처에 paired_verdict.json (approved) 추가 |
| ml_registry/servc_institution_v1/v_20260807_110637_435/paired_verdict.json | 기각 판정 기록 |

## 4. 검증 결과

- `uv run pytest tests/ -q -m 'not data_assets'`: 3036 passed, 25 skipped, 3 deselected
- `python3 scripts/validate_agent_rules.py --quiet`: 17/17 통과
- test_data_preservation 의 test_model_bin_files_exist, test_chroma_db_exists 2건은 워크트리에 데이터 파일이 없어 실패. 이 Task 의 결함이 아님
