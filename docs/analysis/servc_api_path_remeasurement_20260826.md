# Servc 운영 API 경로 재측정 (2026-08-26)

> **측정일**: 2026-08-26 (Asia/Seoul)
> **소스 커밋**: `43a5fb7`
> **운영 모델**: `servc_institution_v1/v_20260807_043210_535`
> **Orca Run**: `run_174e3518fbbb`
> **Task**: `task_7822e82af0b6`

---

## 1. 목적

과거 `predict_price_api` 평가가 인자 오바인딩으로 전 표본을 제외했을 가능성이 확인되어, 수정된 운영 호출부를 통해 2025년 표본을 다시 측정했습니다. 측정 중 저장소 변경이 발생한 최초 4,000건 실행은 즉시 중단하고 결과를 폐기했으며, 작업 트리를 다시 clean 상태로 만든 뒤 처음부터 재실행했습니다.

---

## 2. 운영 API 경로 300건

```bash
uv run python scripts/eval_servc_api_path.py --samples 300 --year 2025 --seed 42
```

| 지표 | 결과 |
| --- | ---: |
| 요청 / 평가 / 제외 | 300 / 300 / 0 |
| MAE / RMSE / 편향 | 1.2686 / 2.9048 / +0.0292 |
| 절대오차 중앙값 | 0.2240%p |
| 0.5%p 이내 | 190건 (63.33%) |
| 1.0%p 이내 | 227건 (75.67%) |
| 2.0%p 이내 | 249건 (83.00%) |
| 3.0%p 이내 | 267건 (89.00%) |
| 5.0%p 이내 | 279건 (93.00%) |
| 예측구간 피복률 | 89.67% |
| 예측구간 폭 중앙값 | 1.791%p |

300건 모두 운영 champion `servc_institution_v1`으로 평가됐습니다.

---

## 3. 하한율 집단별 4,000건

```bash
uv run python scripts/eval_servc_interval_by_group.py --samples 4000 --year 2025 --seed 42
```

| 집단 | 평가 / 제외 | MAE | 0.5%p 이내 | 피복률 | 구간 폭 중앙값 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 전체 | 3,996 / 4 | 1.3840 | 64.74% | 89.64% | 1.6351%p |
| 하한율 존재 | 2,808 / 0 | 0.8444 | 77.64% | 89.46% | 1.1588%p |
| 하한율 결측 | 1,188 / 4 | 2.6593 | 34.26% | 90.07% | 8.1985%p |

전체 실제값 표준편차는 6.111%p였습니다. 하한율 결측 집단은 피복률은 명목 90%에 가깝지만 MAE와 구간 폭이 크게 늘어나는 기존 판정을 재확인했습니다.

---

## 4. 판정

- 과거 전 표본 제외 가능성이 있던 결과는 본 측정으로 대체합니다.
- 수정된 `predict_price_api` 운영 경로는 표본 300건을 제외 없이 처리했습니다.
- 성능 수치는 기존 champion 승격 판정을 뒤집지 않습니다.
- 하한율 결측 집단의 넓은 구간은 불확실성 반영 결과이므로 임의 축소하지 않습니다.
