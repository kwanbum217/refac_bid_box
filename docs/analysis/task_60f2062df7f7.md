# task_60f2062df7f7 분석 및 작업 보고서: Redis Claim 실패 응답 계약 복원 및 토큰 비노출

> **작성일**: 2026-09-03
> **작업 태스크**: `task_60f2062df7f7`
> **대상 브랜치**: `kwanbum217/orca-p1`
> **기준 커밋**: `1957073193df637a5c9180b289581c7b376f3fee`

---

## 1. 작업 개요

커밋 `1957073`에서 구현된 스케줄 claim 소유 토큰 원자 해제 및 실패 시 claim 유지 로직을 유지하면서, 다음 두 가지 결함을 최소 변경으로 보완하고 회귀 테스트로 고정했습니다.

1. **최상위 error 계약 복원**: `nightly_schedule_task` 및 `development_data_refresh_task`의 Redis claim 획득 실패 응답에서 누락되었던 `error: claim.detail`을 각각 단일 키로 복원하여 최상위 오류 계약을 유지했습니다.
2. **소유 토큰 INFO 로그 비노출**: `acquire_schedule_claim` 성공 시 기록되던 INFO 로그에서 claim 해제 권한을 가진 UUID 토큰 원문을 제거하고, `owner`, `key`, `ttl`만 안전하게 기록하도록 수정했습니다.

---

## 2. 변경 내역

### 2.1 `src/tasks/scheduled_tasks.py`

| 대상 위치 | 변경 전 (`1957073`) | 변경 후 (`task_60f2062df7f7`) | 목적 |
| --- | --- | --- | --- |
| `nightly_schedule_task` 실패 응답 | `status`, `reason`, `claim` | `status`, `reason`, `error: claim.detail`, `claim` | 최상위 오류 계약 복원 (중복 키 없음) |
| `development_data_refresh_task` 실패 응답 | `status`, `reason`, `claim` | `status`, `reason`, `error: claim.detail`, `claim` | 최상위 오류 계약 복원 (중복 키 없음) |
| `acquire_schedule_claim` 성공 로그 | `logger.info(..., token)` | `logger.info(..., ttl)` (token 제거) | 토큰 원문 노출 방지 |

### 2.2 `tests/test_schedule_catchup.py`

| 테스트 함수 | 유형 | 검증 내용 |
| --- | --- | --- |
| `test_nightly_aborts_pipeline_when_redis_unavailable` | 단언 강화 | `result["error"] == result["claim"]["detail"]` 검증 |
| `test_nightly_aborts_pipeline_on_command_error` | 신규 추가 | Redis 명령 에러 시 fail-closed 및 최상위 `error` 계약 검증 |
| `test_development_refresh_aborts_pipeline_when_redis_unavailable` | 단언 강화 | `result["error"] == result["claim"]["detail"]` 검증 |
| `test_development_refresh_aborts_pipeline_on_command_error` | 신규 추가 | Redis 명령 에러 시 fail-closed 및 최상위 `error` 계약 검증 |
| `test_acquire_schedule_claim_log_does_not_expose_token` | 신규 추가 | `caplog` 기반으로 INFO 로그에 `claim.token` 미포함 및 `owner`, `key`, `ttl` 정상 포함 검증 |

---

## 3. 검증 결과

1. **Pytest 단위/회귀 테스트**:
   - `uv run pytest tests/test_schedule_catchup.py -v`: 29건 전량 통과 (신규 3건 추가)
   - `uv run pytest tests/test_schedule_catchup.py tests/test_scheduled_tasks.py tests/test_backup_schedule.py tests/test_task_offload_scheduled.py -q`: 49건 전량 통과
2. **Ruff 린터**:
   - `uv run ruff check src/tasks/scheduled_tasks.py tests/test_schedule_catchup.py`: 통과 (All checks passed)
3. **규칙 정합성 검증**:
   - `python3 scripts/validate_agent_rules.py --quiet`: 통과 (20/20 건 통과)
