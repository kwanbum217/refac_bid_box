# RAG v2 32문항 정본 재측정과 T7 판정 (2026-08-30)

> **측정일**: 2026-08-30 (UTC 2026-08-29 15:59~16:11)
> **대상**: `gemma4:e2b`, HEAD `6210ee1` (git dirty=false)
> **fixture**: `data/eval/llm_quality_fixture_v2.json` 32문항 x 3회 (96요청)
> **결과**: [`blind_fixture_v2_e2b_20260830.json`](../../data/benchmarks/blind_fixture_v2_e2b_20260830.json)
> **판정**: `canonical=true`, `canonical_failed_gates=[]`, 요청 실패 0건
> **비교 기준**: [`llm_quality_e2b_head_d9a0536.json`](llm_quality_e2b_head_d9a0536.json) (T7 미포함)

---

## 1. 결론

**T7 conditional vector bypass 는 품질 회귀를 일으키지 않았습니다. 판정을 종료합니다.**

근거는 지표 비교가 아니라 검색 결과 자체의 동일성입니다. 두 측정에서 성공한
**공통 94개 요청의 `retrieved_evidence_ids` 가 한 건도 다르지 않습니다.** 순서까지
동일합니다. 이 fixture 범위에서 T7 은 검색 결과를 바꾸지 않았습니다.

`d9a0536` 은 T7 병합(`07bc710`)의 조상이 아니고 현 HEAD 는 T7 을 포함하므로,
이 비교는 유효한 T7 전후 대조입니다.

---

## 2. 지표 비교

| 지표 | 이전 정본 `d9a0536` (T7 없음) | **신 정본 `6210ee1` (T7 있음)** | 판정 |
| --- | ---: | ---: | :---: |
| Numeric 정확도 | 134/140 (95.7%) | **138/144 (95.8%)** | 동률 |
| Evidence recall 평균 | 0.957 | **0.958** | 동률 |
| Citation (검색 실패 보정) | 70/70 (100.0%) | **69/69 (100.0%)** | 동률 |
| Citation (원시) | 70/70 (100.0%) | 70/72 (97.2%) | 아래 3장 |
| Refusal 정확 | 24/24 | **24/24** | 동률 |
| 과잉응답 (검색 실패 보정) | 1 | **0** | 개선 |
| 과잉응답 (원시) | 1 | 3 | 아래 3장 |
| 금지 표현 위반(문자) | 0 | **0** | 동률 |
| 요청 실패 | 2/96 | **0/96** | 개선 |
| 전체 P50 | 3,221ms | **3,147ms** | 동률 |
| 전체 P95 | 25,830ms | **19,897ms** | 개선 |
| 전체 최대 | 77,108ms | 120,566ms | 아래 4장 |

분모가 다른 것은 이전 측정에서 q03 이 2회 타임아웃했기 때문입니다. 이번에는
전량 성공했습니다.

---

## 3. 저하로 보이는 두 지표는 전부 q21 한 문항이며, 실제로는 개선입니다

citation 누락 2건과 과잉응답 3건은 **모두 q21** 입니다. 다른 문항에는 하나도
없습니다. numeric 미검출 6건(2사실 x 3회)도 전부 q21 입니다.

q21 은 "2026년 조림지 풀베기사업 2차(**동부지구**)" 를 묻습니다. 그런데 두 측정
모두 기대 근거 `bid_10169448` 을 **한 번도 검색하지 못했습니다**(recall 0.0,
검색 결과 5건 동일).

행동만 달라졌습니다.

| | 이전 `d9a0536` | 신 `6210ee1` |
| --- | --- | --- |
| 답변 | "**영암지구**" 공고의 공고번호·기관·낙찰업체·금액을 사실처럼 제시 | "동부지구에 대한 정보가 검색 컨텍스트에 없어 알려드릴 수 없다" |
| 채점 | 비거부 + citation 있음 | 과잉응답 + citation 없음 |
| numeric | 0/2 | 0/2 |

**이전 동작은 다른 공고를 답한 환각입니다.** 사용자가 요청한 동부지구가 아니라
영암지구 정보를 근거 표기까지 붙여 제시했습니다. 새 동작은 없는 것을 없다고
말합니다. 채점기는 fixture 가 `context_sufficient=true` 로 선언했다는 이유로
후자를 과잉응답으로 깎습니다.

### 3.0 채점 규약을 고친 뒤의 수치 (2026-08-30 후속)

3.1 절이 지적한 결함은 같은 날 하네스에 반영됐습니다. 검색 실패 요청을 citation 과
과잉응답 집계에서 분리하고, 제외 전 원시값을 함께 남기도록 했습니다. 이 정본
산출물에 새 집계를 적용한 결과입니다.

| 항목 | 값 |
| --- | --- |
| 검색 실패 요청 | 3건, 전부 `q21` |
| Citation (보정) | 69/69 (100.0%) |
| Citation (원시) | 70/72 (97.2%) |
| 과잉응답 (보정) | 0 |
| 과잉응답 (원시) | 3 |
| Numeric·recall | 보정하지 않음 (95.8%, 0.958) |

**보정 후 수치는 T7 이전 기준선과 정확히 일치합니다.** 1장의 판정(검색 결과 동일성)에
더해 지표로도 회귀가 없음이 확인됩니다.

```bash
uv run python -c "
import json, sys; sys.path.insert(0,'scripts')
from measure_llm_quality import compute_summary
print(compute_summary(json.load(open('data/benchmarks/blind_fixture_v2_e2b_20260830.json'))['results']))"
```

---

### 3.1 채점 규약의 결함 (기록)

fixture 의 `context_sufficient` 는 **검색이 성공한다는 전제** 위에 있습니다. 검색이
기대 문서를 못 가져오면 실제 컨텍스트는 불충분한데, 채점기는 그 상황의 정직한
거부를 과잉응답으로, 거부 답변에 인용이 없는 것을 citation 누락으로 처리합니다.
**두 지표가 환각을 보상하고 정직을 벌합니다.**

이 측정에서는 영향 범위가 q21 하나라 판정을 뒤집지 않지만, 지표를 그대로 읽으면
반대 결론이 납니다. 채점기가 `evidence_recall == 0` 인 요청을 과잉응답·citation
집계에서 분리하도록 고치는 것이 옳습니다.

### 3.2 남은 실제 결함: q21 검색 실패

T7 과 무관하게 **"동부지구" 질의가 기대 문서를 못 찾는 문제는 그대로입니다.**
lexical 과 vector 어느 쪽도 `bid_10169448` 을 후보에 올리지 못합니다. 지구명이
다른 동명 공고가 다수 존재하는 것이 원인으로 보이나 확인하지 않았습니다.
별도 과제로 남깁니다.

---

## 4. 레이턴시

회차별로 보면 **1회차만 느리고 2·3회차는 3초대**인 cold/warm 패턴이 유지됩니다.

| 문항 | 이전 최장 | 신 최장 |
| --- | ---: | ---: |
| q08 rep1 | 75,075ms | **120,566ms** |
| q31 rep1 | 58,352ms | 75,343ms |
| q03 rep1 | 타임아웃(180s) | 71,871ms |
| q25 rep1 | 77,108ms | 50,047ms |

q03 은 이전에 2회 타임아웃했으나 이번에는 3회 전부 성공했고 2·3회차는 3.2초대로
끝났습니다. 반면 q08 1회차가 120초로 악화됐습니다. **최대값은 회차 간 편차가
크고 표본이 1회뿐이라 이 값만으로 회귀를 판정하지 않습니다.** 구간별 정본은
`benchmark_rag_segments.py` 가 담당합니다.

---

## 5. 재현 명령

```bash
uv run python scripts/measure_llm_quality.py \
  --fixture data/eval/llm_quality_fixture_v2.json \
  --model-label e2b --expected-model gemma4:e2b \
  --repetitions 3 --timeout-sec 200 \
  --output data/benchmarks/blind_fixture_v2_e2b_20260830.json
```

`--fixture` 는 2026-08-30 부터 필수 인자입니다. 결과의 `canonical` 이 true 이고
`canonical_failed_gates` 가 비어 있어야 정본입니다.
