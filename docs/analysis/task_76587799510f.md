# 개체 조회 질의 의도 라우팅 결함 수정 및 일반화 회귀 검증 (task_76587799510f)

> **작성일**: 2026-08-25
> **Task ID**: `task_76587799510f`
> **대상 모듈**: `src/rag/query_planning.py`, `tests/test_query_planning.py`
> **상태**: 완료 (Coordinator Feedback Resolved & Verification Passed)

---

## 1. 결함 원인 분석 및 코디네이터 피드백 반영

### 1.1 최초 결함 원인
- `src/rag/query_planning.py`의 `build_retrieval_plan`에서 `use_sql`과 `use_vector`가 상호 배타적으로 계산되어, `낙찰률` 등 통계 키워드가 포함된 단건 공고 개체 조회 질의(q03, q07 등)가 정형 통계 전용으로 분류되어 ChromaDB 벡터 검색이 꺼지는 결함이 발생하였습니다.

### 1.2 코디네이터 반려 사항 및 구조적 개선
1. **속성어 단독 출현 및 괄호 과탐 방지**:
   - `낙찰금액`, `낙찰업체` 등의 속성어가 단독으로 존재하거나 `(용역)` 같은 단순 카테고리 괄호가 포함되었을 때 순수 집계 질의("최근 3개월 평균 낙찰금액 추세를 알려줘", "평균 낙찰률(용역) 추세")가 개체 지정 질의로 오분류되던 문제를 해결하였습니다.
   - 속성어 단독으로는 개체로 판정하지 않으며, 고유명 인용구, 공고번호 패턴, 기관/법인 지목, 또는 '~의 [속성]' 수식 구조와 결합할 때만 개체 지정으로 판정합니다.
2. **fixture 과적합 제거 및 구조적 신호 일반화**:
   - fixture 19문항에서 직출한 개별 사업명/업종명(`포스트프로덕션`, `세천정비`, `시료분석` 등) 및 특정 기관명(`체육시설사업소`, `건축사사무소` 등)을 전면 제거하였습니다.
   - 대한민국 공공조달 표준 법인/기관 형태 접미사(`주식회사`, `(주)`, `학교`, `병원`, `공사`, `공단`, `사업소`, `청`, `서` 등)와 한국어 명사 수식 문법 구조(`(?:용역|공사|구매|사업|제작|설치|공고|입찰)\s*의\s*(?:속성)`)로 일반화하였습니다.
3. **정확한 `route_reason` 기록**:
   - `use_vector`가 켜졌다는 이유만으로 의미 키워드가 없는데 "문맥/의미 질의"를 붙이던 동작을 수정하여, 실제 발생한 의도(`개체 지정 질의`, `정형 통계 질의`, `문맥/의미 질의`, `낙찰 결과 목록 질의`, `KB 상태 질의`)만 정직하게 남기도록 정비하였습니다.

---

## 2. 핵심 구현 상세

### 2.1 일반화된 개체 판정 (`is_entity_specific_query`)
```python
ENTITY_ORG_SUFFIXES = (
    "주식회사", "(주)", "㈜", "(유)", "유한회사", "합자회사", "협동조합", "법률사무소", "회계법인",
    "초등학교", "중학교", "고등학교", "대학교", "대학원", "유치원", "어린이집", "여중", "남중", "여고", "남고",
    "병원", "의료원", "보건소",
    "공항공사", "철도공사", "도로공사", "수자원공사", "토지주택공사", "가스공사",
    "시설공단", "관리공단", "사업소", "교육청", "지원청", "기상청", "경찰서", "소방서", "주민센터", "행정복지센터",
)
```
- **인용구**: `"..."`, `'...'`, `「...」`, `『...』`, `[...]`
- **식별자 패턴**: 공고번호(`R26BK...`, `\d{8,15}-\d{2,3}`), 문서 ID(`bid_\d+`)
- **형태소/기관 접미사**: 법인/학교/병원/공기업/관공서
- **구조적 수식어**: `\S+\s*의\s*(?:공고번호|수요기관|낙찰업체|낙찰자|최종\s*낙찰자|1순위\s*낙찰업체|입찰\s*참가\s*조건)`

### 2.2 독립 판정 라우팅 매트릭스
- **개체 지정 질의**: `use_sql = True, use_vector = True` (통계 키워드가 있어도 벡터 검색 필수 수행)
- **순수 집계 질의**: `use_sql = True, use_vector = False` (SQL 전용 집계 최적화 유지)
- **결과 목록 질의**: `use_sql = True, use_vector = False` (`result_limit` 추출)
- **문맥/의미 질의**: `use_sql = False, use_vector = True`
- **일반/기본 질의**: `use_sql = False, use_vector = True` (기본 벡터 질의)

---

## 3. 검증 결과

### 3.1 라우팅 및 회귀 테스트 (`tests/test_query_planning.py`)
- `test_coordinator_counterexamples_pure_aggregation`: 반례 2건("최근 3개월 평균 낙찰금액 추세를 알려줘", "평균 낙찰률(용역) 추세") `use_vector=False` 및 SQL 전용 라우팅 확인 (PASS)
- `test_independent_routing_both_sql_and_vector_can_be_true`: `use_sql`, `use_vector` 독립 동시 활성화 (PASS)
- `test_pure_aggregation_queries_maintain_sql_centric_routing`: 순수 집계 7종 질의 SQL 전용 라우팅 유지 (PASS)
- `test_fixture_q03_question_enables_vector_search`: fixture q03 `use_vector=True` (PASS)
- `test_fixture_q07_question_enables_vector_search`: fixture q07 `use_vector=True` (PASS)
- `test_all_context_sufficient_fixture_questions_enable_vector_search`: fixture q01~q16 전수 `use_vector=True` (PASS)

### 3.2 린터 및 전체 테스트 스위트
- `uv run ruff check src/ tests/`: 통과 (0 errors)
- `python3 scripts/validate_agent_rules.py --quiet`: 통과 (12/12 건)
- `uv run pytest tests/ -q -m 'not data_assets'`: 2001개 전수 테스트 통과
