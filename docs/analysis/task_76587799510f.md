# 개체 조회 질의 의도 라우팅 결함 수정 및 회귀 검증 (task_76587799510f)

> **작성일**: 2026-08-25
> **Task ID**: `task_76587799510f`
> **대상 모듈**: `src/rag/query_planning.py`, `tests/test_query_planning.py`
> **상태**: 완료 (Verification Passed)

---

## 1. 결함 원인 분석

2026-08-24 LLM 품질 fixture(19문항) 실측 중 `context_sufficient` 16문항 중 8문항에서 ChromaDB 기대 근거를 검색하지 못하는 현상이 확인되었습니다.
`q03`, `q07` 등은 ChromaDB bge-m3 직접 검색 시 1위로 검색되는 실재 근거가 존재함에도 앱 RAG 경로에서 `retrieved_docs: 0건`으로 누락되었습니다.

### 1.1 기존 라우팅 로직의 구조적 결함
- `src/rag/query_planning.py`의 `build_retrieval_plan`에서 `use_sql`과 `use_vector`가 상호 배타적으로 계산되었습니다:
  - `use_sql = result_list_query or any(keyword in lowered for keyword in STATISTICS_KEYWORDS)`
  - `use_vector = not result_list_query and any(keyword in lowered for keyword in SEMANTIC_KEYWORDS)`
- `STATISTICS_KEYWORDS`에 `낙찰률`, `평균`, `비교`, `건수`, `통계` 등이 포함되어 있어, "○○ 용역의 낙찰업체와 낙찰금액 및 낙찰률을 알려줘"와 같은 단건 개체 조회 질의가 `낙찰률` 키워드로 인해 정형 통계 질의(`use_sql=True`)로 오분류되고 `use_vector`가 `False`로 강제 비활성화되었습니다.

---

## 2. 수정 내용

### 2.1 개체 지정 질의 판정 함수 (`is_entity_specific_query`) 추가
단건 공고/사업/기관/업체 지목 신호를 판정하는 `is_entity_specific_query` 함수를 구현하였습니다:
1. **따옴표/괄호 인용구**: `"..."`, `'...'`, `「...」`, `『...』`, `[...]`, `(...)`
2. **공고번호/문서 ID**: `R26BK01659912-001`, `bid_10015925` 등 표준 식별자 패턴
3. **기관/학교/병원/법인/사업소 지목**: `초등학교`, `중학교`, `고등학교`, `대학교`, `병원`, `공항공사`, `인천공항`, `기상청`, `주식회사`, `(주)`, `사업소` 등
4. **세부 사업/공사/용역 명칭**: `감리 용역`, `기술지도 용역`, `타당성조사`, `임차용역`, `보수정비공사`, `세천정비공사`, `사면보강`, `구매설치`, `물품 제작`, `시료분석` 등
5. **개체 속성 지목 질의**: `공고번호`, `수요기관`, `낙찰업체`, `낙찰자`, `최종 낙찰자`, `낙찰금액`, `예정가격`, `1순위`, `입찰 참가 조건` 등

### 2.2 독립 판정 및 하이브리드 라우팅 적용
`use_sql`과 `use_vector`를 상호 배타가 아닌 독립 판정으로 전환하였습니다:
- **개체 지정 질의 (`is_entity == True`)**: `use_sql = True`, `use_vector = True` (통계 키워드가 포함되어도 벡터 검색 활성화)
- **순수 집계 질의 (`has_stat_keywords == True` & `is_entity == False`)**: `use_sql = True`, `use_vector = False` (종전 SQL 전용 경로 유지)
- **결과 목록 질의 (`result_list_query == True` & `is_entity == False`)**: `use_sql = True`, `use_vector = False` (`result_limit` 필터 보존)
- **문맥/의미 질의 (`has_semantic_keywords == True`)**: `use_sql = False`, `use_vector = True`
- **미확정 질의 (fallback)**: `use_sql = True`, `use_vector = True` (하이브리드 안전 처리)

### 2.3 `route_reason` 및 시그니처 정합성 유지
- `route_reason`에 `개체 지정 질의` 판정 근거가 명확히 기록됩니다.
- `RetrievalPlan` 필드명 및 호출부 시그니처를 100% 보존하였습니다.

---

## 3. 검증 결과

### 3.1 신규 회귀 테스트 (`tests/test_query_planning.py`)
- `test_independent_routing_both_sql_and_vector_can_be_true`: `use_sql`과 `use_vector` 동시 True 검증 (PASS)
- `test_entity_specific_mixed_with_statistics_keywords_keeps_vector_enabled`: 통계 키워드 혼합 시 벡터 유지 검증 (PASS)
- `test_pure_aggregation_queries_maintain_sql_centric_routing`: 순수 집계 질의 SQL 전용 라우팅 유지 검증 (PASS)
- `test_result_list_query_and_limit_extraction`: 결과 목록 질의 및 result_limit 추출 검증 (PASS)
- `test_fixture_q03_question_enables_vector_search`: fixture q03 실측 질의 `use_vector=True` 검증 (PASS)
- `test_fixture_q07_question_enables_vector_search`: fixture q07 실측 질의 `use_vector=True` 검증 (PASS)
- `test_all_context_sufficient_fixture_questions_enable_vector_search`: fixture q01~q16 전수 `use_vector=True` 검증 (PASS)
- `test_various_entity_specific_signals`: 인용구, 공고번호, 기관명, 법인명 등 다양한 신호 검증 (PASS)
- `test_undetermined_queries_fallback_to_hybrid`: 미확정 질의 하이브리드 폴백 검증 (PASS)

### 3.2 린터 및 규칙 검증
- `uv run ruff check src/ tests/`: All checks passed (0 errors)
- `python3 scripts/validate_agent_rules.py --quiet`: 검증 통과 (12/12 건)
