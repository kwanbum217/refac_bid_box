# mypy 1건 모듈 17개 override 해소 (task_3a6355447a9d)

> **작성일**: 2026-08-26
> **작업 ID**: `task_3a6355447a9d`
> **선행 인벤토리**: `docs/analysis/task_cd74b709edb0.md`

---

## 1. 목표

인벤토리에서 오류 1건으로 확인된 17개 모듈의 mypy 오류를 코드 수정으로 해소하고, `pyproject.toml`의 `[[tool.mypy.overrides]]` 목록에서 해당 모듈을 제거한다.

---

## 2. 해소한 모듈 (17개)

| 모듈 | 억제 코드 | 수정 요약 |
| --- | --- | --- |
| `scripts.summarize_worker_done` | unused-ignore | 미사용 `type: ignore[no-redef]` 제거 |
| `src.app.api.v1.chatbot` | arg-type | `_PendingRagAnswer.kb_status`를 `dict[str, Any] \| None`으로 정정 |
| `src.app.services.home_context` | arg-type | `sucsf_bid_rate` None 가드 후 `float()` |
| `src.app.services.conversation_state` | assignment | JSON 컬럼 할당에 `cast(dict[str, Any], chart_payload)` |
| `src.app.services.tools.trend_analyzer` | arg-type | 시계열 값에 `cast(float, item["value"])` |
| `scripts.eval_servc_api_path` | arg-type | `Request` + `db=session`로 `predict_price_api` 호출 |
| `scripts.eval_servc_interval_by_group` | arg-type | 동일 |
| `scripts.eval_servc_inst_clsfc_cross` | operator | `practical_min_gain` 지역 변수로 비교 |
| `scripts.eval_servc_ensemble_headroom` | call-overload | `rows` MAE 값 `cast(float, ...)` |
| `scripts.benchmark_arq_throughput` | call-overload | `cpu_count` 숫자 타입 좁히기 |
| `scripts.benchmark_rag_segments` | attr-defined | `perf_config` dict 검증 후 `.get()` |
| `scripts.import_data_assets` | index | `manifest`/`collections` 타입 명시 |
| `scripts.repair_contract_method` | attr-defined | `CursorResult` cast 후 `rowcount` |
| `scripts.run_p9_sse_parallel_bench` | attr-defined | `get_git_sha` import를 `benchmark_arq_throughput`로 |
| `scripts.run_p9_sse_rebaseline` | attr-defined | 동일 |
| `scripts.audit_servc_rawdata_regime_scan` | assignment | CSV 루프 변수 `in_use_flag`로 분리 |
| `scripts.arq_gate` | assignment | `main()` 분기별 결과 변수 분리 |

---

## 3. override 변화

- 제거 전: 55개 모듈, 297건 억제 (인벤토리 기준)
- 제거 후: 38개 모듈 (17개 모듈 override 목록에서 제거)
- 삭제된 override 블록: `summarize_worker_done`(unused-ignore 단독), `eval_servc_inst_clsfc_cross`(operator 단독), `benchmark_arq_throughput`/`eval_servc_ensemble_headroom`(call-overload 공유)

---

## 4. 검증

| 명령 | 결과 |
| --- | --- |
| `uv run mypy src/ scripts/` | Success |
| `uv run ruff check src/ scripts/ tests/` | 통과 |
| `uv run pytest tests/ -q -m 'not data_assets'` | 2133 passed |
| `python3 scripts/validate_agent_rules.py --quiet` | 12/12 통과 |

---

## 5. 런타임 동작

- `predict_price_api` 호출은 `db=session`을 명시하고 빈 `Request`를 전달한다. 기존에는 `session`이 `request` 자리에 바인딩되어 `Depends` 기본값이 DB에 주입되지 않았다.
- `arq_gate` 임계값 필수화(`RepetitionThresholds`)는 변경하지 않았다.
- 새 `type: ignore` 주석은 추가하지 않았다.
