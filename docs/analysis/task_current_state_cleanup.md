# CURRENT_STATE.md 종결 항목 4.1절 이관 및 정본 경량화 분석 보고서

> **작성일**: 2026-08-22
> **Task ID**: task_current_state_cleanup
> **대상 파일**: `docs/context/CURRENT_STATE.md`
> **목적**: 4장 종결 항목의 4.1절 이관, 진행 항목 재정렬, 본문 예산 확보 (8000자 -> 7400자 이하)

---

## 1. 이관 및 경량화 개요

`docs/context/CURRENT_STATE.md` 는 기존 8,000자(상한 8,000자)로 포화 상태였으며, 신규 운영 과업 추가를 위한 예산 확보가 시급했습니다. 본 작업에서는 사양에 따라 사실의 삭제 없이 위치 이전 및 문맥 압축만을 수행하여 **7,378자**로 경량화(622자 여유 확보)를 완료했습니다.

---

## 2. 주요 변경 사항 대조

| 구분 | 변경 전 위치 및 상태 | 변경 후 위치 및 상태 | 보존된 핵심 사실 및 근거 |
| --- | --- | --- | --- |
| **무료 워커 경합 종료** | 4장 항목 0 (152자) | 4.1절 `워커 풀 정비·상시 관측 전환` 통합 | 2026-08-20 경합 10종 중 5종 완주, n=3 순위 미재현으로 2026-08-21 정기 경합 종료, 최근 10회 성공률·연속 실패 강등·제외(3회 미만 미반영), 파일 잠금 보호, 트리거(`benchmarks/free_workers/README.md` 6장), `suitable_for` 실측 역할 한정, 격리 4종, Kimi 런치 인자 |
| **ORM Mapped[] 전환** | 4장 항목 4 (165자) | 4.1절 `ORM Mapped[] 전환 완료` 전용 항목 | `bids.py`, `chatbot.py`, `accounts.py`, `predictions.py` 13개 모델·137개 컬럼 전량 전환, 메타데이터 지문 `60a9cf49…c51c8` 전후 일치(DDL 불변 실증), mypy `call-overload` 예외 6개 전량 제거 |
| **워커 기동 준비 자동화** | 4장 항목 1 (178자) | 완료부 4.1절 이관, 후속 판단 4장 항목 1 유지 | [완료부] `scripts/orca_prepare_worktree.py` `.env`·신뢰·pre-commit 자동화(`--check` 미준비 시 종료 1), `shift+tab` 필요.<br>[후속 판단] pre-commit 미실행 pytest 실패 커밋의 게이트 한정 노출 대응을 위한 커밋 게이트 도입 판단 |
| **동기 블로킹 I/O P95** | 4장 항목 3 (173자) | 실측 완료부 4.1절 이관, 후속 판단 4장 항목 3 유지 | [실측 완료부] 예측 API 웜 P95 61.9~83.6ms(목표 100ms 4회 연속 충족, 콜드 1~2회차 383·122ms 미달), SSE 첫 토큰 1.26~2.29s 및 완료 6.37~7.65s 충족, 기준선 부재로 개선폭 미주장, 단발 6.3~10.3s 목표 미정의.<br>[후속 판단] 예측 콜드스타트 워밍업 도입 여부, 단발 질의 목표/SSE 유도 판단, Arq 태스크 큐 적재 실측 설계 |
| **진행 과업 재정렬** | 4장 항목 0..7 (불연속) | 4장 항목 1..6 (연속 재번호) | 1: 워커 기동 후속(pytest 커밋 게이트), 2: 코디네이터 토큰 최적화 v2, 3: 블로킹 I/O 후속(콜드/단발/Arq), 4: Ollama 병렬도·SSE, 5: Windows Docker Desktop, 6: 수집 2·3회차 관찰 |

---

## 3. 검증 결과

1. **컨텍스트 예산 검증**:
   - `CURRENT_STATE.md`: **7,378자** (목표 7,400자 이하 충족, 상한 8,000자 대비 622자 여유 확보)
   - `python3 scripts/validate_agent_rules.py`: `[PASS] 컨텍스트 예산` 확인

2. **수치 및 판정 근거 무결성 검증**:
   - G1/G2/G3 판정, 예측 API 레이턴시 테이블, SSE 스트리밍 지표 테이블, 23개 금지 항목 일람, 메타데이터 지문, P95 측정 수치 전량 보존

3. **source_commit 불변 검증**:
   - `source_commit`: `1cfea2e` 유지 (코디네이터 소유 규칙 준수)
