# task_49b8bc065322 분석 보고서

> **작성일**: 2026-08-28
> **Task ID**: task_49b8bc065322
> **브랜치**: kwanbum217/t2-verif-identity
> **커밋**: 014e3ac

---

## 1. 작업 목적

`verify_verification_truth` 의 검증 수준을 pass/fail 진위 대조에서 결과 동일성 대조로 승격합니다.

기존 게이트는 실제 실행 결과가 성공이고 보고서도 성공이면 통과했습니다. 보고서가 `43 passed` 인데 `500 passed` 로 적어도 양쪽 모두 성공이므로 통과하는 허점이 있었습니다.

---

## 2. 변경 요약

### 2.1 `scripts/orca_contract.py`

| 변경 항목 | 설명 |
| --- | --- |
| `parse_pytest_summary` 추가 | pytest -q 출력 전체에서 뒤에서부터 요약 줄을 탐색. ANSI 이스케이프 및 `=` 장식 제거 후 건수 사전 반환. 요약 없으면 `None`. |
| `verify_verification_truth` 업그레이드 | 재실행 출력에서 건수 파싱 -> 보고서 건수와 대조 -> 불일치 시 violations 추가. |
| 명시적 선택 필드 지원 | `exit_code`, `passed`, `failed`, `skipped` 필드가 있으면 result 문자열 파싱보다 우선 사용. |
| `detailed_results` 필드 확장 | `actual_counts`, `reported_counts`, `count_match`, `actual_exit_code`, `stdout_digest` 추가. |
| 하위 호환 유지 | 보고서가 건수를 적지 않으면 대조를 건너뛰고 기존 pass/fail 판정만 적용. |

### 2.2 `tests/test_orca_contract.py`

Capsule required_change (a)~(i) 항목 모두 고정:

| 테스트 | 항목 |
| --- | --- |
| `test_parse_pytest_summary_passed_and_skipped` | (a) 43 passed, 2 skipped 파싱 |
| `test_parse_pytest_summary_failed_and_passed` | (b) 3 failed, 40 passed 파싱 |
| `test_parse_pytest_summary_no_summary_returns_none` | (c) 요약 없으면 None |
| `test_parse_pytest_summary_with_ansi_and_decoration` | (d) ANSI 및 = 장식 제거 후 파싱 |
| `test_verify_verification_truth_count_mismatch_fails` | (e) 43 vs 500 -> FAIL |
| `test_verify_verification_truth_count_match_passes` | (f) 건수 일치 -> PASS |
| `test_verify_verification_truth_no_counts_in_report_passes` | (g) 건수 없는 보고서 -> PASS (하위 호환) |
| `test_verify_verification_truth_explicit_passed_mismatch_fails` | (h) 명시 passed 불일치 -> FAIL |
| `test_verify_verification_truth_explicit_exit_code_mismatch_fails` | (i) 명시 exit_code 불일치 -> FAIL |
| `test_verify_verification_truth_details_have_required_fields` | detailed_results 필드 존재 확인 |

### 2.3 `tests/test_orca_level1_gate.py`

`test_count_mismatch_propagates_to_gate6_failure` 추가: 건수 불일치가 violations 로 반환되어 Level 1 게이트 실패로 전파됨을 고정.

### 2.4 `docs/ops/orca_task_capsule_v2.md`

섹션 3.1.1 추가: verification 배열 선택 필드(`exit_code`, `passed`, `failed`, `skipped`) 및 건수 대조 규칙 문서화.

---

## 3. 검증 결과

| 명령 | 결과 |
| --- | --- |
| `uv run pytest tests/test_orca_contract.py tests/test_orca_level1_gate.py -q` | 78 passed, 1 warning |
| `uv run pytest tests/ -q -m 'not data_assets'` | 2467 passed, 6 skipped |
| `uv run ruff check src/ scripts/ tests/` | All checks passed |
| `uv run mypy src/` | Success: no issues found in 89 source files |
| `python3 scripts/validate_agent_rules.py --quiet` | 검증 통과: 12/12 건 |

---

## 4. 리뷰 체크리스트 자체 평가

| ID | 질문 | 결과 |
| --- | --- | --- |
| `backward_compat_broken` | 건수를 적지 않은 기존 보고서가 실패하게 됐는가 | No - 하위 호환 테스트 (g) 통과 |
| `gate_loosened` | 기존 pass/fail 진위 판정이 약해졌는가 | No - exit code != 0 분기와 `_is_failure_result_str` 모두 유지 |
| `summary_last_line_only` | 요약 줄을 마지막 줄만 보고 파싱하는가 | No - `reversed(lines)` 로 뒤에서부터 탐색 |
| `slow_test` | 새 테스트가 실제 pytest 전량을 재실행하는가 | No - `patch("scripts.orca_contract.subprocess.run")` 모킹 |
| `digest_used_as_criterion` | stdout_digest 를 판정 기준으로 사용하는가 | No - 기록 전용 필드, violations 생성 조건에 미포함 |
| `scope_creep` | 허용 범위 밖 파일을 수정했는가 | No - 4개 파일 모두 `allowed_write_files` 내 |

---

## 5. 잔여 리스크

없음. 새 Python 라이브러리를 추가하지 않았고(hashlib, re 표준 라이브러리), DB 스키마 변경 없음.
