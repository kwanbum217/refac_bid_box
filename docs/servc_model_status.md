# 용역(Servc) 모델 개선 현황판

> **성격**: 날짜를 붙이지 않는 **갱신형 문서**입니다. 작업이 끝날 때마다 이 파일을 고치십시오. 새 파일을 만들지 마십시오.
> **최종 갱신**: 2026-08-13
> **현재 champion**: `quantile(0.5)`, `num_leaves` 255, 분위 모델 리프 63, 등각 배율 1.151263
> **운영 기준선**: MAE 1.4050, 0.5%p 적중 64.97%, 구간 폭 1.6706, 피복률 89.56% (2025년 8,995건)

---

## 0. 이 문서를 쓰는 법

| 문서 종류 | 역할 | 갱신 |
| --- | --- | --- |
| `docs/design/*_YYYYMMDD.md` | 그날의 측정과 근거 | **불변.** 고치지 않습니다 |
| **이 파일** | 어디까지 왔는가 | **매번 갱신** |
| `servc-model-tuning` 스킬 기각 목록 | 하지 말 것 | 축이 닫힐 때 추가 |

새로 측정하면 날짜 문서에 근거를 남기고, **결론만 이 표에 옮기십시오.** 다음 사람이 읽어야 할 파일은 이것 하나여야 합니다.

---

## 1. 열린 축 (지금 할 수 있는 것)

우선순위 순입니다.

| 축 | 상태 | 다음 행동 | 근거 |
| --- | --- | --- | --- |
| **학습 프레임의 비예가 누락** | **원인 규명 완료. 학습 축은 닫힘** | 조인 결함이 아니라 비예가는 낙찰률 자체가 없습니다(원천이 낙찰자·낙찰금액은 주고 낙찰률만 안 줌). **회수하지 마십시오.** 남은 것은 서빙 라우팅으로, 지금 비예가 공고(2026년 용역의 33.6%)가 그대로 일반 용역 모델에 들어갑니다 | `design/servc_nonprearng_population_cause_20260812.md` |
| **제도 플래그 특징 추가** | **전환 없는 3종은 닫힘.** 2장 참조 | 남은 것은 `rbidPermsnYn` 과 `prdctClsfcLmtYn` 뿐입니다. 아래 두 행이 그 선행 작업입니다 | `design/servc_flag_features_holdout_20260811.md` |
| 2025-01 체제 지시자 도입 | **범위 확정** | `prdctClsfcLmtYn` 축에만 필요. 하한율·`rbidPermsnYn` 축은 불필요로 판정 | `design/servc_flag_notation_verdict_20260811.md` |
| `rbidPermsnYn` 결측 수준 정규화 | 미착수 | 표기 이동으로 판정. 키 부재(최근 38%)를 제3수준으로 두면 지금 사용 가능 | 위와 동일 |
| `prearng_mthd` 와 결측 지시자 공선성 | 미착수 | 경계 이후 단일예가 99.34% 가 결측이라 두 특징 정보가 겹침. 학습 실험 축 | 위와 동일 |
| 신규 검출 6개 필드의 표기·실질 판정 | 미착수 | 92개 전수 스캔에서 2025-01 전환 6개 추가 검출. 아직 표기 이동인지 실질 변화인지 갈리지 않음. 전부 미사용 필드 | `design/servc_rawdata_regime_scan_20260811.md` |
| 적격심사제 계열 피복률 | **대기** | t 가 -1.6 미만이라 유의하지 않음. 재학습 후 부호 유지만 확인 | `design/servc_interval_coverage_recheck_20260811.md` |

### 1.1 제도 플래그 신호 요약

2025년 운영 잔차 96,141건(조인 100%) 기준입니다. 전체 err +0.0388, MAE 1.2897.

| 필드 | 집단 간 편향 폭 | 최대 t | 연도 재현 |
| --- | ---: | ---: | --- |
| `indstrytyLmtYn` | 0.42%p | +19.4 | 3/3 |
| `cmmnSpldmdMethdNm` | 0.34%p | -11.6 | 3/3 |
| `rbidPermsnYn` | 0.23%p | +10.1 | 미확인 |
| `dsgntCmptYn` | 0.21%p | -5.2 | 3/3 |
| `prdctClsfcLmtYn` | 0.15%p | +7.4 | 3/3 |

비교 13개가 본페로니 임계 2.9 를 모두 넘습니다.

**그러나 이 표를 기여의 예고로 읽지 마십시오.** 위 세 필드(`indstrytyLmtYn`,
`cmmnSpldmdMethdNm`, `dsgntCmptYn`)를 실제로 넣은 결과는 홀드아웃 평균 -0.0068
로 분할 산포 안이었습니다. 잔차 편향이 있다는 것과 그 정보가 기존 35종에 없다는
것은 다른 명제입니다. 상세는 2장과
`design/servc_flag_features_holdout_20260811.md` 를 보십시오.

---

## 2. 닫힌 축 (다시 열지 마십시오)

근거 없이 재시도하면 같은 실험을 두 번 하는 것입니다. 상세는 `servc-model-tuning` 스킬 기각 목록에 있습니다.

| 축 | 닫힌 날 | 한 줄 근거 |
| --- | --- | --- |
| 제도 플래그 3종 특징 추가(전환 없는 것) | 2026-08-11 | 세 분할 MAE 차이 -0.0177 / -0.0006 / -0.0022. 평균 -0.0068 이 분할 산포 0.0074 안이고 표준편차 0.0094 가 평균보다 큼. 승격 지표인 0.5%p 적중률은 2025년에서 부호가 뒤집힘. 코드 변경은 되돌림 |
| 분포 예측 계열(LightGBMLSS, NGBoost) | 2026-08-11 | 도입 논거 80.32% 는 출처 없음. 실측 90.02% 로 명목 일치 |
| 분위 `num_leaves` 상향(127/255) | 2026-08-11 | 세 분할 전부 63 이 구간 폭 최소 |
| `asignBdgtAmt`, `cntrctCnclsMthdNm` 추가 | 2026-08-11 | 기존 컬럼과 100% 동일 |
| `quantile` alpha 조정(0.35~0.58) | 2026-08-10 | 홀드아웃 이득이 분할 변동 안 |
| 학습 타깃을 초과분으로 변경 | 2026-08-10 | 세 시드 전부 MAE 악화 |
| 잔차 후처리 계열 다섯 축 | 2026-08 초 | 전부 기각 |
| 하한율 결측을 수집으로 축소 | 2026-08-10 | 결측의 99.1% 가 제도적 부재 |
| 하한율 결측 메커니즘 재분해 | 2026-08-11 | 2025-01 이후 표본에서 제도적 부재 99.98%. 미분류 9건. `lwlt_rate_missing` 하나로 충분 |
| 2026-05-26 레짐 (재개 조건 재확인) | 2026-08-13 | 재개 조건 둘 다 미달(희소 비중 3.26% 대 15%, MAE 격차 0.2422 대 0.5). 신제도 전체 편향 +0.0274 t=1.13 로 유의하지 않고 대조군과 부호 반대. MAE 원 격차 +0.0837 은 하한율 결측 비중 상승(+5.84%p)이 만든 것이며 구성 통제 시 -0.0156 으로 뒤집힘 |
| 낙찰방식별 분리 모델 | 이전 | 전부 악화 |

---

## 3. 사용자 결정 대기

| 항목 | 내용 |
| --- | --- |
| 건설(Cnstwk) parquet 재생성 | 1,358,882행, 전 연도 하한율 약 100% 보유. 기대값은 크나 **용역 우선 방침에 어긋남** |
| 물품(Thng) 후속 | 재생성 검증 완료(`data/feature_store_rebuild_20260811/`). 실익은 `ntce_kind_nm`, `bid_methd_nm` 두 컬럼. 연기 상태 |

---

## 4. 알려진 사실 (재조사 불필요)

측정해 둔 값입니다. 다시 재지 마십시오.

| 사실 | 값 |
| --- | --- |
| 하한율 값 존재율 추이 | 2024-06 100% -> 2025-01 56.8% -> 2026-08 38.3%. **단 이 값은 문자열 `'0'` 을 값으로 셈** |
| 하한율 실보유(양수) 비율 | 2024-06 46.6% -> 2026-08 38.4%. 2025-01 계단 없음. 위 급락은 표기 변경 |
| 2025-01 전환 필드 | `prdctClsfcLmtYn`, `rbidPermsnYn`, `sucsfbidLwltRate`(표기 변경, 실제 계단 아님) + 미사용 raw_data 6개 ([`servc_rawdata_regime_scan_20260811.md`](design/servc_rawdata_regime_scan_20260811.md)) |
| 학습 사용 필드(`IN_USE` 13개) 중 전환 | 92개 확대 조사에서 전환 없음. `sucsfbidLwltRate` 도 재분해 결과 표기 변경으로 정정 |
| 결측 집단 구간 폭 | 8.1197%p (보유 집단 1.1700%p 의 약 7배) |
| 결측 집단 피복률 | 90.02% (명목 90% 와 일치) |
| 홀드아웃 분할 산포 | 0.0074. 시드 산포 0.0010 의 7.4배 |
| 잔차와 특징의 최대 상관 | 0.105 |
| 파생 parquet 컬럼 수 | Servc 28, Thng 12(구형), Cnstwk 12(구형) |
| 학습 프레임 최신 개찰일 | **2026-08-03.** parquet 이 그 시점에 고정돼 있어 이후 표본이 반영되지 않습니다 |
| 기본 Compose 주간 재학습 | **꺼져 있음**(`ML_WEEKLY_RETRAIN_ENABLED=false`). 레짐 축 기각 근거가 "정기 재학습 유지" 를 전제하므로 재생성 없이는 재개 조건이 움직이지 않습니다 |

---

## 5. 판정 기준 (바꾸지 마십시오)

1. 홀드아웃 차이가 **0.0074 이내**면 분할 변동입니다. 운영 검정으로 보내지 않습니다
2. 분할마다 부호가 갈리면 기각입니다
3. 운영 쌍대 검정을 통과해야 승격입니다
4. 성능 주장에는 실측이 따릅니다. 추정치로 개선됐다고 쓰지 않습니다

---

## 6. 실행 참고

```bash
uv run python scripts/audit_servc_unused_fields.py           # 미사용 필드와 무조건부 신호
uv run python scripts/audit_servc_flag_residuals.py 2025     # 잔차 편향, 연도 인자
uv run python scripts/audit_servc_flag_regime.py <필드명>     # 월별 체제 전환 추적
uv run python scripts/eval_servc_interval_by_group.py --year 2025 --seed 42
```

`model.bin` 을 읽는 스크립트는 **주 저장소에서만** 동작합니다. 격리 작업 트리에서는 실패합니다.
