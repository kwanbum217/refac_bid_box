# 용역 모델 서빙 배선 인수인계

> **작성일**: 2026-08-04
> **갱신일**: 2026-08-06
> **상태**: 용역 EWM·리프 255 검증 및 `v_20260806_025423_494` 승격 완료
> **주요 병합 커밋**: `e1f7255`, `cbb9e5b`, `3dfe311`, `4e1f54c`

---

## 1. 무엇이 문제였나

성능은 올려 놨는데 **그 모델을 서비스에 태울 경로가 없었습니다.** 네 지점에서 값이 끊겼습니다.

| 지점 | 증상 | 상태 |
| --- | --- | --- |
| `model_registry` 특징 맵 복제본 | 문자열 범주가 float 변환 실패로 0.0 | 해결 |
| 서빙 불가 특징 0 채움 | 모르는 컬럼을 조용히 0.0 으로 | 해결 |
| `ml_registry` -> `data/model_files` | 잇는 코드 자체가 없음 | 해결 |
| `predict_optimal_price` 프레임 축소 | 제도 특징 26종·재발주 6종 유실 | 해결 |
| `predict_price_api` 제도 필드 누락 | 34개 중 30개가 기본값 | 해결 |

전부 **예외가 나지 않고 성능만 조용히 무너지는** 형태였습니다.

---

## 2. 최종 실측

기본 API 경로, 2025년 무작위 4,000건입니다. 금액 미공개 1건을 제외한
3,999건을 필터 없이 채점했습니다.

| 서빙 버전 | MAE | RMSE | 편향 | 0.5%p 이내 | 90% 구간 피복률 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `v_20260806_025423_494` | **1.4471** | **3.9564** | 0.1359 | **64.02%** | **89.40%** |

직전 champion과 같은 3,999건을 쌍대 비교한 결과 MAE는 1.4984에서
1.4471로 3.42% 개선됐고, 절대오차 차이는 -0.05135±0.00684(t=-7.50)였습니다.
최소 감지 차이 0.01369보다 관측 차이가 커 방향을 판정할 수 있습니다.

---

## 3. 함께 고친 것

### 3.1 모델 선택 기준

`avg_r2` 로 고르고 있었습니다. 실측에서 CatBoost 가 R2 는 이기고(0.6994 대 0.6967) **0.5%p 적중은 46.80% 대 60.49% 로 크게 졌습니다.** 낙찰률 잔차가 0 에 몰린 비대칭이라 R2 는 중심을 위로 밀어 올린 모델을 뽑습니다. `avg_mape` 로 교체했고 LightGBM 이 선택됩니다.

### 3.2 하이퍼파라미터

운영 학습기가 200트리/31리프로 실험본(600트리/63리프)보다 작았습니다. 맞춘 뒤 홀드아웃 R2 0.6688 -> 0.6881, 0.5%p 적중 54.77% -> 59.77%.

### 3.3 승격 게이트

설계서 7장 필수 4(어느 폴드도 R2 > 0.99 아닐 것)를 판정할 데이터가 없었습니다. 폴드 지표가 평균으로 뭉개져 저장됐기 때문입니다. `cv_metrics["folds"]` 에 원지표를 남기도록 고쳤습니다. 승격본 폴드별 R2 는 0.554 ~ 0.6819 로 누수 없음이 확인됩니다.

### 3.4 카테고리 기본 모델 매핑 3중 복제

`model_registry` / `bid_queries` / `bid_prediction_tool` 세 곳에 같은 dict 가 있었습니다. `model_registry.CATEGORY_DEFAULT_MODELS` 를 정본으로 삼고 나머지는 참조만 합니다.

---

## 4. 새로 생긴 것

| 파일 | 역할 |
| --- | --- |
| `src/ml/promotion.py` | `ml_registry` -> `data/model_files` 승격. 조건 검사 + 백업 + 롤백 |
| `src/ml/repeat_history.py` | (기존) 재발주 이력 |
| `src/ml/dataset.py::announcement_feature_payload` | 공고 1건을 학습 프레임 키 규격으로 펼침 |
| `src/ml/features.py::unservable_features` | 배포 전 서빙 가능 판정 |
| `ModelRegistry.verify_servable_features` | 배포 게이트 |
| `tests/test_serving_feature_parity.py` | 회귀 방지 48건 |
| `scripts/eval_servc_prediction_interval.py` | 예측 구간 보정 측정 |

승격 절차입니다.

```python
from src.ml.promotion import promote

promote("servc_institution_v1", category_code="Servc")
```

백업은 `data/model_backups/` 에 남습니다. **서빙 루트 안에 두면 `ModelRegistry` 가 백업을 모델로 오인해 로드합니다.**

---

## 5. 예측 구간 (완료)

상세는 [`docs/design/servc_prediction_interval_20260804.md`](../design/servc_prediction_interval_20260804.md).

**소박한 분위 회귀 구간은 보정에 실패합니다.** 명목 80% 대비 실제 75.52%, 10억 이상 구간은 66.85%. 등각예측 배율을 학습 때 함께 산정해 적용합니다.

| 대상 | 보정 전 | 보정 후 | 구간 폭 중앙값 |
| --- | ---: | ---: | ---: |
| 학습기 홀드아웃 183,526건 | 76.04% | **90.22%** | 1.645 -> 1.913%p |
| API 경로 200건 | - | **93.00%** | 1.572%p |

**목표는 90% 고정입니다.** 80% 는 보정 후에도 76.77% 라 표기와 실제가 어긋납니다.

구간은 부가 정보이므로 분위 아티팩트가 없거나 값 형태가 어긋나면 `None` 을 돌려주고 점 추정은 그대로 냅니다. 응답 필드도 전부 nullable 이라 구 모델은 원본 `JsonResponse` 계약을 유지합니다.

---

## 6. 남은 작업

1~3번은 2026-08-04 에 전부 처리했습니다. **두 건은 완료가 아니라 측정 후 기각**입니다.

| 순서 | 작업 | 결과 |
| --- | --- | --- |
| 1 | 참가자 수 수집 | **기각.** 수집은 됐으나(2025년 116,397건, 보유율 100%) 오라클 상한이 RMSE 4.9%, 이력은 0.35% 뿐 |
| 2 | 프론트엔드에 구간 노출 | **완료.** `detail.html` 에 낙찰률·투찰가 범위와 피복률 표기 |
| 3 | 하이퍼파라미터 탐색 | **구조만 채택.** 점 추정·분위 파라미터를 분리했고 리프 상향은 운영 실측 후 기각 |

### 6.1 기각 목록 (다시 하지 마십시오)

| 항목 | 근거 문서 |
| --- | --- |
| 세그먼트별 보정 배율 | 전역 배율이 8개 구간 전부에서 88.2~91.1%. 세그먼트 배율은 전부 더 나쁨 |
| 참가자 수 이력 특징 | [`servc_restricted_competition_20260803.md`](../design/servc_restricted_competition_20260803.md) 2.3 |
| 분위 모델 리프 조정 | 같은 문서 7장. 폭이 U자를 그리며 현행 63 이 최소 |
| 하한율 결측 역산, 공고명 텍스트 | 이전 세션 기각 |
| 고정 시간 창 기관 이력(30/90/365일) | [`servc_rolling_institution_20260805.md`](../design/servc_rolling_institution_20260805.md) 2장. 특징 9개를 넣어도 이득 0 |
| 목록 API 로 하한율 보강 | [`servc_lwlt_availability_20260804.md`](../design/servc_lwlt_availability_20260804.md) 3.1. 현행 결측 227건 중 값 확보 0건 |
| 결측 전체를 비적용으로 해석 | [`servc_lwlt_availability_20260804.md`](../design/servc_lwlt_availability_20260804.md) 2.3. 결측의 76.8%는 방법명으로 설명 불가 |
| `subsample_freq` 1/5 | [`servc_hyperparam_search_20260804.md`](../design/servc_hyperparam_search_20260804.md) 3장. MAE 이득 0.0001%p, 0.5%p 적중 -0.04%p |

### 6.2 지금 남은 것

| 순서 | 작업 | 근거 |
| --- | --- | --- |
| 1 | 하한율 결측 구간의 낙찰방식별 난이도 대책 | 규칙 복원은 실제 결측 행 적용률 0%로 기각됐습니다. 결측은 수의시담 등 제도상 하한율이 없는 방식에 몰립니다 |
| 2 | 공고 첨부 문서에서 하한율 확보 여부 결정 | 실제 누락과 제도상 비적용을 먼저 구분해야 하며 API 경로는 닫혔습니다. HWP/PDF 파서 비용 판단이 필요합니다 |
| - | ~~물품 모델을 학습 경로에 올리기~~ | **완료 및 승격 (2026-08-06).** 승격 후 3,000건 MAE 2.8958, R2 0.4160 |
| - | ~~기각 후보 세 건 재확인~~ | **완료 및 채택 (2026-08-06).** 리프 255와 지수감쇠 반감기 20건 결합을 검증·승격했습니다 |
| - | ~~MLOps 알림 구현~~ | **완료 (2026-08-05).** `src/tasks/notifier.py`, 5개 지점 배선, 테스트 11건. `MLOPS_WEBHOOK_URL` 미설정이라 실환경 수신만 미검증 |
| - | ~~as-of 평가 스냅샷~~ | **기각 (2026-08-05).** 제곱오차 t=2.23, 구간 폭 t=6.39 로 현행 조회가 우세. 현재 시점 이력은 표본이 많아 추정이 안정적이며 그 이점이 시점 불일치를 덮습니다 |

기각 후보 재확인은 끝났습니다. 같은 학습 상한의 편향 없는 평가에서 리프 255와
지수감쇠 결합이 MAE 3.13%를 개선했고, 운영 쌍대 비교에서도 3.42% 개선되어
승격했습니다. 상세는
[`servc_unbiased_candidate_recheck_20260805.md`](../design/servc_unbiased_candidate_recheck_20260805.md)입니다.

물품은 정식 학습기가 이미 `--category Thng`을 지원하는 것을 확인해 784,266행으로
재학습·승격했습니다. 기존 4특징 규칙 모델의 R2 -0.2133은 승격본 0.4160으로
개선됐습니다. 상세는
[`thng_retraining_20260806.md`](../design/thng_retraining_20260806.md)입니다.

하한율 결측 축소는 2026-08-04 에 한 번 기각됐다가 **2026-08-05 에 판정을
되돌렸습니다.** 기각 근거였던 미개찰 공고 10,434건은 학습 모집단과 낙찰방법 표기
관행이 달랐습니다. 학습 데이터 917,629행에서 재측정하면 결측 205,238건 중
**76.8%(157,647건)가 낙찰방법으로 설명되지 않고**, 거의 전부가 `공고서참조` 한
방법에 몰려 있습니다. 이 방법은 같은 이름 안에서 79.4%가 하한율을 보유하므로
제도상 비적용이 아니라 값을 공고 문서로 넘긴 것입니다.

되돌리지 않는 부분은 하나입니다. **결측을 역산하거나 대치하지 않습니다.** 결측
안에 진짜 비적용(23.2%)이 섞여 있어 일괄 보간은 허위 제약을 주입합니다. 확보는
공고 원문에서 실제 값을 가져오는 방식이어야 합니다. 상세는
[`servc_lwlt_availability_20260804.md`](../design/servc_lwlt_availability_20260804.md)입니다.

### 6.3 2026-08-05 에 찾은 결함

**모델이 아니라 배관이 문제였습니다.** 특징·파라미터 실험을 아무리 해도 운영이
좋아지지 않던 이유의 상당 부분이 여기 있었습니다.

| 결함 | 성격 | 운영 효과 |
| --- | --- | --- |
| `inst_sample_cnt` 가 서빙에서 항상 0 | 값이 아예 전달되지 않음 | **MAE -6.1%, 구간 폭 -8.3%** |
| 재발주 이력 조회에 카테고리 필터 없음 | 다른 모집단에서 계산 | 중립 |
| 재발주 이력 학습이 동시각을 포함 | **학습 쪽이 틀림.** 미래 정보 누수 | **MAE +0.33%** (성능을 내주고 정합성을 삼) |

세 번째는 성능이 나빠지는 쪽을 택한 것입니다. 예측은 개찰 전에 이루어지므로
같은 날 개찰될 다른 공고의 결과는 예측 시점에 없습니다. 학습이 그것을 보면
지표를 신뢰할 수 없습니다. 상세 근거는 설계 문서 5.3 절입니다.

세션 전체로는 운영 MAE 1.6838 -> 1.5867(-5.8%), 구간 폭 2.0745 -> 2.0103
(-3.1%) 입니다.

앞의 것 하나가 이 세션에서 시도한 모든 특징·파라미터 실험을 합친 것보다 큽니다.
**재학습 없이** 입력만 학습과 맞춘 결과입니다. 상세는
[`servc_feature_parity_20260805.md`](../design/servc_feature_parity_20260805.md).

찾은 방법은 단순합니다. 같은 공고에서 학습 프레임과 서빙 프레임의 전체 특징값을
나란히 놓고 특징별 상관을 냈습니다
([`scripts/audit_feature_parity_values.py`](../../scripts/audit_feature_parity_values.py)).

**기존 `test_serving_feature_parity.py` 는 이 결함을 통과시켰습니다.** 키가
있는지만 보고 값이 맞는지는 보지 않았기 때문입니다. 회귀 테스트 3건을 더해
세션이 있을 때 조회값이 실제로 들어오는지 봅니다.

### 6.4 서빙 모델이 최신 20% 를 학습하지 않고 있었습니다

오늘 찾은 것 중 가장 큰 결함입니다. 상세는
[`servc_refit_on_full_20260805.md`](../design/servc_refit_on_full_20260805.md).

`trainer` 가 시간순 80/20 으로 나눈 뒤 **앞 80% 로 학습한 모델을 그대로
저장·승격**하고 있었습니다. 시계열 분할이라 버려지는 20%(183,526행)는 가장 최신
구간이며, 2024년 후반과 2025년 전체가 여기 들어갑니다. 분위 모델도 같았습니다.

| 확인 방식 | MAE 개선 |
| --- | ---: |
| 운영 쌍대 비교 (낙관 편향 포함) | -6.3%, 구간 폭 -18.5% |
| **편향 없는 확인** | **-7.12%** (t=-52.27) |

**이 결함이 오늘 하루를 설명합니다.** 홀드아웃 실험은 2024년까지 전량을
학습하고 운영은 2024년 중반까지만 학습한 모델을 썼습니다. 두 경로의 학습 범위가
애초에 달랐으므로 한쪽의 개선이 다른 쪽에서 재현될 이유가 없었습니다. 6.5 절의
"홀드아웃을 믿지 말라" 는 교훈은 여전히 유효하나, 그 원인의 상당 부분이
방법론이 아니라 **구조 결함**이었습니다.

수정 후 같은 학습 상한으로 세 후보를 재검증했고 리프 255와 지수감쇠 결합을
채택·승격했습니다(6.2절).

### 6.5 이번에 얻은 방법론

**성능이 안 오르면 모델보다 배관을 먼저 의심하십시오.** 2026-08-05 에 특징 두
건과 파라미터 두 건을 실험해 전부 기각했는데, 정작 이득은 서빙에서 값이 하나
빠져 있던 것을 고쳐서 나왔습니다(MAE -6.1%). 실험 전에 대조부터 했다면 하루를
아꼈을 것입니다.

**요약값 비교로 우열을 말하지 마십시오.** 1,000건 표본의 MAE 표준오차가 0.099
인데 관측 차이가 0.0015 인 것을 읽고 방향을 단정한 적이 있습니다. 같은 공고에
두 모델을 돌리는 쌍대 비교
([`scripts/compare_servc_models_paired.py`](../../scripts/compare_servc_models_paired.py))
를 쓰면 표준오차가 22배 작아지고, 그 표본이 잡을 수 있는 최소 차이를 함께
출력하므로 "판별 불가" 를 명시할 수 있습니다.

**평가 표본을 서비스 모집단과 맞추십시오.** `--require-lwlt` 로 하한율 보유
건만 채점해 왔는데, 결측은 전체의 30% 이고 오차가 3.3배입니다. 사용자가 겪는
나쁨이 몰린 구간을 측정에서 빼고 있었습니다.

**이력 특징을 검증할 때는 전량에 붙인 뒤 잘라내십시오.** `attach_*` 는
`shift(1).expanding()` 이라 프레임을 연도로 자르면 각 기관의 연초 건이 최소
표본에 걸립니다. 이 함정으로 "train/serve skew 실재" 라는 오진단을 낸 적이
있습니다.

**홀드아웃은 후보를 거르는 데까지만 씁니다.** 전량 재적합 수정 전에는 리프 상향의
홀드아웃 이득이 운영에서 재현되지 않았습니다. 학습 상한을 맞춘 뒤에는 리프 255와
지수감쇠 결합이 편향 없는 검증과 운영 쌍대 비교에서 모두 우세했습니다. 이 사례는
홀드아웃만으로 승격하지 말아야 한다는 원칙과 학습·운영 모집단을 먼저 맞춰야 한다는
원칙을 함께 보여 줍니다.

**측정 대상이 서로 묶여 있는지 먼저 확인하십시오.** 2026-08-04 의 "정확도와 구간
폭은 맞바꿔야 한다" 는 판정은 틀렸습니다. 학습기가 점 추정과 분위 모델에
`LGB_BASE_PARAMS` 하나를 쓰고 있어 리프를 올리면 두 모델이 동시에 바뀌었을
뿐입니다. `QUANTILE_PARAM_OVERRIDES` 로 갈라 놓으니 구간 폭이 점 추정 용량에
전혀 반응하지 않습니다.

**표본의 결론을 다른 모집단으로 옮기지 마십시오.** 하한율 결측 기각도 같은
유형의 오류였습니다. 미개찰 공고 10,434건에서 성립한 결론을 학습 데이터
917,629행으로 일반화했는데, 두 모집단은 낙찰방법 표기 관행이 다릅니다.


**홀드아웃 개선을 운영 개선으로 단정하지 마십시오.** 이번 리프 255도 전량 재적합,
편향 없는 재검증, 필터 없는 운영 쌍대 비교를 모두 통과한 뒤에만 승격했습니다.

승격 후 검증은 **백업을 임시 ID 로 서빙 루트에 복제해 같은 표본에서 두 모델을
동시에 호출**하는 방식이 확실합니다. 표본이 다르면 우열을 가릴 수 없습니다.
측정 스크립트는 [`scripts/eval_servc_api_path.py`](../../scripts/eval_servc_api_path.py) 입니다.

---

## 7. 주의

### 7.1 병렬 세션과의 간섭

같은 저장소에서 **다른 Claude Code 세션이 프론트엔드와 DB 재현 리팩토링을 동시에** 진행했습니다. 두 세션이 모두 `git add -A` 를 쓴 탓에 서로의 변경을 커밋하는 일이 양방향으로 일어났습니다.

| 방향 | 사례 |
| --- | --- |
| ML -> 프론트 | 로그인 next 리다이렉트, 예측 AJAX 전송 방식 변경을 함께 커밋 (되돌림) |
| 프론트 -> ML | `d0d9b98` 예측 구간 스크립트, `56a26a6` 문서 인덱스, `0b781d0` 미완성 ML 작업 트리 |

특히 `0b781d0` 은 **ML 작업의 중간 상태**가 커밋된 것입니다. `e1f7255` 로 병합됐으므로 후속 수정 `cbb9e5b` 가 반드시 함께 있어야 정상 동작합니다.

테스트도 간섭받습니다. `test_bid_list_parity` 2건이 일시 실패했는데, 상대 세션이 `src/app` 을 수정하는 중간 상태를 읽었기 때문입니다. 단독 재실행에서는 통과했습니다.

**대응 원칙입니다.**

- `git add -A` 를 쓰지 않고 **변경한 경로를 명시적으로 스테이징**합니다.
- 테스트가 `src/app` 계층에서 실패하면 **단독 재실행으로 먼저 격리**합니다. 상대 세션의 중간 상태일 수 있습니다.
- 병합 전 `git log --oneline main..HEAD` 로 **남의 커밋이 섞이지 않았는지** 확인합니다. 섞였으면 체리픽으로 분리합니다.
- 근본 해결은 세션별 `git worktree` 분리입니다. 작업 디렉터리가 갈리면 간섭이 사라집니다.

### 7.2 데이터셋

`data/feature_store/dataset_Servc.parquet` 은 **917,629행 x 28컬럼**이어야 합니다. 실험 전 행 수를 확인하십시오. 과거에 테스트 픽스처가 80행으로 덮어쓴 적이 있습니다.

### 7.3 모델 가중치

`.gitignore` 의 `*.bin` 때문에 `model.bin` 은 git 에 없습니다. `metadata.json` 만 추적됩니다. 다른 기기에서는 재학습 후 `promote` 가 필요합니다.

---

## 8. 재개 절차

```bash
git checkout main && git pull
.venv/bin/python -m pytest tests/ -q                      # 전량 통과 확인
.venv/bin/python scripts/validate_agent_rules.py          # 6/6 기대

# 운영 성능 기준선 (2026-08-06, 서빙 v_20260806_025423_494)
.venv/bin/python scripts/eval_servc_api_path.py --samples 4000 --year 2025 --seed 42
#   MAE 1.4471 / 0.5%p 적중 64.02% / 구간 폭 중앙값 1.7286 기대

# 학습·서빙 35개 특징 정합성. 예상 밖 차이가 생기면 배관을 점검합니다
.venv/bin/python scripts/audit_feature_parity_values.py --samples 600
#   알려진 시점 차이 9건, 예상 밖 차이 0건 기대
```
