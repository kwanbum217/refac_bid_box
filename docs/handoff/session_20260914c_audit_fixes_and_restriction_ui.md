# 세션 인수인계: 2026-09-14 (셋째) 외부 감사 보강과 제한정보 화면·백필

> **작성일**: 2026-09-14
> **작성자**: Claude Opus 5 (Orca 코디네이터, 사용자 부재 중 권장안으로 결정)
> **기준 커밋**: `cc414933` (`main`)
> **이어받은 문서**: [`docs/handoff/session_20260914b_restriction_collector.md`](session_20260914b_restriction_collector.md)

---

## 1. 한 줄 요약

공고 상세에 제한정보를 표시하고(그룹 해석 문구·접기·차단 사유별 안내 포함) 입찰 중 공고의 제한정보를 백필했습니다.
외부 감사 보고서를 코드로 대조해 사실로 확인된 결함 중 기관명 catalog 정합성, 로컬 캐시 상한, Chroma 구조 가드,
Caddy digest 고정을 Orca 워커 두 대로 병렬 구현·검증해 병합했습니다.

---

## 2. 병합 내역

| 병합 커밋 | 내용 | 검증 |
| --- | --- | --- |
| `2ae0f22b` | 공고 상세 참가 자격 제한 카드 (면허제한 그룹, 참가가능지역) | 전량 4,737 passed, CI 성공 |
| `c4da56ed` | 그룹 해석 문구, 셋 초과 그룹 접기, 계산 차단 사유 8종별 안내 | 전량 4,739 passed, CI 성공 |
| `23d9909b` | `scripts/backfill_bid_restrictions.py` (월 단위, 재실행 멱등) | 전량 4,742 passed, CI 성공 |
| `505023bd` | 기관명 catalog 빈 일치 시 DB 해석 확인, 빈 해석 결과 300초 캐시, 수집 후 catalog 갱신 | Level 1 게이트 9개 전부 통과, 리뷰 결함 0, 전량 4,731 passed, CI 성공 |
| `cc414933` | 로컬 캐시 1,000항목 상한, Chroma 임베디드 전용 가드 테스트, Caddy digest 고정·전 이미지 digest 테스트 | 게이트 3의 운영 compose 검증만 필수 환경변수 부재로 실패(가짜 환경변수로 별도 실행 통과), 나머지 통과, 리뷰 결함 0, 전량 4,736 passed |

모든 premerge 증거는 `HTTPS_PROXY=http://127.0.0.1:9` 로 네트워크를 막은 조건에서 기록했습니다.

---

## 3. 사용자 부재 중 권장안으로 결정한 것

| 결정 | 내용 | 근거 |
| --- | --- | --- |
| 그룹 의미 문구 | "그룹 중 하나를 충족하면 참가, 같은 그룹 업종은 모두 필요한 것으로 보입니다. 최종 자격은 공고문 확인" | 사용자 지시. 실제 공고 `R26BK01725356`(12그룹)이 업종 변형 조합을 그룹마다 하나씩 펼친 형태 |
| 접기 기준 | 그룹 3개까지 펼침, 나머지는 `<details>` | 사용자 지시(접기) |
| 백필 범위 | 공고일 2025-07-01 이후 입찰 중 공고를 기간 조회로 백필 (15개월, 약 530회 호출) | 그 이전 "입찰 중" 3,213건은 60% 가 마감일 2028년 이후인 비정상 값이고 대부분 물품 변경공고. 공고번호 단건 조회(약 2,600회)는 보류 |
| 감사 보강 범위 | catalog 정합성, 캐시 상한, Chroma 가드, Caddy digest | 감사 주장 중 코드로 사실 확인되고 규모가 작은 것. readiness 의 LLM 필수 여부(운영 정책)는 결정하지 않음 |

---

## 4. 실측

| 항목 | 결과 |
| --- | --- |
| 백필 | 실패 구간 0. 면허제한 648,683행, 참가가능지역 412,972행 |
| 백필 커버리지 | 공고일 2025-07 이후 입찰 중이면서 `indstrytyLmtYn=Y` 인 공고 4,030건 중 4,029건에 제한정보 존재 |
| 실제 화면 | `id 10234594` 에서 해석 문구, "(모두 필요)" 표시, "나머지 9개 그룹 펼치기" 확인 |

---

## 5. Orca 기록 (Run `run_f0b675534c8b`)

| Task | 역할 | 모델 | 결과 |
| --- | --- | --- | --- |
| `task_39626b833b7d` | 빌더 ac1 catalog | Antigravity `gemini-3.8-flash-medium` | succeeded, 보고 4,731 passed 가 게이트 재실행과 일치 |
| `task_d62f022c989b` | 빌더 ac2 캐시·가드 | Antigravity `gemini-3.8-flash-medium` | succeeded, 보고 4,736 passed 가 게이트 재실행과 일치 |
| `task_915e3d7af1af` | 리뷰어 ac1 | OpenCode `muse-spark-1.3-contributor-free` | pass, 결함 0 |
| `task_d27772b8bccb` | 리뷰어 ac2 | OpenCode `muse-spark-1.3-contributor-free` | pass, 결함 0 |

- 네 워커 모두 런처 경로(비감독 Dispatch)라 `worker-release` 후 `terminal close` 로 회수했습니다. 워크트리 두 개와 브랜치는 병합 후 `orca worktree rm` 으로 제거했습니다.
- **감시 중 개입 1건**: ac2 빌더가 `env | grep -E 'DB_|REDIS_|CORS_|TLS_|SECRET_'` 승인을 요청해 거부했습니다. 비밀값이 기록에 남기 때문입니다. 운영 compose 검증은 코디네이터가 가짜 환경변수로 실행하도록 지시했습니다.
- **코디네이터 실수 2건**: (1) 게이트 1차에 compose 용 가짜 환경변수를 게이트 전체에 넣어 pytest 설정 검증이 깨졌습니다(워커 결함 아님, 재실행으로 통과). (2) 체인 대기 조건이 복사된 선행 출력의 종료 줄에 걸려 조기 종료했습니다.

---

## 6. 알려진 부작용과 남은 과업

| 항목 | 내용 |
| --- | --- |
| catalog DB 확인 비용 | 존재하지 않는 기관명(오타)은 300초마다 LIKE 해석 질의를 부릅니다. 콜드에서 수 초 걸릴 수 있습니다. 정합성 우선으로 수용 |
| 미채움 공고 | 공고일 2025-07 이전 "입찰 중" 3,213건. 마감일 비정상 값이 많아 보류 |
| 감사 잔여 (사실 확인됨) | readiness 의 LLM 필수 설정으로 Ollama 장애 시 배포 때 Caddy 미기동, LLM TTFT·토큰 지표 부재, 프롬프트 인젝션 fixture 부재, release 가 태그 생성뿐, backup 서비스 최소권한, `current_state_facts.yaml` 의 `updated_at` 갱신 누락 |
| 감사 오판 | `source_commit ≠ HEAD` 는 설계. backup 이 root 비밀번호를 받는다는 주장은 사실 아님(앱 계정) |
| 이전 과업 | 첫 야간 수집 뒤 적재 확인, Windows 실기(G2), chromadb 재확인(2026-12-31), 운영 배포 반영 |

---

## 7. 자원 상태 (세션 종료 시점)

| 대상 | 상태 |
| --- | --- |
| Docker | 전 서비스 실행 중. `app`·`worker` 는 `cc414933` 코드로 재시작 |
| 로컬 DB | `alembic_version` = `9d4e2b7a1c63`. 제한정보 두 테이블에 백필 적재 |
| 워크트리·브랜치 | 주 저장소 하나(`main`). 이 문서 브랜치 `docs/handoff-20260914c` |
| Orca | Run `run_f0b675534c8b` 의 Task 전부 종결, 회수 대기 0, 잔류 세션 0 |
