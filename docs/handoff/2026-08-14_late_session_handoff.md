# 2026-08-14 후반 세션 인수인계

> **작성일**: 2026-08-14
> **기준 커밋**: `main` `f47d448`, `origin` 동기화 완료
> **코디네이터**: Claude Opus 5
> **직전 정본**: [`2026-08-14_session_handoff.md`](2026-08-14_session_handoff.md)
> **검증 상태**: 1011 passed / 2 skipped, ruff 통과, `validate_agent_rules` 6/6

---

## 1. 이 세션이 남긴 가장 중요한 것

**규약이 없으면 성능 판정은 측정 조건 선택으로 결정됩니다.** 직전 세션이
"G3 판정이 표본 수에 따라 뒤집힌다" 를 미해결로 남겼고, 이 세션은 그 원인이
표본 수 자체가 아니라 **게이트 규약의 부재**임을 확인해 규약을 제정했습니다.

정본은 [`../ops/latency_gate_protocol.md`](../ops/latency_gate_protocol.md)
입니다. 앞으로 모든 레이턴시 측정은 이 문서를 따릅니다.

| 규정 | 값 | 근거 |
| --- | --- | --- |
| 표본 수 | `n >= 오염요청수 x 60` (예측 600, SSE 60) | 지연 이벤트 2회까지 P95 오염 없음 |
| warmup | 동시성 수만큼, 표본 제외 | 냉시동 배치가 표본에 들어감 |
| 반복·대표값 | 3회 이상, **최악값** | 중앙값은 3회 중 2회 통과로 통과가 됨 |
| c10 기준 | P95 100ms 이하 | G3 |
| 초과율 | 관측 0.5% 이하 **그리고** Wilson 상한 1.0% 이하 | 관측 0건일 때 Wilson 은 표본 수만의 함수 |
| 주변 부하 | 코어당 중앙 30% / 최대 50% 이하, **값을 기록** | 부재는 증명 불가, 부하는 측정 가능 |
| 소급 | **제정 이전 측정은 통과·미달 양방향 무효** | 한쪽만 무효면 유리한 쪽 고르기 |

---

## 2. 성능 결과

### 2.1 예측 API — 게이트 통과

`PREDICTION_GC_MODE=freeze` 를 기본값으로 채택했습니다.

| 지표 | 채택 전 | 채택 후 |
| --- | ---: | ---: |
| c10 최악 P99 | 108.55ms | **59.32ms** |
| 100ms 초과 (1,800 요청) | 21건 | **0건** |
| c10 P50 | 39.47ms | 38.96ms (대가 없음) |
| c1/c2/c4 최댓값 | 71~93ms | **21~43ms** |

원인은 generation-2 GC 였고 `gc.freeze()` 가 모델 가중치를 영구 세대로 옮겨
순회 대상에서 제외합니다. 회수 자체는 유지되므로 10분 지속 부하 RSS 가
평평합니다.

**정본 기준선**은 주변 부하를 기록한 재확인 측정값입니다.

| 동시성 | P50 | P95 | P99 | Max | 회귀 판정선 (P95 +10%) |
| ---: | ---: | ---: | ---: | ---: | ---: |
| c1 | 12.37 | 15.47 | 17.56 | 20.85 | 17.02 |
| c2 | 17.17 | 19.79 | 25.32 | 30.39 | 21.77 |
| c4 | 28.29 | 33.46 | 39.45 | 42.76 | 36.81 |
| **c10** | **42.45** | **56.45** | **62.08** | **73.01** | **62.10** |

`batch-disable` 은 **기각**했습니다. 초과 30건으로 기본(21건)보다 나쁩니다.
코드는 남아 있으나 운영에 쓰지 마십시오. `threshold` 는 보류입니다.

### 2.2 SSE — c1 통과, 동시성 기준 없음

| 항목 | 값 | 판정 |
| --- | ---: | --- |
| c1 첫 토큰 P95 (3회 최악) | 1.545초 | 통과 (목표 3초) |
| c1 전체 P95 | 7.287초 | 통과 (목표 20초) |
| c4 첫 토큰 P95 | 18.086초 | **판정 불가** |

c4 는 게이트 실패가 아닙니다. **`OLLAMA_NUM_PARALLEL` 미설정으로 Ollama
인스턴스 1개가 생성을 직렬 처리하는 배포 사실**입니다. 3초/20초 목표는
`scripts/benchmark_latency.py:138` 의 순차 루프로 측정된 c1 값이므로 c2 이상에
적용할 수 없습니다.

**SSE 표본은 회차당 30 으로 수행돼 규약 기준 60 에 미달합니다.** c1 판정은
마진이 커서 유지하되 기준선으로 쓰지 말고 다음 측정에서 60 이상으로
재수립하십시오.

### 2.3 G3 상태

| 항목 | 상태 |
| --- | --- |
| 예측 API c10 P95 | **통과** (규약 준수) |
| 예측 API c1/c2/c4 회귀 | **없음** |
| SSE c1 | **통과** (표본 미달, 기준선 아님) |
| SSE 동시성 | **기준 없음** |
| Windows Docker Desktop | 미검증. 실기 필요 |

**G3 전체를 통과로 선언하지 마십시오.** 항목별로 위 표대로 적으십시오.

---

## 3. 오케스트레이션 규약 변경

### 3.1 워커 배정을 코디네이터 토큰 기준으로 정합니다

정본은 [`../ops/orca_orchestration_playbook.md`](../ops/orca_orchestration_playbook.md)
4장이며 스킬 미러 3곳과 `.cursor`·`.antigravity` 상시 규칙에도 반영했습니다.

| 유형 | 처리 |
| --- | --- |
| 100줄 이상 분석·감사·측정 보고서 | 위임 |
| 긴 측정 실행 | 직접 (배경 실행이 더 쌉니다) |
| 50줄 이하 판정 문서 | 직접 (부대 비용이 쓰는 비용과 비슷) |
| 검증·병합 판정·게이트 기준 제정·되돌리기 어려운 조작 | **위임 금지** |

| 풀 | 배정 |
| --- | --- |
| Claude 구독 | 코디네이터 전용 |
| Antigravity Gemini Flash | **주력 워커** |
| Antigravity Claude 계열 | 별도 풀, 허용량 적음 |
| Codex | 주간 잔량 넉넉할 때만 |
| OpenCode 무료 | 임계 경로 금지 |

**절감률은 50~60% 이며 90% 가 아닙니다.** 검증 비용이 남습니다.

### 3.2 워커 지시 도달을 확인하십시오

**`dispatch --inject` 는 Antigravity CLI 에서 재현적으로 실패합니다.** 이 세션에서
두 번 모두 `ok: true` 를 반환하고 Task 가 `dispatched` 로 바뀌었지만 프롬프트는
비어 있었습니다. 6분간 워커 3대가 유휴였습니다.

우회는 `orca terminal send` 로 지시서 경로와 `worker_done` 명령 전문을 직접
전달하는 것입니다. 절차는 플레이북 5.2 절에 있습니다.

`terminal create` + 주입 경로 워커는 `worker-list`·`worker-read` 에 나타나지
않으므로 관측 수단이 `orca terminal read` 하나입니다. **`ps` 로 프로세스가 살아
있는 것은 진척의 근거가 아닙니다.**

워커 워크트리는 `orca worktree create` 로 만드십시오. `git worktree add` 로 만든
트리는 Orca 사이드바에 나타나지 않아 사용자가 워커 작업을 볼 수 없습니다.

---

## 4. 워커 산출물 검증 기록

**워커 4대의 산출물 4건 모두에 오류가 있었고 전부 코디네이터 검증에서
발견됐습니다.** 검증을 생략하면 이것들이 `main` 에 들어갑니다.

| 워커 | 잡은 것 |
| --- | --- |
| P4 Gemini | 부트스트랩이 실측과 4배 어긋남. 요청 독립 가정이 배치 상관을 지움 |
| P5 Opus 4.6 | 브랜치 고유 파일 0개 주장 → 실제 4개. `git diff main...branch` 를 merge base 기준으로 오독 |
| P6 Sonnet 4.6 | "GIL 로 스레드별 GC 제어 가능" 은 사실 아님. gen2 빈도 1/3 이 아니라 1/6 |
| P7 Gemini | `champion_summary.json` 미참조 주장 → 실제 참조되며 병합 시 허위 성능 정보 노출 |
| P8 Gemini | 표본 30 이 규약 60 미달. c4 를 c1 출처 목표로 미달 판정 |

### 4.1 코디네이터 자신의 오류 5건

같은 기준으로 기록합니다.

| 오류 | 내용 |
| --- | --- |
| compose `${VAR:-freeze}` | 빈 문자열을 미설정으로 보아 끌 수 없음. **18회 측정 무효**, 파일 21개 삭제 |
| 헬스체크 `/health` | 404. 정본은 `/api/v1/health/ready`. 측정 루프 2회 타임아웃 |
| c1/c2/c4 1회차 비교 | 자기 규약 3회 규정 위반. c2 를 회귀로 오판 |
| "단독 실행" 보고 | 자기 워커만 끄고 다른 세션의 Codex(2h25m 실행)를 확인하지 않음 |
| P4 사양 범위 | `phase8_predict_tail_*` 로 한정해 판정 근거였던 107.6ms 를 제외시킴 |

---

## 5. 미병합 브랜치

| 브랜치 | 상태 |
| --- | --- |
| `perf/predict-tail` (`0fd489a`) | **병합 불가 판정 완료.** 진단 전용으로 유지. 근거는 `../ops/phase8_predict_tail_merge_verdict_20260814.md` |
| `integrate/arq-worker-cutover` | **폐기 판정, 사용자 지시로 삭제 보류.** 고유 파일 4개 중 `preprocess.py` 와 `champion_summary.json` 은 **병합 금지** |
| `feat/codex-task-routing` | 추가 조사 필요. 고유 파일 17개, 현행 스킬 인덱스 12개와 정합 확인 선행 |

---

## 6. 다음 순서

| 순서 | 작업 | 비고 |
| ---: | --- | --- |
| 1 | SSE 기준선을 회차당 60 이상으로 재수립 | 규약 1.2. c1 은 마진이 커서 급하지 않음 |
| 2 | `OLLAMA_NUM_PARALLEL` 실험 후 SSE 동시성 기준 제정 | c4 18초의 원인. 애플리케이션 아님 |
| 3 | Windows Docker Desktop 전체 스택 | 실기 필요 |
| 4 | 수집 2·3회차 관찰 | 완료 기준은 `2026-08-14_collection_observation_round1.md` 5장 |
| 5 | `feat/codex-task-routing` 판정 | 스킬 인덱스 정합 확인 선행 |

`threshold` GC 모드 재검토는 불필요합니다. `freeze` 가 채택됐고 두 모드는
배타적입니다.

---

## 7. 다른 세션 정보

이 세션 중 다른 Claude 세션이 **Cerebras Gemma-4 31B** 워커를 준비했다는 상태
메시지가 Orca 로 들어왔습니다. 무료 크레딧 활성화와 API 호출 성공(15ms)을
보고했고 `opencode -m cerebras/gemma-4-31b` 로 기동 가능하다고 합니다. 상세는
그 세션이 만든 `2026-08-14_cerebras_gemma4_worker_setup.md` 입니다. **이
세션에서 검증하지 않았습니다.**

---

## 8. 종료 시 환경 상태

| 항목 | 상태 |
| --- | --- |
| Docker 컨테이너 | **실행 중** (`PREDICTION_GC_MODE=freeze`) |
| Orca 워커 | 전량 반납, 워크트리 제거 |
| `main` | `f47d448`, `origin` 동기화 |
| 미커밋 변경 | 없음 |
| 다른 세션 | Codex 세션 1개 실행 중. **건드리지 마십시오** |
