# 무료 워커 스택 벤치마크

> **작성일**: 2026-08-20
> **관련 문서**: [`../../docs/ops/orca_do_not_repeat.md`](../../docs/ops/orca_do_not_repeat.md) 17 장

---

## 1. 무엇을 재는가

**모델이 아니라 워커 스택을 잽니다.** 하나의 측정 단위는
`모델 + 제공자 + CLI 하네스` 셋의 조합입니다. 같은 모델이라도 OpenCode 로
띄운 것과 Kimi 로 띄운 것은 다른 스택이며, 승인 정책과 대화 경로가 달라
결과가 갈립니다.

`or-free/nemotron-ultra`(Kimi)와 `opencode/nemotron-3-ultra-free`(OpenCode)를
따로 잰 이유가 이것입니다.

---

## 2. 구조

    builder_01/
      capsule.yaml     과제 사양 (ORCA_TASK_CAPSULE_V2)
      logs/            스택별 실행 로그 꼬리 4KB
    scoring.py         구현 독립 행동 시나리오 채점기
    runner.sh          상한 3 을 지키며 대기열을 소화하는 러너
    results/           회차별 결과 JSON

---

## 3. 채점 방법

각 스택은 격리 워크트리에서 같은 Capsule 을 받아 실제로 파일을 고치고
테스트를 씁니다. **각자 자기 테스트를 통과하므로 그것으로는 우열이
갈리지 않습니다.**

그래서 `scoring.py` 를 따로 둡니다. 구현 내부(함수명, 상태 파일 스키마)를
보지 않고 `main()` 의 종료 코드와 stdout 만 검사하므로, 어떤 구현이든 같은
계약을 지켰다면 통과합니다.

```bash
uv run python benchmarks/free_workers/scoring.py <임시디렉터리>
```

---

## 4. 이 벤치마크로 말할 수 있는 것과 없는 것

**말할 수 있는 것**

- 이 스택이 저위험 Python builder 과제를 완주하는가 (pass/fail)
- 완주하지 못한다면 어떤 방식으로 실패하는가

**말할 수 없는 것**

- 스택 사이의 속도 우열. `results/2026-08-20.json` 은 스택당 1회 실행이며,
  무료 엔드포인트의 큐·콜드스타트·429 편차를 분리하지 못했습니다.
- `benchmarker`, `documenter` 적합성. 잰 적이 없습니다.
- `reviewer` 적합성. 병합 판정에 쓰이는 임계 경로라 무료 풀에 열지 않습니다.

---

## 5. 다음 회차에 반드시 지킬 것

2026-08-20 1회차에서 어긴 것들입니다. 상세는 반복 금지 17 장입니다.

| 항목 | 규칙 |
| --- | --- |
| 실격선 | **시작 전에** 정하고 `capsule.yaml` 에 적습니다. 1회차는 결과를 보고 정했습니다 |
| 채점기 | 모델 실행 **전에** 자체 시험을 끝내고 동결합니다. 1회차는 결과를 보고 고쳤습니다 |
| 반복 | 스택당 최소 3회. 대표값은 median 과 p95 로 적습니다 |
| 과제 | 1종으로 전역 순위를 만들지 않습니다. 역할별로 나눠 잽니다 |
| 하네스 | 대화 경로 유무가 다르면 결과에 그 사실을 함께 적습니다 |

**전원 만점이 나오면 순위를 매기지 말고 과제를 바꾸십시오.** 천장에 붙은
측정은 변별이 아니라 측정 실패입니다.

---

## 6. 언제 이 벤치마크를 멈추는가

**2026-08-21 3차를 끝으로 정기 경합은 하지 않습니다.**

이 벤치마크의 존재 이유는 `FREE_BUILDER_ORDER` 배정 순서 하나를 정하는
것입니다. 따라서 **측정이 그 결정을 더 이상 바꾸지 못하면 그 시점이 종료
시점**입니다.

3차가 그 근거를 스스로 만들었습니다. `or-free/laguna-xs` 는 2차에서
429~512초에 3/3 으로 완주했는데, 같은 base ref 와 같은 동시 3대 조건으로
하루 뒤 다시 재자 3회 전부 720초 시한을 넘겨 0/3 이 됐습니다. 그중 2회는
채점 6/6 만점 코드를 만들어 놓고 커밋에 도달하지 못한 것이므로 **능력
저하가 아니라 무료 엔드포인트의 응답 지연**입니다.

**증명된 것은 n=3 수준에서 스택 간 순위가 안정적으로 재현되지 않는다는
것까지입니다.** 표본을 얼마나 늘리면 수렴하는지는 재 본 적이 없으므로
"아무리 늘려도 수렴하지 않는다" 고 말할 수 없습니다. 다만 순위를 쓸 수 있을
만큼 좁히려면 회차를 크게 늘려야 하고, 그 비용을 들여 얻는 것은 여전히
**측정한 날의 순위**입니다. 4차를
돌리면 또 다른 순서가 나오고 그 순서도 다음날 무효가 됩니다. 비용만 늘고
결정 품질은 나아지지 않습니다.

**대신 상시 관측으로 갈음합니다.** `scripts/orca_model_router.py` 의
`apply_inventory_history()` 가 실재 관측 이력을 누적해 소멸·의심 후보를
자동으로 강등·제외합니다. 정기 경합보다 이 경로를 넓히는 편이 낫습니다.

**재실행 트리거는 셋뿐입니다.** 이 밖의 이유로는 돌리지 않습니다.

1. 무료 풀에 새 스택을 편입할 때 (해당 스택만. 전체 재측정 아님)
2. 관측 이력이 어떤 스택을 소멸 확정해 빈자리를 대체 후보로 채울 때
3. 과제 종류를 새로 열 때. 현재는 저위험 Python builder 1종만 측정됐고
   `reviewer`, `benchmarker`, `documenter` 적합성은 잰 적이 없습니다

---

## 7. 채점 실행 함정

`scoring_02.py` 는 후보 모듈을 경로로 로드하는데, 후보가
`from scripts.orca_model_router import MODEL_POOL` 를 하므로 **저장소 루트가
`sys.path` 에 있어야** 합니다.

`uv run python benchmarks/free_workers/builder_02/scoring_02.py` 로 실행하면
`sys.path[0]` 이 스크립트 디렉터리라서 후보 전원이
`ModuleNotFoundError: No module named 'scripts'` 로 **채점 불가**가 됩니다.
점수 0 이 아니라 오류이므로 조용히 틀리지는 않지만, 원인을 모르면 산출물
결함으로 오해합니다.

```bash
PYTHONPATH=$(git rev-parse --show-toplevel) \
  uv run python benchmarks/free_workers/builder_02/scoring_02.py \
    --candidate <태그>=<산출물 경로>
```

집계는 실행 조건을 인자로 받습니다. 기본값을 두지 않는 이유는, 조건을 바꿔
돌렸을 때 결과 JSON 이 조용히 이전 조건을 기록하기 때문입니다.

```bash
uv run python benchmarks/free_workers/aggregate.py \
  --runs <results.tsv> --scores <scores.txt> \
  --timeout-sec 720 --concurrency 3 --date <YYYY-MM-DD> \
  --note "<이 실행에만 해당하는 한계>" \
  --out benchmarks/free_workers/results/<날짜>-<이름>.json
```

일부 스택만 다시 잴 때는 `BENCH_STACKS` 로 좁히되, **원래와 같은 수의
스택을 동시에 투입해 경합 조건을 유지해야 합니다.** 혼자 돌리면 백엔드
경합이 사라져 성공 쪽으로 편향되고, 나머지 회차와 비교할 수 없게 됩니다.
