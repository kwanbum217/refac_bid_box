---
description: Phase 2 인프라 구축 (DB ORM, Redis 캐시, Celery/Arq 태스크 큐)
globs: src/app/models/**,migrations/**,src/tasks/**
alwaysApply: false
---

# 03-infrastructure-setup

해당 경로 작업 시 `.agents/skills/infrastructure-setup/SKILL.md`를 먼저 읽으십시오.

핵심 계약:
- MySQL 8 스키마 1:1 ORM 이식 및 이중화(SQLite fallback) 제거
- Redis 캐시 도입으로 `.django_cache` / locmem 전면 대체
- 수집 및 KB 업데이트 비동기 태스크 큐 오프로드
